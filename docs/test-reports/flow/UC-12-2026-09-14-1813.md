# UC-12 用户在 Flow 中使用内置输入类型测试报告

## 基本信息

时间：2026-09-14，Asia/Shanghai。代码版本：`62835f9 dirty`；包含并发 Maven 发布配置修改。目标 UC：`docs/uc/flow/UC-12 用户在 Flow 中使用内置输入类型.md`（ACCEPTED）。唯一主测试类：`core/src/test/java/org/cses/flow/core/services/flows/Uc12BuiltInInputsTest.java`，由旧 `Uc12BusinessInputsTest` 同步改名。本次只修改测试及报告，不修改生产实现或 UC。

## 测试范围

用户撤回宿主 Input 扩展需求，固定九种内置类型。UC 使用带 min/max 的 INTEGER；合法输入使用 Long 数值向 Integer 转换，YAML 数值默认值转换为 Integer；数值文本不属于合法 INTEGER 输入。原有公开发布、重新查询、启动、恢复、真实 ApplicationContext 重启、下游观察及无活动任务断言保留。S2 使用单个参数化方法覆盖三种输入位置 × 三种非法类型，共九个调用。

真实 JDK 25.0.4、Gradle Wrapper、Micronaut、PostgreSQL 17、Pulsar 4.0.7。专属 PostgreSQL 容器 `flow-input-pg-0914`，本机 55184，空库 `flow_input` 执行 `gen/sql/flow/001_create_flow_tables.sql`，另有空宿主库 `flow_input_host` 用于数据源隔离测试；专属 Broker `flow-input-pulsar-0914`，56654/58084，启动 health=ok。所有 UC 使用独立租户，`FLOW_POSTGRES_TEST_CLEANUP=true`。未访问开发数据库或业务 Broker。

执行期间使用临时环境脚本 `/tmp/flow-input-env.sh`（已删除，不保留凭据）；测试结果配置 `/tmp/flow-input-test.init.gradle`，按阶段和模块隔离 XML/HTML/binary。没有 Java 21 构建兼容性结论。清理重建后 `core/build/classes/java/main/META-INF/flow/inputs` 不存在；`META-INF/services/tools.jackson.databind.JacksonModule` 保留 `InputJacksonModule`。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 发布并运行内置输入 | `s1UserPublishesAndRunsBuiltInInputs` | PASS | 三处输入均为 INTEGER，分别 min=1/2/3、max=100、default=10/20/30；重新查询类型、key、displayName、required、上下界与默认值；启动 41L 转为 Integer 41；Pause 52L 转为52；下游输出 `41&#124;52&#124;builtin-default`，执行唯一，根 SUCCESS，无活动/未完成任务或 Pause。 |
| S2 拒绝非法定义及非内置类型 | `s2UserCannotPublishInvalidBuiltInDefinitions`，九参数 | PASS | 三处分别设置 min=101/max=100、默认值101、BUSINESS_CODE；发布逐一明确拒绝，草稿原文和版本不变，latest及精确正式版本均不存在，没有运行实例；删除并确认草稿不再可见、无等待任务。 |
| S3 启动值拒绝后纠正 | `s3UserCorrectsAnInvalidBuiltInStartupValue` | PASS | 数值101超出max100，抛业务错误且运行查询为空；63L转为Integer63，下游输出63且只执行一次；唯一实例SUCCESS，无活动/等待任务。 |
| S4 恢复值拒绝后纠正 | `s4UserCorrectsAnInvalidBuiltInResumeValue` | PASS | 查询当前Pause后提交101，明确拒绝，根/Pause状态、任务编号列表不变，Pause无结果且后续未执行；74L转为Integer74，同一实例恢复，下游输出74，终态SUCCESS且无等待。 |
| S5 重启后继续 | `s5UserReentersTheSystemAndContinuesWithPersistedBuiltInRules` | PASS | 关闭并重建ApplicationContext，仅用公开key/版本/运行编号重新查询；三处定义/默认值保持，已接纳启动81仍可查询，Pause编号一致；非法101不推进，合法92L使同一实例完成，下游输出 `81&#124;92&#124;builtin-default`，无活动或等待任务。 |

以上 S1/S3/S4/S5 初次正确样本证据：`/tmp/flow-input-results/uc12-corrected/core/test/xml/TEST-org.cses.flow.core.services.flows.Uc12BuiltInInputsTest.xml`；S2 独立复验证据：`/tmp/flow-input-results/uc12-s2-recheck/core/test/xml/TEST-org.cses.flow.core.services.flows.Uc12BuiltInInputsTest.xml`。模块回归再次覆盖完整主类。

## 执行命令与结果

```bash
JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :core:clean :server:clean :core:compileTestJava :server:compileTestJava
source /tmp/flow-input-env.sh
FLOW_TEST_RUN=uc12-corrected ./gradlew -I /tmp/flow-input-test.init.gradle :core:test --tests '*Uc12BuiltInInputsTest'
FLOW_TEST_RUN=uc12-s2-recheck ./gradlew -I /tmp/flow-input-test.init.gradle :core:test --tests '*Uc12BuiltInInputsTest.s2*'
FLOW_TEST_RUN=technical ./gradlew -I /tmp/flow-input-test.init.gradle :core:test --tests '*InputPluginTest' --tests '*PluginRegistryTest' --tests '*InputBindingTest' :server:test --tests '*InputViewTest' --tests '*PluginControllerTest' --continue
FLOW_TEST_RUN=modules ./gradlew -I /tmp/flow-input-test.init.gradle :core:test :server:test --continue
FLOW_TEST_RUN=modules ./gradlew -I /tmp/flow-input-test.init.gradle test --continue -x :core:test -x :server:test
```

- clean + compileTestJava：通过；日志 `/tmp/flow-input-test-compile.log`。
- 未配置环境的首次UC执行在夹具启动前明确失败；随后创建独立真实环境，未用替身绕过。
- 首次真实执行：样本错误地把INTEGER默认值和运行值写成数字文本，触发既有严格类型拒绝；修正为合法数值转换，无生产更改。
- corrected：13调用中12通过；S2 resume/custom 的测试字符串替换未匹配缩进，未生成非法样本。修正样本生成后单独复验 S2，9/9通过，原非法类型拒绝断言保留。
- 定向技术：27/27通过，0失败/跳过（InputBinding8、InputPlugin6、PluginRegistry9、Server4）。覆盖九种固定目录、Schema、JSON/YAML/PAAS多入口、JSONB往返、未知/完整类名拒绝及HTTP投影；不是额外UC验收。证据 `/tmp/flow-input-results/technical/`，日志 `/tmp/flow-input-technical.log`。
- 模块完整回归：Core449项，447通过、2失败、0跳过；Server18项全通过。合计467项，465通过、2失败、0跳过。UC12主类13/13通过。耗时14分9秒；证据 `/tmp/flow-input-results/modules/{core,server}/test/xml/`，日志 `/tmp/flow-input-modules.log`。
- 根 `test --continue -x :core:test -x :server:test` 补齐其余模块：通过，gen:test为NO-SOURCE。为避免重复已完整执行的14分钟测试，明确排除了已经执行过的Core/Server；完整项目覆盖由本条和前条共同构成，不宣称整个项目全绿。日志 `/tmp/flow-input-root.log`。
- 两个失败方法独立复验：2项仍失败；未修改断言。命令为下方命令，日志 `/tmp/flow-input-failures-recheck.log`，XML `/tmp/flow-input-results/failures-recheck/core/test/xml/`。

```bash
FLOW_TEST_RUN=failures-recheck ./gradlew -I /tmp/flow-input-test.init.gradle :core:test \
  --tests '*StateTest.rejectsInvalidHistoryAndIllegalGenericTransitions' \
  --tests '*Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance'
```

## 失败与反例

本次 UC-12 未发现稳定生产反例；两次测试样本错误均已纠正并真实复验。其他 UC 的既有反例不以本次 UC-12 通过宣告解决，全量发现将单列。

模块回归的两项失败均单独复现：

- State 技术测试：`State.created().withState(PAUSED)`；原预期抛 `WorkflowException`，实际未抛。`State.java` 和 `StateTest.java` 本次未修改，此前报告也已记录。本次不定义新的状态迁移规则。
- CE-01（已有 UC04 S8 反例）：发布 Pause→ThrowingTask→never-run，启动后通过公开查询定位Pause，恢复输入为 `decision=APPROVED`，下游抛 `uc04-downstream-failure`。预期拒绝后根/Pause保持提交前PAUSED且没有部分推进，实际根FAILED（`expected: <PAUSED> but was: <FAILED>`）。断言中止，用户取消收尾未执行；fixture清理不等于该场景业务闭环。本次仅复验，未修改UC或生产实现，不以UC12通过宣告该反例解决。

## 数量汇总

UC场景5：PASS5、FAIL0、NOT_COVERED0、BLOCKED0。S2九参数不计为九个UC场景。

## 测试结论

UC-12 各场景已有完整映射和通过证据；完整回归仍有上述两项既有失败，不能宣称全仓通过。

## 未解决事项

上述State技术问题及CE-01保持未解决；本次目标UC12没有覆盖、环境或实现阻塞。未进行浏览器/真实外部UI、负载或集群故障验证；Server证据是Controller/视图及模块测试，不冒称浏览器验收。

最终资源审计：executions=0、active_executions=0、task_runs=0、active_task_runs=0、paused_task_runs=0；两条Executor订阅均backlog=0、unacked=0、consumers=0、publishers=0。证据 `/tmp/flow-input-resource-audit.json`、`/tmp/flow-input-command-stats.json`、`/tmp/flow-input-event-stats.json`。随后删除本次专属两个容器及匿名卷，容器列表无本次名称；最终本次容器0、仍运行流程0、等待任务0。失败场景的物理清理不替代用户收尾。临时 `/tmp/flow-input-env.sh` 已删除；其他旧环境脚本未修改。复验需按harness重新创建独占环境并提供环境变量。

报告滚动保留：本次单UC不生成BATCH；新增本报告，删除最旧 `UC-12-2026-09-10-1208.md`，清理1份，UC12保留3份。
