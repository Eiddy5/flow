# UC-02 用户启动、查询与取消 Flow 测试报告

## 基本信息

- UC 文档：`docs/uc/flow/UC-02 用户启动、查询与取消 Flow.md`
- 主测试类：`core/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java`
- 执行时间：2026-08-26 20:56 +08:00
- 代码版本：`3ebbb77`，工作区 `dirty`
- 环境：Java 21、Micronaut 真实装配、PostgreSQL 17.10；临时库
  `flow_fix_20260826_194256`
- 用户入口：`ExecutionService` 启动、查询和取消能力

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束证据 |
| --- | --- | --- | --- |
| S1 | 主测试类未提供 `s1...` 方法 | NOT_COVERED | 缺少 UC-02 主测试类中的完整启动并完成证据。补充类有相关运行回归，但不替代主测试映射。 |
| S2 | 主测试类未提供 `s2...` 方法 | NOT_COVERED | 缺少版本发布前后分别运行的 UC 主场景证据。 |
| S3 | 主测试类未提供 `s3...` 方法 | NOT_COVERED | 缺少精确取消目标流程及对照流程的 UC 主场景证据。 |
| S4 | 主测试类未提供 `s4...` 方法 | NOT_COVERED | 缺少不可用或不可见 Flow 启动拒绝的 UC 主场景证据。 |
| S5 | 主测试类未提供 `s5...` 方法 | NOT_COVERED | 缺少不存在、越权和终态取消的 UC 主场景证据。 |
| S6 | 主测试类未提供 `s6...` 方法 | NOT_COVERED | 缺少等待期间发布新版本并分别完成的 UC 主场景证据。 |
| S7 | 主测试类未提供 `s7...` 方法 | NOT_COVERED | 缺少完成与取消竞争的 UC 主测试方法；不能用技术回归替代。 |
| S8 | `s8TwoExecutionsOfOneFlowKeepIndependentCompleteHistories` | PASS | 同一 Flow 的两个运行实例独立完成，历史和终态互不污染。 |
| S9 | `s9FreshServerStartsThePersistedPublishedFlowInOrder` | PASS | 重新创建运行上下文后仍能启动持久化正式 Flow，并按顺序完成。 |

## 执行结果

- 目标命令：`JAVA_HOME=$(/usr/libexec/java_home -v 21) FLOW_POSTGRES_TEST_URL=jdbc:postgresql://localhost:5432/flow_fix_20260826_194256 FLOW_POSTGRES_TEST_USER=flow FLOW_POSTGRES_TEST_PASSWORD=flow FLOW_POSTGRES_TEST_CLEANUP=true ./gradlew :core:test --tests 'org.cses.flow.core.services.executions.Uc02ExecutionLifecycleTest' --rerun-tasks --max-workers=1 --no-daemon --console=plain`
- 结果：2 tests，全部通过，`BUILD SUCCESSFUL`。
- 补充技术回归：`ExecutionAsyncStartIntegrationTest` 2/2、`ExecutionPauseLifecycleIntegrationTest` 7/7 通过；因不是 UC-02 主测试类，不计入上述 UC 覆盖数。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 9 | 2 | 0 | 7 | 0 |

## 结论

部分通过。S8、S9 已通过真实数据库验证；S1～S7 需要补齐或迁移到唯一 UC-02 主测试类后，才能判定 UC-02 全部通过。

