# UC-03 自动 Task 顺序执行复验报告

## 基本信息

- UC 文档：`docs/uc/flow/UC-03 用户运行自动流程.md`
- 主测试类：`server/src/test/java/org/cses/flow/core/services/executions/Uc03AutomaticTaskFlowTest.java`
- 验证领域：`flow`
- 测试类型：工厂与兼容层清理后的完整回归
- 执行时间：`2026-07-29 12:00-12:04 CST`
- 代码版本：`b665c29`，工作区 `dirty`
- 上次报告：`docs/test-reports/flow/UC-03-2026-07-28-2322.md`

## 测试范围

验证 AUTO Task 改为静态 `create(...)` / `rehydrate(...)` 后的顺序执行、实例
隔离、失败回滚、跨进程持久化恢复和终态保护。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 | `s1CompletesAutomaticTasksInDefinitionOrder` | PASS | 三个 AUTO 按定义顺序各执行一次并最终 COMPLETED |
| S2 | `s2KeepsTwoExecutionsAndTheirTaskRunsIsolated` | PASS | 两个实例及其 TaskRun 身份和历史完全隔离 |
| S3 | `s3ExplicitWorkerFailureStopsFollowingTasks` | PASS | 明确失败使 TaskRun 和 Execution FAILED，后续 Task 未启动 |
| S4 | `s4UnhandledWorkerFailureRollsBackCreatedExecution` | PASS | 异常传播且启动事务回滚，没有残留 Execution |
| S5 | `s5FreshServerStartsPersistedDefinitionByTaskIdValue` | PASS | 新上下文通过静态恢复得到相同 taskId 顺序并完成 |
| S6 | `s6CancelCompletedExecutionIsRejectedWithoutMutation` | PASS | COMPLETED 后取消被拒绝，状态、版本和历史不变 |

## 执行命令与结果

- `./gradlew build`（配置真实 PostgreSQL 和自动清理）：78 tests，
  0 failures，0 errors，0 skipped；本主测试类 6 tests 全部通过。

## 失败与反例

无失败；无新增或复验 CE。

## 数量汇总

- PASS：13
- FAIL：0
- NOT_COVERED：0
- BLOCKED：0

## 测试结论

PASS。AUTO Task 不再依赖 Factory，运行与恢复行为保持一致。

## 未解决事项

无。
