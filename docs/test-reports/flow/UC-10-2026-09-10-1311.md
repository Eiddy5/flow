# UC-10 用户处理包含嵌套编排的审批 Flow测试报告

## 基本信息

时间：2026-09-10 13:11 Asia/Shanghai。代码：`01cc2d400b3597ab49e2cddb6a81865fa2dfb1e7 dirty`。

UC：[UC-10](</System/Volumes/Data/workspace/java/flow/docs/uc/flow/UC-10 用户处理包含嵌套编排的审批 Flow.md>)，ACCEPTED；唯一主类：[Uc10NestedApprovalFlowTest.java](</System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java>)。

## 测试范围

本轮验证 Execution 自带 Long lock、无显式作用域的 CAS 仓储改造；业务 UC 预期不变。真实 Micronaut、PostgreSQL、Pulsar，经公开 FlowService/ExecutionService 操作并重新查询。保留工作区的 Task 输出和 SubFlow 等并行改动；Test Agent 未修改生产、UC 或测试源码。

覆盖 13 个场景、13 次独立 JUnit 调用。以 315 个源码、资源、UC 和构建文件 SHA-256 记录执行边界：独立类阶段出现 OrchestrationTask.java、SubFlow.java、TaskPluginTestSupport.java、SubFlowTest.java 的并行修改；UC 文档和主类未变。Core 前→完整回归前→完整回归后这 315 项均一致，最终完整回归覆盖当前版本；不把早期独立执行称为不可变全仓快照。清单保存在本轮证据目录的 source-hashes-*.json。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 多级串签审批通过 | `s1SequentialNestedApprovalsCompleteOnlyAfterAllApprovals` | PASS | 已核对：初审通过前不会出现财务或法务审批；财务审批完成前不会出现法务审批；全部审批完成前不会执行记录和结束动作；财务审批、法务审批、记录和结束动作均只执行一次。结束：Flow 已以审批通过的完成终态结束，没有未处理审批。 |
| S2 初审驳回后短路结束 | `s2RejectedInitialApprovalShortCircuitsNestedApprovalBranch` | PASS | 已核对：初审驳回后只出现驳回处理并结束；财务审批、法务审批和通过后的记录动作均不出现，Flow 不继续后续审批。结束：Flow 已以驳回处理的完成终态结束，没有未处理审批或通过分支步骤。 |
| S3 并行会签后进入终审 | `s3ParallelNestedApprovalsJoinBeforeFinalApproval` | PASS | 已核对：两个会签分支可以分别推进，但每个分支的复核审批都只能在本分支负责人审批完成后出现；两个分支的负责人审批和复核审批全部完成前不会出现总负责人终审；总负责人终审只出现并执行一次，之后才执行一次汇总和结束步骤。结束：Flow 已完成，没有未处理的会签审批或终审。 |
| S4 并行会签中途取消 | `s4CancelingParallelNestedApprovalsPreventsRemainingAndFollowingSteps` | PASS | 已核对：取消后未完成的会签审批不能继续；总负责人终审以及后续汇总、发布和通知都不出现，已完成的部分审批不会重新触发其他分支或后续步骤。结束：Flow 已取消，没有仍可处理的会签审批或后续步骤。 |
| S5 补充材料后重新审批 | `s5ConditionalNestedReviewLoopReachesFinalApprovalAfterSupplement` | PASS | 已核对：每一轮都先完成补充材料再进行材料复核；第一轮需要补充时才创建下一轮；第二轮材料复核通过前不会出现最终审批；最终审批只在循环满足通过条件后出现并执行一次。结束：Flow 已以审批通过的完成终态结束，没有未完成轮次或审批。 |
| S6 补充材料超过最大次数 | `s6ConditionalNestedReviewLoopFailsAtMaximumWithoutFinalApproval` | PASS | 已核对：每一轮都先完成补充材料再进行材料复核；达到最大轮数后 Flow 明确失败，不创建下一轮；最终审批以及后续步骤都不出现。结束：Flow 已处于失败终态，没有未完成轮次、最终审批或后续步骤。 |
| S7 同一条件分支内退回上一审批 | `s7SameConditionalBranchRewindsAndRejectsOldSource` | PASS | 已核对：每次退回后原实例停止，由新实例整体接替；条件内的复核可以退回同分支初审，自动通知不阻断退回；不同编号的新实例中，初审、通知和复核按顺序重新执行，未命中分支不执行；使用原实例编号恢复旧复核和再次退回的请求均被明确拒绝，原实例保持停止，新初审及新实例当前路径不受拒绝请求影响，也不额外产生退回实例；新实例后续记录只在新的复核通过后执行一次，并使用重新审批的结果；原初审、通知、复核及退回原因的真实历史仍可查询。结束：新实例已完成，退回前后的真实历史保留；本次退回的原实例均已停止，没有等待恢复的 Pause。 |
| S8 条件分支首个审批退回分流前审批 | `s8FirstConditionalApprovalRewindsBeforeTheSplit` | PASS | 已核对：每次退回后原实例停止，由新实例整体接替；分支首个审批可以跨过条件和通知退回分流前初审，退回后的重做和后续流程由不同编号的新实例推进；目标初审之前的有效结果保留且步骤不重复，重新初审完成前分支审批不可处理；申请金额不变时仍只进入原命中分支，未命中分支的审批不执行；新实例后续记录使用重新审批结果并只在分支完成后执行一次，原有审批和通知的真实记录仍可查询。结束：新实例已完成，退回前后的真实审批历史可查询；本次退回的原实例均已停止，没有等待恢复的 Pause。 |
| S9 条件汇合后退回实际命中分支的最后审批 | `s9AfterConditionalMergeRewindsOnlyTheActuallyExecutedBranch` | PASS | 已核对：每次退回后原实例停止，由新实例整体接替；每次退回都产生不同编号的新实例，由它重新进入本次实际命中分支并继续后续流程，汇合与通知不阻断退回；未命中分支没有可供退回的审批运行记录，也不会因退回产生审批；两条退回路径互不影响；目标分支之前的有效结果保留且步骤不重复，新分支审批完成后才出现新的终审，各新实例最终结果使用各自重新审批的结果，原有真实历史保留。结束：两个新实例均已完成，各自只执行一次最终记录；本次退回的原实例均已停止，没有等待恢复的 Pause。 |
| S10 深层条件内审批退回外层分支审批 | `s10DeepConditionalApprovalRewindsToOuterApproval` | PASS | 已核对：每次退回后原实例停止，由新实例整体接替；退回不因条件嵌套层数受阻，不同编号的新实例从外层审批重新推进，外层审批完成后才按原条件路径再次到达深层审批；各层未命中分支不执行，目标之前的有效结果保留且步骤不重复；新实例的深层新审批完成前外层记录和最终结束步骤不执行，完成后各执行一次并采用最新审批结果；原有层级及真实路径仍可从历史查询。结束：新实例已完成且所有条件层级均已结束；本次退回的原实例均已停止，没有等待恢复的 Pause。 |
| S11 并行分支内退回保留其他分支的未完成进度 | `s11BranchRewindKeepsTheOtherPausedBranchValid` | PASS | 已核对：原实例整体停止，由不同编号的新实例整体接替，只重做第一分支初审至复核片段并继续后续流程；第二分支准备步骤及审批的原运行编号、已完成结果和当前进度保留，查询明确区分沿用记录和第一分支重新执行的新记录，第二分支已完成动作不重做；使用旧实例编号提交第二分支结果被明确拒绝，新实例的审批仍可处理、进度和结果不变，原实例保持停止；改用新实例编号与第二分支原审批运行编号后可以完成审批，结果只推进新实例，原实例历史中的审批不会被改为此次完成；第一分支重新审批完成前不能提前汇总；新实例最终汇总使用第一分支最新结果和第二分支有效结果，且只执行一次。结束：新实例已完成，第二分支的有效进度及真实历史保留；本次退回的原实例均已停止，没有等待恢复的 Pause。 |
| S12 并行汇合后只退回用户选定的上一分支审批 | `s12AfterParallelMergeRewindsOnlyTheSelectedPredecessor` | PASS | 已核对：每次退回后原实例停止，由新实例整体接替；退回使用用户选定的第一分支审批，不因第二分支更晚完成而改选目标；不同编号的新实例只重新审批一个选定分支，第二分支已完成记录和结果仍有效且不重复，其历史不会被当作新实例重新执行的审批；第一分支新审批完成前不会出现新的终审；新实例终审可以结合第一分支新结果和第二分支原结果正常完成，最终记录只执行一次。结束：新实例已完成，没有第二分支重复审批；本次退回的原实例均已停止，没有等待恢复的 Pause。 |
| S13 跨条件退回后再次退回使用当前有效审批 | `s13RepeatedConditionalRewindUsesOnlyTheCurrentEffectiveApprovals` | PASS | 已核对：每次退回后原实例停止，由新实例整体接替；已被替代的旧分支审批不能作为新实例本次退回目标，拒绝后当前终审和有效路径不变，也不额外创建运行实例；第二次合法退回使用当前终审和当前已完成的分支审批，再产生不同编号的新实例，只重做当前有效路径的相应片段并继续后续流程；两次退回的原因及各次真实审批历史均保留，已被替代的旧审批不能重新成为新实例的可处理步骤；最新实例最终记录使用最新一轮审批结果，未命中分支不执行。结束：最新实例已完成，历史保留两次退回及各实例真实记录；本次退回的原实例均已停止，没有等待恢复的 Pause。 |

## 执行命令与结果

实际 JDK 25.0.4（不证明 Java 21 兼容）；测试库 `jdbc:postgresql://127.0.0.1:64991/flow_inline_lock`、用户 flow；Pulsar `pulsar://127.0.0.1:56659`、admin `http://127.0.0.1:65093`。密码由环境注入，`FLOW_POSTGRES_TEST_CLEANUP=true`。运行时数据源和 Broker 同步指向这些专属资源，Gradle/AOT 保留默认数据源和消费者禁用守卫，UC 夹具显式启用真实消费者；最终 Core Test 的两项误继承环境已按下文隔离移除。未访问默认 5432 业务库。

实际执行：

```bash
FLOW_POSTGRES_TEST_PASSWORD='<scratch 密码>' bash core/build/cas-inline-lock-uc-pvi5G0/run.sh uc10 :core:test --rerun --tests '*Uc10NestedApprovalFlowTest'
```

runner 使用 `./gradlew -I /private/tmp/flow-cas-refactor.4sTnkB/isolated-tests.gradle --offline --max-workers=1 --console=plain --no-build-cache`。init 只隔离 binary/XML/HTML 与标准流展示，不排除编译源码、不替换实现或断言。该类 XML：[独立执行](</System/Volumes/Data/workspace/java/flow/core/build/cas-inline-lock-uc-pvi5G0/uc10/core/test/xml/TEST-org.cses.flow.core.services.executions.Uc10NestedApprovalFlowTest.xml>)；日志：`/System/Volumes/Data/workspace/java/flow/core/build/cas-inline-lock-uc-pvi5G0/uc10/run.log`。

| 阶段 | 本类实际结果 |
| --- | --- |
| 独立执行 | 13 tests / 13 passed / 0 failed / 0 skipped |
| Core 模块 `:core:test --rerun` | 13 tests / 13 passed / 0 failed / 0 skipped |
| 完整 `test --rerun build --continue` + probe | 13 tests / 13 passed / 0 failed / 0 skipped |

本 UC 独立类无失败方法，无需单独重跑。 原 Core 全局环境导致另 8 项数据源/队列技术失败（原总数 419 passed / 16 failed / 1 skipped）。经安全核对，仅在独占 `test-environment.gradle` 为 Core Test 移除误继承的 `DATASOURCES_DEFAULT_ENABLED` 和 `PULSAR_CONSUMER_ENABLED`，保留测试原有 properties/mock 契约及 AOT/Server 守卫。两类 10/10 复核及最终完整测试通过，原失败日志仍保留。

完整阶段实际命令：`FLOW_POSTGRES_TEST_PASSWORD='<scratch 密码>' bash core/build/cas-inline-lock-uc-pvi5G0/run.sh full -I core/build/cas-inline-lock-uc-pvi5G0/test-environment.gradle -I docs/harness/flow-operation-cost/probe.gradle test --rerun build --continue :server:flowQueryProbe`。本类最终 XML：`/System/Volumes/Data/workspace/java/flow/core/build/cas-inline-lock-uc-pvi5G0/full/core/test/xml/TEST-org.cses.flow.core.services.executions.Uc10NestedApprovalFlowTest.xml`。

## 失败与反例

本 UC 没有独立类执行失败。以下是本批仍未解决的公共反例，完整保留，不算作本 UC 新失败：

CE-01（历史未解决，本轮再次复现）：UC-04 S8 通过公开 Service 发布 Pause → ThrowingTask → never-run，启动并查询 Pause，提交 `{"decision":"APPROVED"}`；后置任务抛出 `uc04-downstream-failure`。UC 要求异常明确返回、根与目标 Pause 保持提交前 PAUSED、不产生部分后续结果，再由用户取消完成收尾。实际根进入 FAILED，`Uc04PauseFlowTest.java:273` 报 `expected: <PAUSED> but was: <FAILED>`，断言中断，未执行用户取消。独立整类、同一方法重跑、Core 和完整回归均保留该反例；没有修改 UC、业务代码、超时或断言。影响是失败恢复场景不满足 UC 的原子用户体验；不能用 fixture 物理清理代替用户闭环。此方法同预期/实际已有 2026-09-09 记录，未经版本二分，不据此宣称本次 CAS 改造造成或修复了问题。

复现（需按本报告环境启动独占依赖）：

```bash
FLOW_POSTGRES_TEST_PASSWORD='<scratch 密码>' bash core/build/cas-inline-lock-uc-pvi5G0/run.sh retry-uc04-s8 :core:test --rerun --tests '*Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance'
```

已执行目录有覆盖保护；再次复现请使用新的 stage 名。单独重跑 XML：`/System/Volumes/Data/workspace/java/flow/core/build/cas-inline-lock-uc-pvi5G0/retry-uc04-s8/core/test/xml/TEST-org.cses.flow.core.services.executions.Uc04PauseFlowTest.xml`。

## 数量汇总

场景：PASS 13 / FAIL 0 / NOT_COVERED 0 / BLOCKED 0。JUnit 调用和场景数量分开统计；参数化调用不算新增场景，测试通过不补足缺失断言。

## 测试结论

本 UC 当前全部场景的已映射断言通过。 最终完整回归 Core 436 tests / 427 passed / 8 failed / 1 skipped，Server 20/20 passed；合计 456 tests / 447 passed / 8 failed / 1 skipped。整个 `test build --continue` 因 Core 失败 exit 1，不宣称全仓通过；正常 query probe 完成，AOT/打包任务本轮为 UP-TO-DATE，不宣称重新生成产物。

## 未解决事项

本 UC 无新增观察缺口。
2026-09-10 13:10–13:11 只读审计本轮专属 flow_inline_lock：flows=0、flow_tasks=0、executions=0、task_runs=0、inherited_task_runs=0；活动 Execution=0、PAUSED Execution=0、活动 TaskRun=0、等待 TaskRun=0。专属 Pulsar 的 flow-executor-command 和 flow-executor-event 均为 backlog=0、unacked=0、consumers=0、publishers=0。审计证据为 `/System/Volumes/Data/workspace/java/flow/core/build/cas-inline-lock-uc-pvi5G0/resource-audit.json`，SQL 为同目录 resource-audit.sql。审计时保留 2 个本轮专属容器和 1 个 PG 匿名卷。随后主 Agent 按核对的完整 ID 删除 flow-cas-lock-20260910、flow-cas-pulsar-20260910 及专属 PG 卷，并确认容器/卷列表无匹配项；最终本轮容器=0、卷=0，临时测试数据销毁，日志/XML/报告及备份保留。未操作其他容器或默认开发库。复现须重新建立独占依赖，原测试端点已关闭。 物理测试清理不替代失败/未覆盖场景的用户收尾。未执行真实外部 UI、负载或 Broker 集群故障验收。

滚动保留：本组新增 1 份、清理 1 份、保留 3 份；本批共新增 13 份、清理 12 份，所有规范命名组均不超过 3 份。本组被删旧报告 `UC-10-2026-09-09-1629.md` 已预先复制并验证 SHA-256，可从 `/System/Volumes/Data/workspace/java/flow/core/build/cas-inline-lock-uc-pvi5G0/retained-old-reports/UC-10-2026-09-09-1629.md` 恢复。 UC-03、非规范秒级旧名及其他领域未改动。
