# UC-04 外部任务等待与持久化恢复复验报告

## 基本信息

- UC：`docs/uc/flow/UC-04 外部任务等待与恢复.md`
- 主测试类：`server/src/test/java/org/cses/flow/core/services/externaltasks/Uc04ExternalTaskResumeTest.java`
- 时间：2026-07-28 18:50-19:02 CST
- 代码版本：`b665c29`，工作区 `dirty`
- 上次报告：`UC-04-2026-07-28-1835.md`

## 新增验证

S9 拆为三个独立测试用例：第一个在启动 server 中把 RUNNING Execution、
RUNNING TaskRun 和 WAITING ExternalTask 提交至 PostgreSQL，然后彻底关闭该
ApplicationContext；第二个作为独立外部触发 server，仅凭租户关联标识从数据库
恢复并调用公开 `ExternalTaskService`；第三个 ApplicationContext 再次从数据库
查询并确认同一 Execution 已进入 COMPLETED。

该流程不复用启动 server 的 Service、Repository、领域对象或内存状态。测试类
显式编排三个阶段，阶段之间的领域状态只通过 PostgreSQL 传递。

## 执行结果

- S9 首次执行按预期得到 RED：期望 COMPLETED，实际 RUNNING。
- UC-04：11 tests，23 条 PASS 全部通过。
- UC-01～UC-07：50 tests，全部通过。
- 完整 Gradle 测试：72 tests，0 failures，0 errors，0 skipped。

## 数据库终态

- Execution：COMPLETED 123、CANCELED 18、FAILED 3、RUNNING 0。
- WAITING ExternalTask：0。
- 正常流程最终为 COMPLETED；取消和故障场景保留其真实 CANCELED/FAILED 终态。

## 结论

PASS；外部恢复已证明是跨 server 生命周期、基于 PostgreSQL 的持久化恢复，UC
结束后没有遗留 RUNNING 或 WAITING。
