# Flow 跨条件退回批量测试报告

## 基本信息与范围

- 日期：2026-09-07，Asia/Shanghai；报告时间 13:58。
- 指定 UC 范围：UC-10 S1–S13、UC-04 S1–S11；其他 UC 仅随必要模块回归执行，不建立额外场景验收范围。
- 版本：含用户已有未提交修改的 `dirty` 快照 `/private/tmp/cses-cross-condition-rewind/flow`，无 `.git`。原始 `flow-baseline.zip` 独立解压至 `flow-baseline-check`，未覆盖或还原实现目录。
- 环境：Java 25.0.4、Gradle 9.5.1、真实 Micronaut/PostgreSQL；凭据从本机配置注入，不写入报告。默认 Java 21 不能解析当前要求 JVM 25 的既有依赖，使用已安装 Java 25 完成验证，没有修改构建配置。

## UC 汇总

| UC | 主测试类 | 场景数 | PASS | FAIL | NOT_COVERED | BLOCKED |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| UC-10 | `core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java` | 13 | 13 | 0 | 0 | 0 |
| UC-04 | `core/src/test/java/org/cses/flow/core/services/executions/Uc04PauseFlowTest.java` | 11 | 10 | 1 | 0 | 0 |
| 合计 | 两个独立主测试类 | 24 | 23 | 1 | 0 | 0 |

独立报告为 [UC-10](UC-10-2026-09-07-1358.md) 和 [UC-04](UC-04-2026-09-07-1358.md)，包含全部场景及结束状态证据。

## 执行命令与结果

Gradle 命令均使用 Java 25，并追加 `--console=plain --offline`；真实数据库设置由进程环境提供，常规测试 `FLOW_POSTGRES_TEST_CLEANUP=true`。

| 执行 | 结果 |
| --- | --- |
| `./gradlew :core:test --tests '*Uc10NestedApprovalFlowTest'` | 13/13 PASS，0 skipped，76.287 秒。 |
| UC-04 整类加 Execution、Generation、codec、ExecutorService、Resume 聚焦 | 55 tests，54 PASS，UC-04 S8 1 FAIL。 |
| UC-04 S8 单方法复验 | 当前 1 FAIL（5.145 秒）；原始 ZIP 1 FAIL（3.725 秒），相同状态差异。 |
| `./gradlew :core:test` | 392 tests，382 PASS、9 FAIL、1 SKIPPED，6 分 27 秒；UC-10 13/13 PASS（80.233 秒），UC-04 10 PASS、S8 FAIL。 |
| 原始 ZIP 对另外 8 个失败方法逐个精确筛选的一次命令 | 8/8 FAIL，23 秒；失败类型/位置与当前一致，日志列表的实际内容存在异步差异。 |
| 最终源码 core 聚焦与 server/gen 模块命令 | core 59/59 PASS，0 skipped；gen:test NO-SOURCE；server 测试编译 BLOCKED，生产编译通过。 |
| 原始 ZIP `bash ./gradlew :server:compileTestJava` | 同样缺失 `org.paas.session.Device`，确认测试编译阻塞早于本次。 |

最终源码命令：

```bash
./gradlew :core:test \
  --tests '*Uc10NestedApprovalFlowTest' \
  --tests '*Uc04PauseFlowTest.s10*' --tests '*Uc04PauseFlowTest.s11*' \
  --tests '*ExecutionTest' --tests '*GenerationTest' \
  --tests '*GenerationJsonCodecTest' --tests '*ExecutorServiceTest' \
  --tests '*ExecutionServiceResumeTest' \
  :server:test :gen:test --continue --console=plain --offline
```

59 条构成为 UC-10 13、UC-04 回退 2、Execution 13、Generation 4、codec 2、ExecutorService 23、Resume 2。UC-10 最终 64.698 秒，S9/S12 直接调用公开 `previousCompletedTaskRuns`，分别断言实际条件分支的唯一候选和真正并行汇合的两个候选 ID；S11 验证无关旧轮次等待 occurrence 保留且可以恢复。测试覆盖最终的祖先容器守卫变更。

按任务安排，使用已完成 core 全量、最终改动相关复验和其他模块执行覆盖项目范围，不再次重复 6 分钟的全量 core。server 未进入测试执行，不能将整条命令的失败退出码解释为 core 行为失败或将项目记为全量通过。

## 失败、基线对照和未解决事项

### CE-01：UC-04 S8 后置异常没有回到原 Pause

公开恢复提交 APPROVED 后，真实下游 Task 抛出 `uc04-downstream-failure`。UC 预期回到 PAUSED 且无部分推进，实际公开查询为 FAILED。当前整类、单方法、模块和独立原始 ZIP 均失败；此项此前已记录，现完整继承，继续阻止 UC-04 整体验收。没有修改 UC、生产代码或降低断言。

其余模块失败按精确方法在原始 ZIP 复验，均早于本次；只作回归观察，不扩大本轮 UC 范围：

| 方法 | 当前结果 | 原始 ZIP 结果 |
| --- | --- | --- |
| `CoreArchitectureStandardTest.executorAndWorkerAreTopLevelPeersOfCore` | 目录位置断言 expected true / false | 同一失败 |
| `CoreArchitectureStandardTest.taskCapabilitiesKeepOrchestrationOutOfWorkers` | 缺少旧 `ExecutorEventHandler.java` 路径 | 同一路径缺失 |
| `StateTest.rejectsInvalidHistoryAndIllegalGenericTransitions` | CREATED→PAUSED 未按既有测试预期抛异常 | 同一失败 |
| `NestedPauseResumeIntegrationTest.reversedDeepBranchResumeOrderKeepsEquivalentHistory` | integrate-results inputs 期望包含前置 outputs，实际 `{}` | 同一失败 |
| `NestedPauseResumeIntegrationTest.resumesTwoLevelNestedFlow` | 同一 inputs 断言失败 | 同一失败 |
| `Uc08DynamicLogFlowTest.s1RendersPrecedingResultOnceAndContinuesTheFlow` | 动态日志列表多出 `test step` | 同一额外日志 |
| `LogFlowIntegrationTest.rendersPrecedingOutputThroughTheExecutorAndWorkerChain` | 期望动态日志，实际 `[]` | 实际 `[test step]`，仍失败；列表受异步影响，不能宣称实际值完全相同 |
| `PostgresRepositoryIntegrationTest.schemaUsesAggregateAndPluginTypes` | schema 列计数 expected 4 / actual 0 | 同一失败 |

- 全量另有 1 SKIPPED：`FlowDatabaseIntegrationTest.usesOnlyTheManuallyProvisionedNamedFlowDataSource` 要求额外 `FLOW_POSTGRES_DEFAULT_TEST_URL`，本机未配置。未把缺少第二数据源记为通过，也未扩展环境。
- server 测试编译缺少 `org.paas.session.Device`，位于既有 `AdminSessionArgumentBinderTest.java:6`；原始 ZIP 同样失败。server 行为测试 BLOCKED，未修改该测试或依赖版本。
- UC-10 S10 曾有一次等待深层审批超过既有 10 秒阈值；未改变阈值或专门修改调度。单方法及随后三次完整类执行均通过，保留为未稳定复现的异步观察，不伪称已修复。
- 基线实验仅证明上述差异早于本次跨条件回退修改，不推断具体 ADR 或某次未提交修改是原因。

## 活动资源与证据

- 所有已启动 Gradle 命令结束，测试 ApplicationContext 已关闭。
- 诊断保留的 14 个 UC-10 租户先核对终态再精确清理；常规测试通过 fixture 清理自身租户。
- 最终从本轮指定日志中取得 92 个 UC-04/UC-10 随机租户，逐项限定查询：Execution=0、TaskRun=0、Flow=0、FlowTask=0、执行队列消息=0，运行实例=0、等待任务=0。没有对其他租户进行清理，既有 PostgreSQL 容器保留。
- 本地证据：`core/build/uc10-completion-results.xml`、`core/build/core-regression-results/`、`core/build/core-regression-summary.json`、`core/build/core-baseline-failures-summary.json`、`core/build/final-regression-results/`、`core/build/final-cross-condition-regression.log`、`core/build/cross-condition-final-resource-summary.json`。原始 ZIP 日志位于独立检查目录 `core/build/`。

## 报告滚动保留

本次写入 UC-10、UC-04、BATCH 各 1 份。UC-10 保留 2 份，删除 0；UC-04 保留 3 份，删除最旧 `UC-04-2026-08-28-190527.md`；BATCH 保留 3 份，删除最旧 `BATCH-2026-08-27-1554.md`。其他报告组未改动，全部组不超过 3 份，仍未解决 CE-01 已完整带入。

## 测试结论

UC-10 13/13 PASS；本次改动最终相关 59/59 PASS。UC-04 因既有 CE-01 仍为 FAIL。core 全量 9 个失败均在原始 ZIP 复现，另有 1 项额外数据源测试跳过；server 测试被既有依赖编译问题阻塞，gen 无测试源码。未提交代码、变更包版本或部署。
