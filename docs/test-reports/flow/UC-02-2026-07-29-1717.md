# UC-02 用户启动、查询与取消 Flow 测试报告

## 基本信息

- UC 文档：`docs/uc/flow/UC-02 用户启动、查询与取消 Flow.md`
- 主测试类：`server/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java`
- 执行时间：`2026-07-29 17:07-17:16 CST`
- 代码版本：`4cce0b07544366cf9d74efb7534f46fb56048baa`，工作区 `dirty`
- 环境：Java 21、PostgreSQL 17.10、真实 Micronaut 装配；使用公开 `FlowService`、`ExecutionService`、`ExternalTaskService`
- 主测试类是否修改：是；补齐当前 S8、S9，并将场景验证收敛到公开查询路径

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 | `s1StartsAndCompletesLatestDeployedFlow` | PASS | 只创建一个绑定版本 1 的 Execution，外派完成后终态 COMPLETED、待办为空 |
| S2 | `s2NewStartsUseLatestReversionWithoutMovingExistingExecution` | PASS | 旧实例保持版本 1，新实例绑定版本 2，历史与待办隔离并分别完成 |
| S3 | `s3CancelOnlyTargetsOneExecutionAndItsWaitingTrigger` | PASS | 仅目标实例及其待办取消，对照实例继续完成 |
| S4 | `s4RejectsDraftClosedMissingAndCrossTenantFlows` | PASS | 草稿、关闭、不存在、跨租户 Flow 启动均被拒绝且未创建 Execution/待办 |
| S5 | `s5RejectsMissingCrossTenantAndTerminalCancellation` | PASS | 不存在、越权及终态取消均被拒绝，目标历史不变，合法对照完成 |
| S6 | `s6RunningExecutionKeepsItsBoundReversionAfterDeployment` | PASS | 等待中的版本 1 不切换，新实例按版本 2 运行，两者均完成 |
| S7 | `s7CompleteAndCancelRaceCommitsOneConsistentTerminalState` | PASS | 完成/取消竞争仅一方生效，最终仅有一个一致终态且无待办 |
| S8 | `s8TwoExecutionsOfOneFlowKeepIndependentCompleteHistories` | PASS | 两个实例编号不同，各自输入、输出、步骤历史完整且均无活动步骤 |
| S9 | `s9FreshServerStartsThePersistedPublishedFlowInOrder` | PASS | 重启后的新上下文公开查询到已发布 Flow，启动后按原顺序完整执行且无活动步骤 |

## 执行命令与结果

- `./gradlew :server:test --tests '*Uc02ExecutionLifecycleTest' --console=plain`：9 tests，0 failures，`BUILD SUCCESSFUL`。
- `./gradlew :server:test`、`./gradlew test`、`./gradlew build`：各执行到 95 tests，唯一失败均为 UC-04 S3；UC-02 的 9 个测试全部通过。

## 失败与反例

无 UC-02 失败或反例。

## 数量汇总

- PASS：9
- FAIL：0
- NOT_COVERED：0
- BLOCKED：0

## 测试结论

PASS。UC-02 的 9 个已接受场景均满足预期。

## 未解决事项

无。
