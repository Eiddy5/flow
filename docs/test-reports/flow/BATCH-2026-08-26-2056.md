# Flow 全场景功能测试批次报告

## 基本信息

- 执行时间：2026-08-26 20:56 +08:00
- 代码版本：`3ebbb77`，工作区 `dirty`
- 数据库：`flow-postgres`，PostgreSQL 17.10
- 测试库：`flow_fix_20260826_194256`（临时库，未修改原 `flow` 库）
- Schema 来源：当前 `gen/sql/flow/001_create_flow_tables.sql` 及表级 SQL
- 对象来源：使用临时库重新执行 `:gen:generateJooq`，生产代码按生成的
  `org.flow.gen.flow` 对象对齐

## 本轮修复

- 修正 `FlowEntry` 使用已生成的 `version` 字段，并将数据库 bigint 时间直接映射为
  Java `long`；同步修正 `ExecutionEntry`、`TaskRunEntry`、`PostgresAudit` 和队列时间映射。
- 将 `ExecutionPostgresRepository.findById` 的 Execution 与 TaskRun 恢复改为单条
  `LEFT JOIN` 查询，避免 PostgreSQL READ COMMITTED 下两次查询拼出不一致快照，修复
  `CREATED Execution` 搭配已提交 TaskRun 的重建异常。
- 按当前领域契约恢复 `Task` 的 `Identified` 能力，收紧 Flow、Execution、BaseDomain
  和 Audited 的无参构造器可见性；同步构造路径断言。
- 修正 `LoopUntil` 内置 YAML 示例的输出声明和不支持的 `required` 字段。
- 恢复 `FlowJooqCondition` 及队列/Executor 的具名 Flow JOOQ 条件，保证未配置
  `datasources.flow` 时不提前创建 Flow 数据库 Bean。
- 恢复 `AdminSessionArgumentBinder` 的默认启用、可配置关闭的 Micronaut 注解。
- 测试清理同时删除 command/event 两类队列消息，避免前一场景遗留消息污染后续场景。

## 执行命令

1. 重新生成数据库对象：

   `JAVA_HOME=$(/usr/libexec/java_home -v 21) FLOW_JOOQ_JDBC_URL=jdbc:postgresql://localhost:5432/flow_fix_20260826_194256 FLOW_JOOQ_JDBC_USER=flow FLOW_JOOQ_JDBC_PASSWORD=flow ./gradlew :gen:generateJooq --rerun-tasks --no-daemon --console=plain`

2. 逐个执行 UC 主测试类：UC-01、UC-02、UC-03、UC-06、UC-07、UC-08、UC-09，均使用
   `FLOW_POSTGRES_TEST_URL=jdbc:postgresql://localhost:5432/flow_fix_20260826_194256`、
   Java 21、`--max-workers=1` 和 `FLOW_POSTGRES_TEST_CLEANUP=true`。

3. 模块回归：

   `./gradlew :core:test --rerun-tasks --max-workers=1 --no-daemon --console=plain`

4. 根工程回归（另提供空 `postgres` 库给双数据源装配测试）：

   `FLOW_POSTGRES_TEST_URL=jdbc:postgresql://localhost:5432/flow_fix_20260826_194256 FLOW_POSTGRES_DEFAULT_TEST_URL=jdbc:postgresql://localhost:5432/postgres ./gradlew test --rerun-tasks --max-workers=1 --no-daemon --console=plain`

## UC 主场景汇总

| UC | 主测试类 | 场景数 | PASS | FAIL | NOT_COVERED | BLOCKED |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| UC-01 | `Uc01FlowLifecycleTest` | 13 | 13 | 0 | 0 | 0 |
| UC-02 | `Uc02ExecutionLifecycleTest` | 9 | 2 | 0 | 7 | 0 |
| UC-03 | `Uc03AutomaticTaskFlowTest` | 3 | 3 | 0 | 0 | 0 |
| UC-04 | 未建立 | 9 | 0 | 0 | 9 | 0 |
| UC-05 | 未建立 | 6 | 0 | 0 | 6 | 0 |
| UC-06 | `Uc06ConditionalRouteTest` | 7 | 1 | 0 | 6 | 0 |
| UC-07 | `Uc07NestedTaskFlowTest` | 7 | 1 | 0 | 6 | 0 |
| UC-08 | `Uc08DynamicLogFlowTest` | 3 | 3 | 0 | 0 | 0 |
| UC-09 | `Uc09LoopOrchestrationTest` | 6 | 6 | 0 | 0 | 0 |
| 合计 |  | 63 | 29 | 0 | 34 | 0 |

`NOT_COVERED` 是按 UC 测试规范对“缺少唯一主测试类或缺少带场景编号的主方法”的判定，不表示实现链路已经失败。现有补充技术回归全部通过，但不能替代 UC 主测试证据：`ExecutionAsyncStartIntegrationTest` 2/2、`ExecutionPauseLifecycleIntegrationTest` 7/7、`ExecutionResumeIntegrationTest` 9/9、`ConditionalRouteResumeIntegrationTest` 6/6、`ParallelPauseResumeIntegrationTest` 6/6、`NestedPauseResumeIntegrationTest` 6/6、`LogFlowIntegrationTest` 1/1。

## 模块和根工程结果

- `:core:test`：325 tests，0 failures，0 errors；单独运行时因未提供默认空库变量有 1 个双数据源测试被跳过。
- 根工程 `./gradlew test`：334 tests，334 passed，0 failures，0 errors，0 skipped。
- `:gen:test`：无测试。
- `:server:test`：9 tests，全部通过。
- 失败类复验：`DomainCapabilitiesTest`、`BuiltInPluginExamplesTest`、
  `AdminSessionArgumentBinderTest` 均已单独通过。

## 活动资源

临时库测试收尾后只读查询结果：

- `flows=0`
- `executions=0`
- `task_runs=0`
- `queues=0`

没有遗留运行流程、TaskRun 或待投递队列消息。

## 日志与非失败告警

- 测试环境未创建 `/var/log/cses/cses.log`，Logback 输出文件 appender 建目录告警；不影响测试结果。
- 数据源条件测试故意构造了 `127.0.0.1:1` 的无效连接，产生连接拒绝日志后按预期完成。
- 队列失败重试测试故意抛出业务异常并验证消息保留，错误日志属于测试预期。

## 结论

数据库生成对象、Core、Server 和根工程回归均已通过，已发现的稳定实现问题已修复并复验。按 UC 证据规范，当前 63 个确认场景中 29 个有唯一主测试映射并通过，34 个仍需补齐/迁移主测试映射；因此 Flow 代码回归通过，但 UC 全场景验收尚未达到 63/63 PASS。

## 后续事项

- 为 UC-04、UC-05 建立唯一主测试类，将现有补充恢复/并行测试按 S1～S9、S1～S6 映射并补齐公开用户路径断言。
- 将 UC-02 的 S1～S7、UC-06 的 S1～S3/S5～S7、UC-07 的 S1～S2/S4～S7 迁移或补齐到对应主测试类，再重新生成独立报告。
