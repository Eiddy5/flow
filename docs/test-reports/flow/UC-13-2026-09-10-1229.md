# UC-13 用户调用并追溯子流程测试报告

## 基本信息

- UC：`docs/uc/flow/UC-13 用户调用并追溯子流程.md`，ACCEPTED。
- 唯一主测试类：`core/src/test/java/org/cses/flow/core/services/executions/Uc13SubFlowTest.java`（新增）。
- 补充技术集成：`core/src/test/java/org/cses/flow/core/services/executions/SubFlowExecutionIntegrationTest.java`（新增）；不增加 UC 场景。
- 日期：2026-09-10，Asia/Shanghai；代码：`01cc2d4` + dirty 工作区。本轮并行存在队列、CAS、Output 改动，不把全仓失败自动归因于 SubFlow。
- Test Agent 没有修改 UC 或生产代码；生产修复由主 Agent 完成。

## 测试范围与环境

真实 Micronaut ApplicationContext、生产 PostgreSQL Repository、PAAS Pulsar Client/Consumer、Executor 与 Worker。所有场景通过 FlowService 发布/删除，通过 ExecutionService 启动、查询、lineage、恢复。日志断言捕获真实 Log Worker 的格式化消息，没有替换被测执行链路。

- JDK 25.0.4（当前依赖所需；不构成 Java 21 构建证明）。
- 独占 PostgreSQL 17 容器 `flow-subflow-postgres-0910`，`127.0.0.1:55172/flow_subflow_uc13`，完整基线含 `parent_task_run_id`。
- 独占 Pulsar 4.0.7 容器 `flow-subflow-broker-0910`，Broker `56651`、Admin `58082`，healthcheck 返回 `ok`。
- 开启 `FLOW_POSTGRES_TEST_CLEANUP=true`，只清理本轮 fixture 租户及本轮专属 Broker backlog，不访问业务库。
- 重试、重启、退回接替、取消传播不属于本 UC 范围。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期与结束证据 |
| --- | --- | --- | --- |
| S1 子 Pause 返回 | `s1ResumesChildPauseAndReturnsResultToWaitingParent` | PASS | 标准 inputs 将 request-s1 传入子运行；父子编号不同，子 parentId/originId 均为父；查询子 Pause 时父 call RUNNING、after 无记录；仅恢复子 Pause 后父子 SUCCESS；返回 executionId 和 wait.approved=true；日志包含 child received request-s1、child result request-s1 true、parent result request-s1 true；重查来源保持；2 个运行、活动/等待均 0。 |
| S2 三层调用 | `s2TracesNestedCallsAndConsumesResultsAtEachLevel` | PASS | 3 个不同编号；中层直接/根来源为顶层，底层直接来源中层、根来源顶层；lineage 重查 3 层输入 request-s2；底层 produce.result=ready，逐层调用的 executionId 对应正确；leaf/middle/root 日志证明逐层消费；3 个运行均 SUCCESS，活动/等待 0。 |
| S3 非法输入 | `s3RejectsMissingAndInvalidChildInputs`，参数 false/true | PASS | 两个独立 fixture 分别缺少 INTEGER 必填 request、传入 not-a-number；父合法启动后 call 与父 FAILED，错误包含 request；无子运行、无成功输出、无 after 记录；每分支 1 个失败运行，活动/等待 0。 |
| S4 删除目标 | `s4FailsWhenReferencedChildDefinitionWasDeleted` | PASS | 发布后通过 FlowService 删除子定义，latestFlow 查询为空；父启动后 call 与父 FAILED，错误包含子 key；无子运行/成功输出/after；1 个失败运行，活动/等待 0。 |
| S5 子业务失败 | `s5PropagatesChildBusinessFailureWithoutSuccessResult` | PASS | 合法 request-s5 启动独立子运行，真实 Log Worker 读取缺失路径失败；子错误包含 missing，保留正确来源和输入；父 call 与父 FAILED，不返回成功输出且 after 不执行；2 个运行均 FAILED，活动/等待 0。 |

S1/S2 的输出值、日志消费与来源均在公开查询或真实日志中重新观察；没有用数据库内部事实替代 UC 断言。每次场景的零活动资源断言发生在 fixture 物理清理之前。

## 执行命令与结果

命令统一使用 Gradle Wrapper，环境保存在 `/tmp/subflow-test-env.sh`（只包含专属测试端点）；临时 init 仅隔离 Test 报告目录及指定最后复验范围，没有修改全局构建设置。

| 阶段 | 命令 | 结果/证据 |
| --- | --- | --- |
| 首次编译 | `./gradlew -I /tmp/subflow-test.init.gradle :core:test --tests '*Uc13SubFlowTest'` | 缺 junit-jupiter-params；主 Agent 补齐测试依赖后解决。 |
| 首次环境尝试 | `:core:test --tests '*SubFlowTest'` | 共享测试 PG 被其他任务删除，连接拒绝；技术 2 通过，UC 无业务执行。随后新建完全独占 PG，全部复验。 |
| 首次 UC | `:core:test --tests '*SubFlowTest'` | 8/8 通过，UC 6 + 技术 2；`/tmp/subflow-uc13-evidence/core/test/xml/`。 |
| 受影响链路 | `:core:test --tests '*SubFlowExecutionIntegrationTest' --tests '*ExecutionTest' --tests '*ExecutionEntryTest' --tests '*ExecutorEventMessageHandlerTest' --tests '*PostgresRepositoryIntegrationTest'` | 30/30 通过：Execution14、Entry3、Event5、Repository7、Parallel1；`/tmp/subflow-targeted-evidence/core/test/xml/`。 |
| 全 Core 首轮 | `./gradlew -I /tmp/subflow-test.init.gradle :core:test` | 436 项：425 通过、10 失败、1 跳过；耗时13m36s；`/tmp/subflow-core-evidence/core/test/xml/`。 |
| 最终改动与失败复验 + 其余模块构建 | `./gradlew -I /tmp/subflow-final.init.gradle build --continue` | Core 筛选61项：46通过、15失败；Server完整20/20通过，其余无测试源码；`build/subflow-final/`。其中7项失败为中间版本新引入的 FlowReference 递归校验问题，下述修复已复验。 |
| 修复后 SubFlow | `./gradlew -I /tmp/subflow-test.init.gradle :core:test --tests '*SubFlowTest' --tests '*SubFlowExecutionIntegrationTest'` | 9/9通过；UC6、SubFlow技术2、Parallel1；`/tmp/subflow-fixed-evidence/core/test/xml/`。 |
| 最终源码打包 | `./gradlew -I /tmp/subflow-package.init.gradle build --continue` | 成功，Core9/9、Server20/20；`build/subflow-package/`，详见下方最终打包结果。 |

主 Agent 为避免重复未变更的已通过长套，要求最后两次 build 的 Core 仅运行变更/失败定向范围。以上组合是“全 Core 首轮 + 最终改动/失败定向复验 + 其他模块完整构建”，不是一次最终全仓无筛选测试，也不表示全仓验收通过。

## 失败与反例

### 本轮 SubFlow 问题：已修复并复验

- CE-UC13-01：中间版本给普通 FlowReference 加 `@Valid` 后，所有父流程发布被拒绝：`Cannot validate ...FlowReference. No bean introspection present`。S1～S5 六执行分支及 Parallel 共7项复现。主 Agent 移除多余递归校验，保留 JsonCreator 的必填/正版本校验；保持同一 YAML 和全部断言，最终9项通过。
- 新增引用 record 位于 Domain class-only 区域，以及嵌套 Output record 与旧同名类触发规范检查；主 Agent 改为普通值对象/输出类。`javaSourcesFollowRecordAndTechnicalIdRules` 在第二阶段构建中通过。

### 全仓回归仍未解决：8 项在最终失败复验中再次出现

| 失败测试 | 预期 / 实际 |
| --- | --- |
| `CoreArchitectureStandardTest.executorAndWorkerAreTopLevelPeersOfCore` | 所需文件组合应存在 / 旧 Handler 路径断言 false。 |
| `CoreArchitectureStandardTest.taskCapabilitiesKeepOrchestrationOutOfWorkers` | 应读取 Handler / `handlers/ExecutorEventHandler.java` 不存在。 |
| `StateTest.rejectsInvalidHistoryAndIllegalGenericTransitions` | CREATED→PAUSED 应抛异常 / 未抛异常。 |
| `NestedPauseResumeIntegrationTest.reversedDeepBranchResumeOrderKeepsEquivalentHistory` | `{outputs={receive-request={}, prepare-release={}}}` / `{}`。 |
| `NestedPauseResumeIntegrationTest.resumesTwoLevelNestedFlow` | 同上。 |
| `NestedPauseResumeIntegrationTest.resumesLongFlowAcrossThreeDeepWaitingPoints` | 同上。 |
| `Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance` | 下游失败后 PAUSED / FAILED。 |
| `LogFlowIntegrationTest.rendersPrecedingOutputThroughTheExecutorAndWorkerChain` | 仅“处理结果：ready” / 另包含前后 test step 日志。 |

这些用例不属于 UC13 场景；本报告不修改其预期、不把它们算作 UC13 反例，也不宣称已证明全部属于旧基线。主 Agent 另外核对了两个 Handler 路径及未变动 State 文件；并行 Output 等改动仍需所属任务处理。

首轮另有 `ExecutionResumeIntegrationTest.repeatedResumeDoesNotChangeCompletedData` 的 STRING Input 类型解析失败（仅注册到 BUSINESS_CODE），在筛选复验中未再现。没有改该测试或相应生产实现；不能将单次复验通过解释为该全套顺序问题已修复。

## 数量汇总与结论

- UC 场景：5；PASS 5、FAIL 0、NOT_COVERED 0、BLOCKED 0。
- UC 可执行测试：6（S3 两个参数），最终全部通过，未禁用。
- 最终 SubFlow 定向：9/9；其中 Parallel 同批两个子调用 + 一个 Worker 真实成功，各 child.parentTaskRunId 与输出 executionId 对应不同调用节点。
- UC13 场景合计创建10个独立运行；每个场景结束活动流程0、活动步骤0、等待 Pause 0。
- 首轮已解决的环境问题不影响最终 UC 结论；全仓仍有上述8项稳定回归失败及1项未复现的顺序相关问题。
- 报告滚动保留：本组原无报告，本次新增1、清理0、保留1；未触及其他 UC 或 BATCH 报告。

## 最终打包与资源清理

最终 `build --continue` 成功（2m12s），同一最终源码下 Core SubFlow 定向9/9、Server完整20/20通过，Core/Server产物完成打包。最终 XML 位于 `build/subflow-package/core/test/xml/` 与 `build/subflow-package/server/test/xml/`；日志 `/tmp/subflow-package.log`。该构建的 Core 有明确筛选，不消除上文全 Core 回归的8项未解决失败。

2026-09-10 12:31 收尾只读查询专属数据库，活动 Execution 0、PAUSED TaskRun 0；该基础设施计数与 UC 场景结束前的公开查询断言分开记录。随后删除且仅删除 `flow-subflow-postgres-0910`、`flow-subflow-broker-0910` 两个本任务容器及其匿名测试卷。

复现需要按 `docs/harness/postgresql-repositories.md`、`docs/harness/pulsar-queues.md` 重新启动独占依赖，并在空数据库执行 `gen/sql/flow/001_create_flow_tables.sql`。本次基线新增 `parent_task_run_id`，`IF NOT EXISTS` 不升级旧表；必须重建隔离测试库，禁止据此清空业务库。环境脚本中的端点当前不再运行。
