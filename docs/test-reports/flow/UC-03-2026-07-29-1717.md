# UC-03 用户运行自动流程测试报告

## 基本信息

- UC 文档：`docs/uc/flow/UC-03 用户运行自动流程.md`
- 主测试类：`server/src/test/java/org/cses/flow/core/services/executions/Uc03AutomaticTaskFlowTest.java`
- 执行时间：`2026-07-29 17:07-17:16 CST`
- 代码版本：`4cce0b07544366cf9d74efb7534f46fb56048baa`，工作区 `dirty`
- 环境：Java 21、PostgreSQL 17.10、真实 Micronaut 装配和真实 Execution 持久化/状态推进
- 主测试类是否修改：是；重写为当前 S1-S3 三个场景
- 测试装配：部署定义仍使用真实 `type: AUTO`；仅在 `flow.uc03.auto-handler=true` 时，以 `Uc03AutomaticTaskHandler` 条件替换默认 AUTO handler，提供确定的成功、明确失败和抛异常行为；未替换 Flow、Execution、仓储或状态推进

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 | `s1TargetAutomaticTaskConsumesInputAndPassesOutputOnce` | PASS | 前置输入传入目标 AUTO，目标和后置观察各执行一次，输出逐级可见，Execution COMPLETED 且无活动步骤 |
| S2 | `s2ExplicitAutomaticTaskFailureStopsFollowingTask` | PASS | 前置完成、目标 AUTO 恰好失败一次、后置未执行，Execution FAILED 且无活动资源 |
| S3 | `s3UnexpectedAutomaticTaskExceptionRollsBackTheStart` | PASS | 异常返回用户，公开 Execution 数量与启动前相同，没有部分流程、任务结果或活动步骤 |

## 执行命令与结果

- `./gradlew :server:test --tests '*Uc03AutomaticTaskFlowTest' --console=plain`：3 tests，0 failures，`BUILD SUCCESSFUL`。
- 模块、完整测试和构建回归均只失败于 UC-04 S3；UC-03 三个测试全部通过。

## 失败与反例

无 UC-03 失败或反例。

## 数量汇总

- PASS：3
- FAIL：0
- NOT_COVERED：0
- BLOCKED：0

## 测试结论

PASS。成功、明确失败和意外异常三条 AUTO 用户路径均满足预期。

## 未解决事项

无。
