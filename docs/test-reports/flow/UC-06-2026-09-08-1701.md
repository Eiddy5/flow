# UC-06 用户使用 Pause 结果选择 Flow 路径测试报告

## 基本信息

- UC：[UC-06 用户使用 Pause 结果选择 Flow 路径](../../uc/flow/UC-06%20%E7%94%A8%E6%88%B7%E4%BD%BF%E7%94%A8%20Pause%20%E7%BB%93%E6%9E%9C%E9%80%89%E6%8B%A9%20Flow%20%E8%B7%AF%E5%BE%84.md)，S1–S7；索引状态 ACCEPTED。
- 唯一主测试类：[Uc06ConditionalRouteTest](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc06ConditionalRouteTest.java)；本轮没有修改主测试类或 UC。
- 日期：2026-09-08，Asia/Shanghai；本类 JUnit 开始时间 `2026-09-08T08:39:13.141Z`（UTC），用时 14.464 秒。
- 代码版本：`64d381a425143f1a1d71ec8ee200a5880c3f5c26 + dirty`；包含本次最终作用域 CAS / `lock` 改造。
- 环境：Java 25.0.4、Gradle wrapper 9.5.1、真实 Micronaut 装配和 PostgreSQL 17.10；唯一数据库 `127.0.0.1:64628/flow`，容器 `flow-cas-20260908`。数据库测试未指向或清理默认 5432 实例。Java 21 没有本轮通过证据。

## 测试范围

在独立发布的 Flow 中公开提交 Pause 结果，检查批准、拒绝、无匹配、缺字段、大小写与跨实例路径选择。

所有现有场景均有唯一、未禁用的测试方法；没有新增、失效或错误归属方法。逐预期审计与方法运行结果分别统计，覆盖缺口保留为 NOT_COVERED，不以绿色方法替代完整验收。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 用户使用 Pause 结果选择批准路径 | `s1PauseApprovalSelectsOnlyApprovalPath`（源码 26 行） | PASS | JUnit PASS。APPROVED 只执行批准处理；两个判断各有 CREATED→RUNNING→SUCCESS/SKIPPED 历史，拒绝子步骤不存在，SUCCESS。 |
| S2 用户使用 Pause 结果选择拒绝路径 | `s2PauseRejectionSelectsOnlyRejectionPath`（源码 61 行） | PASS | JUnit PASS。REJECTED 只执行拒绝处理；批准判断 SKIPPED、拒绝判断 SUCCESS，批准子步骤不存在，SUCCESS。 |
| S3 用户提交没有匹配路径的 Pause 结果 | `s3UnknownPauseResultSelectsNoPathAndStillCompletes`（源码 96 行） | PASS | JUnit PASS。UNKNOWN 时两个判断均 RUNNING→SKIPPED，两个候选处理均无记录，没有默认路径，SUCCESS。 |
| S4 用户发布包含非法条件的 Flow | `s4InvalidRouteConditionIsRejectedBeforePublishing`（源码 128 行） | PASS | JUnit PASS。非法条件发布明确拒绝，最新正式版本为空、公开实例查询为空，无等待。 |
| S5 用户提交 Pause 结果时缺少决定字段 | `s5MissingDecisionSelectsNoPathWithoutDefaulting`（源码 151 行） | PASS | JUnit PASS。缺少 decision 时输出空；两个判断均 SKIPPED、两个子步骤不存在，SUCCESS。 |
| S6 用户改变 Pause 决定值的大小写 | `s6LowercaseDecisionDoesNotMatchUppercasePaths`（源码 184 行） | PASS | JUnit PASS。小写 approved 原样保留、不隐式转大写；两个判断 SKIPPED、候选处理均不存在，SUCCESS。 |
| S7 用户选择另一个 Flow 运行实例中的 Pause | `s7SelectingOneInstanceDoesNotAffectTheOtherInstance`（源码 218 行） | PASS | JUnit PASS。先恢复第二实例只运行批准路径，第一仍等待；再恢复第一走拒绝路径，两个未命中判断均 SKIPPED，最终无等待。 |

## 执行命令与结果

使用以下配置（口令仅通过环境注入，不写入报告）：

```bash
export JAVA_HOME=/Users/cses-7/Library/Java/JavaVirtualMachines/ms-25.0.4/Contents/Home
export FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:64628/flow
export FLOW_POSTGRES_TEST_USER=flow
export FLOW_POSTGRES_TEST_CLEANUP=true
# FLOW_POSTGRES_TEST_PASSWORD 由本地测试环境提供
```

1. `./gradlew :core:testClasses --console=plain --offline --max-workers=1`：exit 0，编译成功。
2. `./gradlew :core:test --tests org.cses.flow.core.services.executions.Uc06ConditionalRouteTest --console=plain --offline --max-workers=1`：exit 0，7 tests，7 PASS、0 FAIL、0 SKIPPED，14.464 秒。

随后执行 `./gradlew :core:test --console=plain --offline --max-workers=1`：exit 1，399 tests，389 PASS、9 FAIL、1 SKIPPED，4 分 15 秒。最后以共享任务图执行 `./gradlew test build --continue --console=plain --offline --max-workers=1`：exit 1，7 分 28 秒；core 再次 399 tests，389 PASS、9 FAIL、1 SKIPPED，本 UC 方法结果与定向一致。

Server AOT、打包及 assemble 成功，但 `server:compileTestJava` 在 `AdminSessionArgumentBinderTest.java:6` 因缺少 `org.paas.session.Device` 失败，Server 测试未运行；整体 build 不通过。已有 Server XML 时间为 2026-09-01，不计入本轮。gen:test 为 NO-SOURCE。core 的唯一 SKIPPED 是缺少第二个数据源配置的 FlowDatabaseIntegrationTest，不是本 UC 场景。

原始证据目录：`core/build/cas-uc-regression-e6X33w`。本类快照 `Uc06ConditionalRouteTest-results/TEST-org.cses.flow.core.services.executions.Uc06ConditionalRouteTest.xml`；模块快照 `core-results/`；最终快照 `full-test-build-results/` 与 `full-test-build.log`。本报告已写明关键结果，不依赖更早报告。

## 失败与反例

本 UC 没有观察到稳定违反用户预期的生产反例；本轮存在的证据缺口（如有）不分配 CE。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 7 | 7 | 0 | 0 | 0 |

JUnit 主类：7 方法，7 PASS、0 FAIL、0 SKIPPED；这不是上表的 UC 验收数量。

## 活动资源与滚动保留

2026-09-08 17:02:02 Asia/Shanghai，在完整 test/build 结束后，只读查询隔离容器 `flow-cas-20260908`：`company_id LIKE 'uc-%'` 的 Flow、FlowTask、Execution、TaskRun、Queue 均为 0；整个隔离库 Execution=0（活动=0）、TaskRun=0（PAUSED=0）、Queue=0。所有 UC fixture 已关闭并按所属租户清理，未删除其他数据库数据或停止容器。

物理清理的 0 不替代逐场景的业务结束断言；表中 NOT_COVERED 仍保持未覆盖。上述为 Test Agent 核对时状态；随后主 Agent 已删除本次隔离容器及专属测试卷，默认开发库保留。

本批新增 9 份 UC 报告与 1 份 BATCH。按 `^(UC-\d+|BATCH)-\d{4}-\d{2}-\d{2}-\d{4}\.md$` 识别并滚动保留最新 3 份；只删除 `UC-04-2026-08-31-1253.md`，未解决 CE-01 已完整带入本批。清理后各活跃 UC 组及 BATCH 各 3 份，历史 UC-03 组 2 份，共 32 份匹配报告；另保留 9 份非匹配旧 HHmmss 文件，总计 41 份。

## 测试结论

UC-06 本次逐场景 PASS；结论仅覆盖本 UC 的公开 Service 路径，不等于整个项目 build 或 HTTP 验收通过。

## 未解决事项

- 本 UC 无新增覆盖缺口。

- 项目回归仍有既有架构、State、NestedPause 和 Log 测试失败；未改写测试以隐藏失败。完整 test/build 命令退出 1：core 既有 9 个失败；server 测试编译在 AdminSessionArgumentBinderTest.java:6 缺少 org.paas.session.Device，server 行为测试未执行。FlowDatabaseIntegrationTest 因未提供独立的 FLOW_POSTGRES_DEFAULT_TEST_URL 而 SKIPPED；gen:test 为 NO-SOURCE。
- 主 Agent 本轮限定既有缺口只报告、不扩展修复；Test Agent 仅新增报告，复用原有真实数据库测试资源。
