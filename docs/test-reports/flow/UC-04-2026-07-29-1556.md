# UC-04 用户处理外派任务并恢复流程测试报告

## 基本信息

- UC 文档：`docs/uc/flow/UC-04 用户处理外派任务并恢复流程.md`
- 主测试类：`server/src/test/java/org/cses/flow/core/services/externaltasks/Uc04ExternalTaskResumeTest.java`
- 验证领域：`flow`
- 测试类型：领域模型校正后的完整回归
- 执行时间：`2026-07-29 15:55-16:03 CST`
- 代码版本：`4cce0b0`，工作区 `dirty`
- 上次报告：`docs/test-reports/flow/UC-04-2026-07-29-1146.md`

## 测试范围

验证 PAUSE Task 的公开查询、输入校验、完成、幂等保护、租户隔离、取消保护、
事务回滚，以及 `Input` / `Output` 数据和 TaskRun 在重启后的恢复。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 | `s1UserQueriesAndCompletesSingleExternalTask` | PASS | 用户查询到唯一 WAITING TaskRun，提交结果后流程 COMPLETED 且无待办 |
| S2 | `s2CompletesAndResumesWithoutRunningPauseWorkerAgain` | PASS | 完成只恢复所属流程，PAUSE Worker 不重复运行，后续 Task 正常完成 |
| S3 | `s3RejectsInvalidInputWithoutChangingWaitingState` | PASS | 非法输入被拒绝，等待任务及 Execution 不变；正确输入随后完成流程 |
| S4 | `s4RejectsRepeatedCompletionWithoutChangingCompletedData` | PASS | 重复完成被拒绝，已提交 Output、终态和锁版本不变 |
| S5 | `s5RejectsCrossTenantReadAndCompletion` | PASS | 跨租户查询与完成均不可见、无副作用，由所属用户完成收尾 |
| S6 | `s6ExternalTaskIdSelectsOnlyItsOwningExecution` | PASS | 用户选择的外派任务只恢复所属 Execution，另一流程保持等待直至被单独处理 |
| S7 | `s7RejectsCompletionAfterExecutionCancellation` | PASS | 取消关闭对应等待任务，之后完成被拒绝且取消终态一致 |
| S8 | `s8ResumeFailureRollsBackExternalTaskAndExecution` | PASS | 后续处理异常使外派结果与 Execution 更新一起回滚，不产生部分提交 |
| S9 | `s9aStarterPersistsWaitingExecutionAndStops`、`s9bExternalTriggerRestoresWaitingExecutionFromPostgres`、`s9cFreshServerVerifiesPersistedCompletion` | PASS | 三个独立上下文通过 PostgreSQL 恢复 WAITING 流程，提交结果后最终 COMPLETED 且无待办 |

## 执行命令与结果

- UC-01～UC-07 主测试类联合执行：56 tests，0 failures，0 errors，
  0 skipped；本主测试类 11 tests、9 个场景全部通过。
- `./gradlew test`（配置真实 PostgreSQL 测试连接）：98 tests，
  0 failures，0 errors，0 skipped。

## 失败与反例

无失败；无新增或复验反例。

## 数量汇总

- PASS：9
- FAIL：0
- NOT_COVERED：0
- BLOCKED：0

## 测试结论

PASS。PAUSE Task 的公开处理、数据对象和跨进程恢复满足 UC-04 全部场景。

## 未解决事项

无。
