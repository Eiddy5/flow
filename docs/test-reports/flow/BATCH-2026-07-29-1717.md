# Flow ACCEPTED UC 全量测试批次报告

## 基本信息

- 范围：`docs/uc/flow/README.md` 中 UC-01～UC-07 全部 `ACCEPTED` UC
- 执行时间：`2026-07-29 17:07-17:16 CST`
- 代码版本：`4cce0b07544366cf9d74efb7534f46fb56048baa`，工作区 `dirty`
- 环境：Java 21、PostgreSQL 17.10、真实 Micronaut 装配、真实 PostgreSQL 持久化
- 用户入口：当前无 Flow HTTP Controller，按项目 harness 使用公开 Service
- 场景映射：7 个 UC、7 个唯一主测试类、53 个 S 场景、53 个 `@Test` 方法，一一对应，无禁用测试
- 基线说明：`15:56` 历史报告的场景/方法映射已与当前 UC 漂移，未作为本轮通过依据；本轮以当前 ACCEPTED UC 和未被 Test Agent 修改的生产代码为验收基线

## UC 汇总

| UC | 主测试类 | 场景 | PASS | FAIL | NOT_COVERED | BLOCKED | 结论 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| UC-01 | `Uc01FlowLifecycleTest` | 13 | 13 | 0 | 0 | 0 | PASS |
| UC-02 | `Uc02ExecutionLifecycleTest` | 9 | 9 | 0 | 0 | 0 | PASS |
| UC-03 | `Uc03AutomaticTaskFlowTest` | 3 | 3 | 0 | 0 | 0 | PASS |
| UC-04 | `Uc04ExternalTaskResumeTest` | 9 | 8 | 1 | 0 | 0 | FAIL |
| UC-05 | `Uc05ParallelTaskJoinTest` | 6 | 6 | 0 | 0 | 0 | PASS |
| UC-06 | `Uc06ConditionalRouteTest` | 7 | 7 | 0 | 0 | 0 | PASS |
| UC-07 | `Uc07NestedTaskFlowTest` | 6 | 6 | 0 | 0 | 0 | PASS |
| 合计 | 7 个主测试类 | 53 | 52 | 1 | 0 | 0 | FAIL |

## 执行命令与结果

所有 Gradle 命令均配置：

`JAVA_HOME=$(/usr/libexec/java_home -v 21) FLOW_POSTGRES_TEST_URL=jdbc:postgresql://localhost:5432/flow FLOW_POSTGRES_TEST_USER=flow FLOW_POSTGRES_TEST_PASSWORD=flow`

- `./gradlew :server:testClasses --console=plain`：`BUILD SUCCESSFUL`。
- 逐主测试类执行顺序：UC-01 → UC-02 → UC-03 → UC-04 → UC-06 → UC-05 → UC-07。
- 逐类结果：13/13、9/9、3/3、8/9、7/7、6/6、6/6。
- UC-04 S3 单方法复跑：1 test，1 failed，稳定复现相同反例。
- `./gradlew :server:test --console=plain`：95 tests completed，1 failed。
- `./gradlew test --console=plain`：95 tests completed，1 failed。
- `./gradlew build --console=plain`：编译、打包和 AOT 阶段完成，`:server:test` 为 95 tests completed，1 failed，构建因此失败。
- 三个回归入口的唯一失败均为 UC-04 S3；`PostgresRepositoryIntegrationTest` 在全量回归中通过。

## 环境准备

测试库最初落后于当前代码，缺少 `flow_drafts.raw`。应用
`gen/sql/production-release/flow/2026-07-29/001_align_flow_source_and_reversion.sql`
时，旧 `ck_flows_status` 阻止 `CLOSE` 更新为 `CLOSED`；仅在本地测试库移除旧约束并转换旧状态后，完整重跑迁移成功。该问题不计为 UC 场景失败。

## 失败与反例

- `CE-UC04-01`：声明 `decision` 输出的外派任务接受空结果并完成；预期拒绝并保持 WAITING，实际 assignment 与 Execution 均变为 COMPLETED。详见 `UC-04-2026-07-29-1717.md`。

## 活动资源

最终直接查询真实 PostgreSQL：

- `executions.status = 'RUNNING'`：`0`
- `assignment.status = 'WAITING'`：`0`

本轮没有遗留运行流程或待处理外派任务。

## 测试代码调整

- 对齐 UC-02 S8/S9、UC-03 S1-S3、UC-04 S3/S9，并强化 UC-05～UC-07 的场景结束断言。
- 增加条件启用的 UC-03 AUTO 测试 handler；真实 Flow、Execution、状态推进和 PostgreSQL 未替换。
- Test Agent 未修改生产代码，也未修改 UC 预期。

## 总体结论

FAIL。53 个场景中 52 个通过，1 个稳定实现失败；没有覆盖缺口、环境阻塞或活动资源遗留。

## 未解决事项

1. 修复并复验 `CE-UC04-01`。
2. 单独复核生产升级脚本对旧 `ck_flows_status` 的迁移顺序。
