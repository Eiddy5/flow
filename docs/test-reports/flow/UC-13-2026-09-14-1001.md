# UC-13 用户调用并追溯子流程测试报告

## 基本信息

- 时间：2026-09-14 10:01，Asia/Shanghai。
- 版本：`e6317f2` + dirty 工作区，保留其他任务的 VoidOutput、ExecutionService、Rewind、Log、WorkerDispatcher 等改动。
- UC：`docs/uc/flow/UC-13 用户调用并追溯子流程.md`，ACCEPTED；UC 未修改。
- 唯一主类：`core/src/test/java/org/cses/flow/core/services/executions/Uc13SubFlowTest.java`。
- 辅助技术集成：`core/src/test/java/org/cses/flow/core/services/executions/SubFlowExecutionIntegrationTest.java`。
- Test Agent 对两类只将3处 YAML 的 `flow: {key, version}` 改为顶层 `flowKey`、`flowVersion`；未调整任何场景断言，未修改生产代码。

## 测试范围

本轮验证 SubFlow 自身持有目标 key/version、OrchestrationTask 删除具体子流程业务引用之后，定义、Schema、持久化属性、Executor 和真实父子运行链路仍可运行。使用真实 Micronaut ApplicationContext、生产 PostgreSQL Repository、PAAS Pulsar、Executor、Worker，经公开 FlowService/ExecutionService 发布、启动、查询、恢复和追溯。

按照本轮主任务明确范围只执行专项及相关 Executor 回归，没有重复全 Core、Server 或完整 build。专项45项成功不等于全仓测试通过，也不补足历史UC观察缺口。

环境全部独占：

- JDK 25.0.4，Gradle Wrapper；不声称 Java 21 构建通过。
- PostgreSQL 17 容器 `flow-subflow-pg-0914`：`127.0.0.1:55174/flow_subflow_flat`，在空库执行 `gen/sql/flow/001_create_flow_tables.sql`。
- Pulsar 4.0.7 容器 `flow-subflow-broker-0914`：Broker `56654`、Admin `58084`；`brokers healthcheck` 返回 `ok`。
- 随机测试租户，`FLOW_POSTGRES_TEST_CLEANUP=true`；没有使用其他任务容器或业务数据库。

## 场景验证

| 场景 | 测试方法 | 状态 | 本轮结果及证据边界 |
| --- | --- | --- | --- |
| S1 子 Pause 恢复并返回 | `s1ResumesChildPauseAndReturnsResultToWaitingParent` | NOT_COVERED | 测试执行成功：扁平配置发布；request-s1传入子运行；父子不同编号与正确来源；子Pause期间父call为RUNNING且after未执行；恢复子Pause后approved=true返回，父后置消费，父子SUCCESS、活动/等待0。仍未证明子流程把输入本身随处理结果一起返回：父后置日志读取自身inputs.request。保留上轮覆盖缺口。 |
| S2 嵌套来源 | `s2TracesNestedCallsAndConsumesResultsAtEachLevel` | NOT_COVERED | 测试执行成功：3层编号独立，直接与根来源正确；嵌套executionId及ready结果逐层消费；lineage重查、3个SUCCESS且活动/等待0。各层输入都为request-s2，未构造UC验证方式要求的各层可区分输入与返回值；保留缺口。 |
| S3 非法输入 | `s3RejectsMissingAndInvalidChildInputs`（false/true） | PASS | 两个独立场景分别缺少必填INTEGER request、传not-a-number；父合法启动后call及父FAILED，错误包含request；无子运行、成功输出或after；各1个失败运行，活动/等待0。 |
| S4 目标不可运行 | `s4FailsWhenReferencedChildDefinitionWasDeleted` | PASS | 公开删除子定义并重查为空；父call与父FAILED，错误含目标key；无子运行/成功输出/after；1个失败运行，活动/等待0。 |
| S5 子运行失败 | `s5PropagatesChildBusinessFailureWithoutSuccessResult` | PASS | request-s5合法启动独立子运行；真实Log读取missing路径失败并保留来源、输入和错误；父call及父失败、无成功输出、after不执行；父子均FAILED，活动/等待0。 |

每个场景收尾的零活动资源断言均在fixture物理清理前经公开查询完成。S3的两个参数是同一场景的两次执行，不增加场景数量。

## 执行命令与结果

环境脚本：`/tmp/subflow-flat-env.sh`；报告隔离脚本：`/tmp/subflow-flat.init.gradle`。后者只设置Test XML/HTML/binary目录，不排除编译源码或改测试逻辑。

```bash
source /tmp/subflow-flat-env.sh
./gradlew -I /tmp/subflow-flat.init.gradle :core:test \
  --tests '*SubFlowTest' \
  --tests '*SubFlowExecutionIntegrationTest' \
  --tests '*ExecutorServiceTest' \
  --tests '*ExecutorEventTest' \
  --tests '*ExecutorEventMessageHandlerTest' \
  --tests '*PluginSchemaGeneratorTest'
```

`BUILD SUCCESSFUL in 1m 10s`；生产与测试编译完成，45 tests / 45 passed / 0 failed / 0 errors / 0 skipped。

| 类 | 执行数量 | 结果 |
| --- | --- | --- |
| Uc13SubFlowTest | 6 | 6通过；场景覆盖状态见上表 |
| SubFlowTest | 3 | 3通过，含扁平Schema必填字段、codec往返、旧嵌套形状拒绝及目标参数校验 |
| SubFlowExecutionIntegrationTest | 1 | 通过；Parallel同批2个SubFlow和1个Worker，分别返回正确调用节点并完成 |
| ExecutorServiceTest | 23 | 全通过 |
| ExecutorEventTest | 1 | 通过 |
| ExecutorEventMessageHandlerTest | 5 | 全通过 |
| PluginSchemaGeneratorTest | 6 | 全通过 |

不存在独立 `TaskPropertiesCodecTest`，codec由 `SubFlowTest.exposesAndPersistsFlatFlowFields` 验证。本轮没有首次失败，因此没有失败重跑。

证据：

- 日志：`/tmp/subflow-flat-0914.log`。
- XML：`build/subflow-flat-0914/core/test/xml/TEST-<完整类名>.xml`。
- HTML：`build/subflow-flat-0914/core/test/html/index.html`。
- UC XML：`build/subflow-flat-0914/core/test/xml/TEST-org.cses.flow.core.services.executions.Uc13SubFlowTest.xml`。

## 失败与反例

本轮无新失败或反例，既有场景缺口不得转换成PASS。

CE-01（历史未解决，本轮未复验，非本次重构新增）：UC04 S8发布Pause→ThrowingTask→never-run，经公开Service启动、查询Pause，提交`{"decision":"APPROVED"}`；后置抛出`uc04-downstream-failure`。既有UC要求异常返回、根与Pause保持提交前PAUSED、没有部分后续结果，再由用户取消；历史实际根为FAILED，断言`expected PAUSED but was FAILED`，因断言中止没有完成用户取消。历史独立重跑、Core和完整回归都复现；本轮只验证扁平配置，既不宣称问题已解决，也不将fixture物理清理算作此场景闭环。重新验证须先启动独占PG/Pulsar，然后执行：

```bash
./gradlew :core:test --tests '*Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance'
```

上轮还记录全仓架构路径、State、NestedPause输出和Log日志断言失败，本轮没有重新运行对应全套，不对其当前状态作判断。

## 数量汇总与结论

- UC场景5：PASS 3、FAIL 0、NOT_COVERED 2、BLOCKED 0。
- UC调用6/6成功；全部专项45/45成功，0跳过。
- 本轮扁平配置重构的专项验证通过；UC13完整验收仍受S1/S2历史观察缺口限制。
- 按本轮明确范围未跑全Core、Server及完整build，不能将本次结果称为全仓回归通过。
- UC13场景合计10个独立运行，场景结束活动流程0、等待Pause0；专项完成后只读数据库核对活动Execution0、PAUSED TaskRun0。
- 报告滚动保留：原2份，本次新增1、清理0、保留3；未修改其他UC或BATCH报告。

## 未解决事项与环境收尾

S1需要验证输入作为子返回结果被父消费；S2需要可区分的分层输入与返回值。本轮按主任务要求保留原断言，不扩展这些历史缺口。

测试依赖收尾后删除本轮独占的 `flow-subflow-pg-0914`、`flow-subflow-broker-0914` 及匿名卷，其他任务与业务资源未动。复现须重新创建独占环境并在空库加载当前完整基线；`IF NOT EXISTS` 不升级旧Schema，不能据此清空业务数据库。日志和XML保留。
