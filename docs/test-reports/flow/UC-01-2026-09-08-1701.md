# UC-01 Flow 草稿生命周期与多租户管理测试报告

## 基本信息

- UC：[UC-01 Flow 草稿生命周期与多租户管理](../../uc/flow/UC-01%20Flow%20%E8%8D%89%E7%A8%BF%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F%E4%B8%8E%E5%A4%9A%E7%A7%9F%E6%88%B7%E7%AE%A1%E7%90%86.md)，S1–S13；索引状态 ACCEPTED。
- 唯一主测试类：[Uc01FlowLifecycleTest](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/flows/Uc01FlowLifecycleTest.java)；本轮没有修改主测试类或 UC。
- 日期：2026-09-08，Asia/Shanghai；本类 JUnit 开始时间 `2026-09-08T08:33:41.043Z`（UTC），用时 5.742 秒。
- 代码版本：`64d381a425143f1a1d71ec8ee200a5880c3f5c26 + dirty`；包含本次最终作用域 CAS / `lock` 改造，UC-01 定向在最后子记录归属保护前执行，后续完整 core 再覆盖最终代码。
- 环境：Java 25.0.4、Gradle wrapper 9.5.1、真实 Micronaut 装配和 PostgreSQL 17.10；唯一数据库 `127.0.0.1:64628/flow`，容器 `flow-cas-20260908`。数据库测试未指向或清理默认 5432 实例。Java 21 没有本轮通过证据。

## 测试范围

通过公开 FlowService 保存、查询、修改、删除与发布 Flow 草稿；检查版本、审计、租户边界和删除后重用。仅定义生命周期，不要求创建运行实例。

所有现有场景均有唯一、未禁用的测试方法；没有新增、失效或错误归属方法。逐预期审计与方法运行结果分别统计，覆盖缺口保留为 NOT_COVERED，不以绿色方法替代完整验收。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 用户完成草稿的版本化生命周期 | `s1UserCompletesTheDraftLifecycle`（源码 38 行） | PASS | JUnit PASS。首次草稿 v1、修改 v2、删除 v3；最新查询内容和版本一致，删除后不可编辑。 |
| S2 用户保存无效草稿并从发布失败中恢复 | `s2UserRecoversFromAnInvalidDeployment`（源码 85 行） | PASS | JUnit PASS。无效原文以 v1 保存；发布拒绝且草稿不变、无正式版本；修改 v2 后删除。 |
| S3 两个租户独立管理内容相同的草稿 | `s3TenantsManageIdenticalDraftsIndependently`（源码 135 行） | PASS | JUnit PASS。两租户同 key 独立 v1；按租户查询行身份与内容，删除 A 不影响 B，双方删除。 |
| S4 同一租户的不同用户创建和修改草稿 | `s4UsersInOneTenantPreserveCreationAudit`（源码 185 行） | PASS | JUnit PASS。编辑后版本递增，creator/createdAt 保持，updater/updatedAt 与内容更新，最后删除。 |
| S5 用户查询不存在或错误的 Flow key | `s5WrongFlowKeysDoNotAffectTheControlDraft`（源码 239 行） | PASS | JUnit PASS。不存在 key 返回空，空白 key 拒绝，对照草稿内容、版本和审计不变，最后删除。 |
| S6 缺少租户身份的用户管理草稿 | `s6MissingTenantIdentityCannotManageDrafts`（源码 273 行） | PASS | JUnit PASS。无租户保存、查、改、删均拒绝，无新草稿/正式版本，对照不变并由所有者删除。 |
| S7 其他租户查询草稿 | `s7AnotherTenantCannotQueryTheDraft`（源码 325 行） | PASS | JUnit PASS。另一租户查询为空，所有者草稿不变并完成删除。 |
| S8 两个租户独立修改相同 Flow key 的草稿 | `s8TenantsReviseTheSameFlowKeyIndependently`（源码 354 行） | PASS | JUnit PASS。两个租户分别 v1→v2，独立内容和审计互不覆盖，最后分别删除。 |
| S9 其他租户删除草稿 | `s9AnotherTenantCannotDeleteTheDraft`（源码 423 行） | PASS | JUnit PASS。越租户删除拒绝；所有者仍可保存下一个版本并删除。 |
| S10 其他租户发布草稿 | `s10AnotherTenantCannotDeployTheDraft`（源码 465 行） | PASS | JUnit PASS。越租户发布拒绝，双方无正式版本，所有者草稿不变并删除。 |
| S11 用户在删除后继续使用同一 Flow key | `s11UserReusesTheFlowKeyAfterDeletingTheCurrentDraft`（源码 498 行） | PASS | JUnit PASS。删除后的查询与发布不可用；同 key 再保存得到更高版本，最后再次删除。 |
| S12 用户混用草稿查询和可运行 Flow 查询 | `s12DraftAndDeployedFlowQueriesStaySeparate`（源码 550 行） | PASS | JUnit PASS。草稿版本的正式查询为空；草稿查询保留版本和原文，最后删除。 |
| S13 用户在草稿内容中填写系统管理字段 | `s13SystemFieldsRemainRawDraftSourceOnly`（源码 584 行） | PASS | JUnit PASS。source 系统字段不覆盖真实身份、租户、版本与审计；发布拒绝，无正式版本，最后删除。 |

## 执行命令与结果

使用以下配置（口令仅通过环境注入，不写入报告）：

```bash
export JAVA_HOME=/Users/cses-7/Library/Java/JavaVirtualMachines/ms-25.0.4/Contents/Home
export FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:64628/flow
export FLOW_POSTGRES_TEST_USER=flow
export FLOW_POSTGRES_TEST_CLEANUP=true
# FLOW_POSTGRES_TEST_PASSWORD 由本地测试环境提供
```

1. `./gradlew :core:testClasses --console=plain --offline --max-workers=1`：exit 0，编译成功。
2. `./gradlew :core:test --tests org.cses.flow.core.services.flows.Uc01FlowLifecycleTest --console=plain --offline --max-workers=1`：exit 0，13 tests，13 PASS、0 FAIL、0 SKIPPED，5.742 秒。

随后执行 `./gradlew :core:test --console=plain --offline --max-workers=1`：exit 1，399 tests，389 PASS、9 FAIL、1 SKIPPED，4 分 15 秒。最后以共享任务图执行 `./gradlew test build --continue --console=plain --offline --max-workers=1`：exit 1，7 分 28 秒；core 再次 399 tests，389 PASS、9 FAIL、1 SKIPPED，本 UC 方法结果与定向一致。

Server AOT、打包及 assemble 成功，但 `server:compileTestJava` 在 `AdminSessionArgumentBinderTest.java:6` 因缺少 `org.paas.session.Device` 失败，Server 测试未运行；整体 build 不通过。已有 Server XML 时间为 2026-09-01，不计入本轮。gen:test 为 NO-SOURCE。core 的唯一 SKIPPED 是缺少第二个数据源配置的 FlowDatabaseIntegrationTest，不是本 UC 场景。

原始证据目录：`core/build/cas-uc-regression-e6X33w`。本类快照 `UC-01-results/TEST-org.cses.flow.core.services.flows.Uc01FlowLifecycleTest.xml`；模块快照 `core-results/`；最终快照 `full-test-build-results/` 与 `full-test-build.log`。本报告已写明关键结果，不依赖更早报告。

## 失败与反例

本 UC 没有观察到稳定违反用户预期的生产反例；本轮存在的证据缺口（如有）不分配 CE。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 13 | 13 | 0 | 0 | 0 |

JUnit 主类：13 方法，13 PASS、0 FAIL、0 SKIPPED；这不是上表的 UC 验收数量。

## 活动资源与滚动保留

2026-09-08 17:02:02 Asia/Shanghai，在完整 test/build 结束后，只读查询隔离容器 `flow-cas-20260908`：`company_id LIKE 'uc-%'` 的 Flow、FlowTask、Execution、TaskRun、Queue 均为 0；整个隔离库 Execution=0（活动=0）、TaskRun=0（PAUSED=0）、Queue=0。所有 UC fixture 已关闭并按所属租户清理，未删除其他数据库数据或停止容器。

物理清理的 0 不替代逐场景的业务结束断言；表中 NOT_COVERED 仍保持未覆盖。上述为 Test Agent 核对时状态；随后主 Agent 已删除本次隔离容器及专属测试卷，默认开发库保留。

本批新增 9 份 UC 报告与 1 份 BATCH。按 `^(UC-\d+|BATCH)-\d{4}-\d{2}-\d{2}-\d{4}\.md$` 识别并滚动保留最新 3 份；只删除 `UC-04-2026-08-31-1253.md`，未解决 CE-01 已完整带入本批。清理后各活跃 UC 组及 BATCH 各 3 份，历史 UC-03 组 2 份，共 32 份匹配报告；另保留 9 份非匹配旧 HHmmss 文件，总计 41 份。

## 测试结论

UC-01 本次逐场景 PASS；结论仅覆盖本 UC 的公开 Service 路径，不等于整个项目 build 或 HTTP 验收通过。

## 未解决事项

- 本 UC 无新增覆盖缺口。

- 项目回归仍有既有架构、State、NestedPause 和 Log 测试失败；未改写测试以隐藏失败。完整 test/build 命令退出 1：core 既有 9 个失败；server 测试编译在 AdminSessionArgumentBinderTest.java:6 缺少 org.paas.session.Device，server 行为测试未执行。FlowDatabaseIntegrationTest 因未提供独立的 FLOW_POSTGRES_DEFAULT_TEST_URL 而 SKIPPED；gen:test 为 NO-SOURCE。
- 主 Agent 本轮限定既有缺口只报告、不扩展修复；Test Agent 仅新增报告，复用原有真实数据库测试资源。
