# UC-09 用户定义并运行循环流程测试报告

## 基本信息

- UC：`docs/uc/flow/UC-09 用户定义并运行循环流程.md`
- 主测试类：`core/src/test/java/org/cses/flow/core/services/executions/Uc09LoopOrchestrationTest.java`
- 执行时间：2026-08-27 14:04～14:17 +08:00
- 版本：`070da94`，工作区 `dirty`
- 环境：Java 21.0.12、真实 Micronaut、PostgreSQL 17.10、临时库 `flow_uc_20260827_140410`

## 测试范围

验证 UC-09 S1～S6，覆盖固定次数循环、条件循环、最大轮数、循环体失败和无效定义。

## 场景验证

| 场景 | 测试方法 | 状态 | 结束证据 |
| --- | --- | --- | --- |
| S1 用户按固定次数完整执行循环体 | `s1FixedLoopRunsEveryBodyStepThreeTimesBeforeFollowingStep` | FAIL | 发布测试 Flow 时命中 CE-01，循环未开始。 |
| S2 条件在后续轮次满足后结束循环 | `s2LoopUntilStopsAfterSecondCompletedIteration` | FAIL | 发布测试 Flow 时命中 CE-01，未到达条件终止证据。 |
| S3 条件首次满足时仍完整执行一轮 | `s3LoopUntilCompletesOneFullIterationWhenConditionMatchesFirst` | FAIL | 发布测试 Flow 时命中 CE-01，未到达首轮完成证据。 |
| S4 达到最大轮数仍不满足条件 | `s4LoopUntilFailsAfterMaximumCompletedIterations` | FAIL | 发布测试 Flow 时命中 CE-01，未到达最大轮数失败证据。 |
| S5 循环体失败后停止创建后续轮次 | `s5BodyFailureStopsFixedAndConditionalLoops` | FAIL | 发布测试 Flow 时命中 CE-01，未到达循环体失败收敛证据。 |
| S6 用户发布无效循环定义 | `s6InvalidLoopDraftsAreRejectedAndDeletedWithoutResources` | FAIL | 保存待发布 Flow 时命中 CE-01，未到达无效定义拒绝与资源清理断言。 |

## 执行命令与结果

- 主类：6 tests，6 failed。
- 命令：`FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:5432/flow_uc_20260827_140410 FLOW_POSTGRES_TEST_USER=flow FLOW_POSTGRES_TEST_PASSWORD=flow FLOW_POSTGRES_TEST_CLEANUP=true ./gradlew :core:test --tests '*Uc09LoopOrchestrationTest' --rerun-tasks --max-workers=1 --no-daemon --console=plain`。

## 失败与反例

- CE-01 输入：六个方法分别提交固定循环、条件循环、失败循环体和无效循环定义的 YAML Flow。
- 预期：按 S1～S5 形成相应轮次与结束状态，S6 原子拒绝非法定义且不留资源。
- 实际：六个方法都在 Flow 持久化阶段抛出 `ActorRef` 无可用 Jackson serializer 的 `InvalidDefinitionException`。
- 影响：所有循环目标断言均未到达；同一方法又在 Core 全量回归中复现。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 6 | 0 | 6 | 0 | 0 |

## 测试结论

FAIL。六个循环场景都在 Flow 持久化阶段被稳定故障截断。

## 未解决事项

- 修复 CE-01 后原样复跑 S1～S6。
