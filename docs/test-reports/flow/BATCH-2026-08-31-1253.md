# Flow Generation 与 rewind UC 批量测试报告

## 基本信息

- 执行时间：2026-08-31 12:51～13:02 +08:00
- 代码版本：`c50ff0d`，工作区 dirty
- 范围：UC-04 全 11 场景；UC-09 全 6 场景；Generation/JSON Codec/Rewind 命令及
  Execution 有效结果的聚焦技术测试；根工程全量回归
- 环境：Java 21、真实 Micronaut、PostgreSQL 17.10，端口 55440
- 公开入口：`FlowService`、`ExecutionService`、Dispatch Queue 和公开 Execution 查询
- 测试数据：`FLOW_POSTGRES_TEST_CLEANUP=true`

## UC 汇总

| UC | 主测试类 | 场景数 | PASS | FAIL | NOT_COVERED | BLOCKED |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| UC-04 | `Uc04PauseFlowTest` | 11 | 10 | 1 | 0 | 0 |
| UC-09 | `Uc09LoopOrchestrationTest` | 6 | 6 | 0 | 0 | 0 |
| 合计 |  | 17 | 16 | 1 | 0 | 0 |

## 关键证据

- 正常 Pause Execution 查询到 `generation.current` 为空、history 为空。
- UC-04 S10：单次 rewind 的 v1/source/target/reason 可查询，只重跑 target→source
  Pause；重启后保持；最终 history 一条且下游读取新输出。
- UC-04 S11：第二次 rewind 使用新 Pause 与新目标，Current=v2，v1 进入 history；最终
  history 两条且下游读取 v2 输出。
- UC-09 S1：固定循环三个等待点显示 v1/v2/v3 和固定轮数继续原因；结束后三个历史
  轮次。
- UC-09 S2：LoopUntil 第二轮显示条件未满足原因，第二轮 DONE 后停止；结束后两个历史
  轮次。
- UC-09 S3～S6：首轮满足、最大轮数失败、循环体失败及五类无效定义/清理路径均通过。
- 上述 rewind、Loop、LoopUntil 均包含关闭并重建 Micronaut 上下文后的 PostgreSQL
  Generation 往返证据。
- 聚焦领域断言验证：回退完成后 `effectiveTaskRuns()` 排除旧片段 WARNING 和旧输出，
  只暴露最新代 target/source 记录与 `latest-success` 输出。
- `GenerationTest` 4/4、`GenerationJsonCodecTest` 2/2、`RewindTest` 1/1 通过；连同
  `ExecutionTest` 共 17/17 聚焦技术测试通过。

## 执行命令与结果

- `./gradlew :core:compileTestJava`：exit 0。
- UC-04 S10：exit 0。
- UC-04 S11：exit 0。
- UC-09 S1：首次测试选择失败；修正为按公开恢复字段选择后单独复验 exit 0。
- UC-09 S2：exit 0。
- UC-09 整类：exit 0，6/6 PASS。
- `ExecutionTest`、`GenerationTest`、`GenerationJsonCodecTest`、`RewindTest`：exit 0，
  17/17 PASS。
- UC-04 整类：exit 1，11 tests completed，S8 FAIL、其余 10 PASS。
- UC-04 S8 单独复验：exit 1，稳定 FAIL。
- 根工程 `./gradlew test`：exit 1，376 tests completed，5 failed，1 skipped。UC-04
  S8 为已知 CE-01；UC-09、Generation/rewind 聚焦测试均通过。其余四个失败分别位于
  UC-08、Log 集成、FlowService 和 PostgreSQL schema 断言，且均已独立复验。

所有真实执行命令均设置：

```text
FLOW_POSTGRES_TEST_URL=jdbc:postgresql://localhost:55440/flow
FLOW_POSTGRES_TEST_USER=flow
FLOW_POSTGRES_TEST_PASSWORD=flow
FLOW_POSTGRES_TEST_CLEANUP=true
```

## 失败与反例

- `CE-01`（UC-04 S8）：Pause 恢复后的后置 RunnableTask 抛出异常时，公开查询得到
  Execution=`FAILED`，而 UC 预期回到原 Pause=`PAUSED` 且无部分推进。整类与单方法
  复验均稳定失败。Test Agent 未修改生产代码或放宽断言。

以下为全量回归观察，与本次 Generation/rewind 指定场景分开归因：

- `REG-01`（UC-08/Log）：`Uc08DynamicLogFlowTest` S1 独立复跑仍得到
  `[处理结果：ready, test step]`，而断言期望仅 `[处理结果：ready]`；
  `LogFlowIntegrationTest` 独立复跑得到包含流程 `observe` 的 `test step`，而断言同样
  只接受动态日志。全量运行时还观察到一次来自待处理队列的额外前置 `test step`，说明
  日志精确列表断言同时存在异步隔离影响。两类独立复验均稳定失败，但不影响
  Generation/rewind 结果。
- `REG-02`（FlowService）：`prefersYamlKeysAndKeepsTheResolvedFlowKeyWhenLaterYamlOmitsIt`
  在第二版 Log task 省略必填 `message` 后发布，独立复跑仍抛出
  `IllegalArgumentException: message: must not be null`；测试原预期发布成功。
- `REG-03`（PostgreSQL schema）：`schemaUsesAggregateAndPluginTypes` 独立复跑仍在
  `flow_tasks`/`executions` 的 `flow_key`、`flow_version` 计数断言失败，期望 4、实际 0。

上述 REG 项未修改生产实现或放宽断言；它们不是 UC-04 S10/S11 或 UC-09 S1/S2 的
新反例。

## 活动资源

- 所有测试 `ApplicationContext` 已关闭。
- cleanup=true 已删除本轮随机租户测试数据。
- 最终仍在运行的本轮 Execution：0。
- 最终仍在等待恢复的本轮 TaskRun：0。

## 结论与未解决事项

批次结论：**FAIL**。Generation/rewind 的指定新增范围以及 UC-09 全部六场景均通过；
UC-04 CE-01 阻止 UC-04 整体验收。根工程全量回归另有 REG-01～REG-03，已与本次新增
功能结果分开记录。

独立报告：

- `UC-04-2026-08-31-1253.md`
- `UC-09-2026-08-31-1253.md`
