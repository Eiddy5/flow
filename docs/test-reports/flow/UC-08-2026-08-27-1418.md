# UC-08 用户在流程中输出动态日志测试报告

## 基本信息

- UC：`docs/uc/flow/UC-08 用户在流程中输出动态日志.md`
- 主测试类：`core/src/test/java/org/cses/flow/core/services/executions/Uc08DynamicLogFlowTest.java`
- 执行时间：2026-08-27 14:04～14:17 +08:00
- 版本：`070da94`，工作区 `dirty`
- 环境：Java 21.0.12、真实 Micronaut、PostgreSQL 17.10、临时库 `flow_uc_20260827_140410`

## 测试范围

验证 UC-08 S1～S3，覆盖动态日志成功渲染、非法消息表达式和运行值缺失三种路径。

## 场景验证

| 场景 | 测试方法 | 状态 | 结束证据 |
| --- | --- | --- | --- |
| S1 输出包含前置结果的日志并继续流程 | `s1RendersPrecedingResultOnceAndContinuesTheFlow` | FAIL | 发布测试 Flow 时命中 CE-01，日志任务未开始。 |
| S2 发布包含无效消息表达式的流程 | `s2RejectsInvalidMessageExpressionBeforePublishing` | FAIL | 保存待发布 Flow 时命中 CE-01，未到达表达式拒绝证据。 |
| S3 日志表达式引用不存在的运行值 | `s3MissingRuntimePathFailsWithoutWritingAMisleadingLog` | FAIL | 发布测试 Flow 时命中 CE-01，未形成运行期失败与无误导日志证据。 |

## 执行命令与结果

- 主类：3 tests，3 failed。
- 命令：`FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:5432/flow_uc_20260827_140410 FLOW_POSTGRES_TEST_USER=flow FLOW_POSTGRES_TEST_PASSWORD=flow FLOW_POSTGRES_TEST_CLEANUP=true ./gradlew :core:test --tests '*Uc08DynamicLogFlowTest' --rerun-tasks --max-workers=1 --no-daemon --console=plain`。

## 失败与反例

- CE-01 输入：三个方法分别发布正常日志表达式、非法表达式和引用缺失运行值的 YAML Flow。
- 预期：分别形成一次正确日志并继续、发布拒绝、运行失败且不写误导日志。
- 实际：三个方法都在 Flow 持久化阶段抛出 `ActorRef` 无可用 Jackson serializer 的 `InvalidDefinitionException`。
- 影响：三个日志场景均未到达目标断言；同一方法又在 Core 全量回归中复现。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 3 | 0 | 3 | 0 | 0 |

## 测试结论

FAIL。三个日志场景都在 Flow 持久化阶段被稳定故障截断。

## 未解决事项

- 修复 CE-01 后原样复跑 S1～S3。
