# UC-01 Flow 定义生命周期复验报告

## 基本信息

- UC 文档：`docs/uc/flow/UC-01 Flow 草稿生命周期与多租户管理.md`
- 主测试类：`server/src/test/java/org/cses/flow/core/services/flows/Uc01FlowLifecycleTest.java`
- 验证领域：`flow`
- 测试类型：工厂与兼容层清理后的完整回归
- 执行时间：`2026-07-29 12:00-12:04 CST`
- 代码版本：`b665c29`，工作区 `dirty`
- 上次报告：`docs/test-reports/flow/UC-01-2026-07-28-2322.md`

## 测试范围

完整构建在真实 PostgreSQL 上执行了现有 S1～S7。现有主测试类仍验证旧
`FlowDefinition` 生命周期，而 UC 目标要求 `FlowWithSource -> Flow.deploy`
模型；本轮 Factory 清理没有消除该模型差距，因此只记录旧回归成功，不把它作为
目标 UC 的通过证据。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 | `s1CreatesAndEditsDraftWithStableIdentity` | BLOCKED | 旧草稿保存及稳定 Task ID 测试通过；未覆盖目标来源草稿模型 |
| S2 | `s2PublishesUpgradesAndClosesFlow` | BLOCKED | 旧发布、升级和关闭测试通过；未覆盖目标 `Flow.deploy` |
| S3 | `s3RejectsInvalidDefinitionsAndLifecycleOperationsAtomically` | BLOCKED | 旧非法定义和原子性断言通过；目标部署链尚未实现 |
| S4 | `s4IsolatesFlowReadsAndWritesByTenant` | BLOCKED | 旧租户隔离断言通过；不能替代目标模型证据 |
| S5 | `s5RequiresExactVersionAndStatusReferences` | BLOCKED | 旧精确版本查询断言通过；不能替代目标模型证据 |
| S6 | `s6KeepsTaskIdentityStableAcrossVersions` | BLOCKED | 静态创建链下跨版本 Task ID 复用通过；身份仍在保存草稿时协调 |
| S7 | `s7RejectsAStaleDraftSaveWithoutOverwritingCommittedContent` | BLOCKED | 旧 revision 冲突及无覆盖断言通过；不能替代目标模型证据 |

## 执行命令与结果

- 真实 PostgreSQL Repository 聚合往返：1 test，成功。
- `./gradlew build`（配置 PostgreSQL 测试连接和自动清理）：78 tests，
  0 failures，0 errors，0 skipped；UC-01 主测试类 7 tests 全部执行成功。

## 失败与反例

没有运行失败，未创建 CE。阻塞来自 UC 与当前生效领域目标之间的既有差距。

## 数量汇总

- PASS：0
- FAIL：0
- NOT_COVERED：0
- BLOCKED：16

## 测试结论

旧模型回归成功；目标 UC-01 仍为 BLOCKED。

## 未解决事项

继续按 ADR 0008 和 `flow-definition-lifecycle.md` 完成来源草稿与部署模型迁移。
