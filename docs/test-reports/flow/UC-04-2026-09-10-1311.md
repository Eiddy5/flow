# UC-04 用户恢复处于 Pause 的 Flow测试报告

## 基本信息

时间：2026-09-10 13:11 Asia/Shanghai。代码：`01cc2d400b3597ab49e2cddb6a81865fa2dfb1e7 dirty`。

UC：[UC-04](</System/Volumes/Data/workspace/java/flow/docs/uc/flow/UC-04 用户恢复处于 Pause 的 Flow.md>)，ACCEPTED；唯一主类：[Uc04PauseFlowTest.java](</System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc04PauseFlowTest.java>)。

## 测试范围

本轮验证 Execution 自带 Long lock、无显式作用域的 CAS 仓储改造；业务 UC 预期不变。真实 Micronaut、PostgreSQL、Pulsar，经公开 FlowService/ExecutionService 操作并重新查询。保留工作区的 Task 输出和 SubFlow 等并行改动；Test Agent 未修改生产、UC 或测试源码。

覆盖 11 个场景、11 次独立 JUnit 调用。以 315 个源码、资源、UC 和构建文件 SHA-256 记录执行边界：独立类阶段出现 OrchestrationTask.java、SubFlow.java、TaskPluginTestSupport.java、SubFlowTest.java 的并行修改；UC 文档和主类未变。Core 前→完整回归前→完整回归后这 315 项均一致，最终完整回归覆盖当前版本；不把早期独立执行称为不可变全仓快照。清单保存在本轮证据目录的 source-hashes-*.json。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 用户查询并恢复单个处于 Pause 的 Flow | `s1UserQueriesAndResumesOnePausedFlow` | PASS | 已核对：Pause 状态可以从 Flow 中查询；本次正常运行没有退回历史；正常恢复后同一个运行实例继续执行，后置步骤取得提交结果。结束：Flow 已完成，没有仍在等待恢复的 Pause 步骤。 |
| S2 用户提交 Pause 结果后只推进当前 Flow | `s2ResumeResultIsConsumedOnceByOnlyTheCurrentFlow` | PASS | 已核对：提交结果只用于当前 Flow 的目标 Pause，目标 Pause 和后续步骤各执行一次。结束：Flow 已完成，没有重复步骤或等待恢复的 Pause。 |
| S3 非法输入后用户使用正确结果恢复 Flow | `s3IllegalResumeInputsAreRejectedUntilValidResultIsSubmitted` | PASS | 已核对：每个非法请求都被明确拒绝，拒绝期间 Flow 和 Pause 状态不变；合法结果能够恢复 Flow。结束：Flow 已完成，没有遗留等待恢复的 Pause。 |
| S4 用户重复恢复已经结束的 Pause | `s4RepeatedResumeOfFinishedPauseIsRejectedWithoutMutation` | NOT_COVERED | 重复恢复被拒绝，根 State、TaskRun ID/数量及 Pause 输出不变；没有逐条比较 TaskRun 状态历史。 测试调用通过，但不足以确认完整预期。 |
| S5 其他租户查询或恢复处于 Pause 的 Flow | `s5OtherTenantCannotQueryOrResumeThePausedFlow` | PASS | 已核对：租户 B 看不到租户 A 的运行实例且恢复请求被拒绝，租户 A 的 Flow 不受影响。结束：租户 A 的 Flow 已完成。 |
| S6 同一用户在两个处于 Pause 的 Flow 间选择准确实例 | `s6SameUserResumesTheSelectedInstanceOnly` | NOT_COVERED | 已验证第二实例先完成、第一仍等待，第一随后保存 FIRST；未断言第二实例实际结果为 SECOND。 测试调用通过，但不足以确认完整预期。 |
| S7 Flow 取消后用户恢复原 Pause | `s7CancelledFlowRejectsResumeOfItsFormerPause` | PASS | 已核对：恢复请求被明确拒绝，不会重新启动已取消的 Flow 或创建后续步骤。结束：Flow 保持已取消，没有等待恢复的 Pause。 |
| S8 Pause 结果提交后的后续处理发生异常 | `s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance` | FAIL | 要求恢复到提交前 PAUSED 并无部分推进；实际 FAILED（断言 PAUSED/FAILED）。用户取消收尾未执行，CE-01。 |
| S9 系统重启后用户继续恢复处于 Pause 的 Flow | `s9FreshServerCanRequeryAndResumeTheSamePause` | PASS | 已核对：无需依赖重启前保存的页面对象或内部信息，也能通过 Flow 查询找到同一 Pause 并继续同一运行实例。结束：Flow 已完成，没有等待恢复的 Pause。 |
| S10 用户从 Pause 退回到指定历史步骤并在新实例继续 | `s10UserRewindsFromPauseToHistoricalStepAndContinues` | PASS | 已核对：退回发起源是用户当时查询到的已创建且正在等待的 Pause，退回目标是用户选定的历史步骤运行记录；每次合法退回只产生一个不同编号的新运行实例，原实例停止且不再推进，由新实例接替重新执行及后续流程；退回返回结果给出新运行编号、退回受理时的原实例快照和受影响片段的步骤运行编号列表；列表只包含退回发起时当前有效运行路径上从目标步骤到源 Pause 的运行记录且两端均包含，同时包含这些步骤下真实发生的子步骤运行记录；未命中条件路径的已跳过运行记录作为叶子包含在列表中，其未创建的子步骤不出现；列表按业务回滚顺序排列，较晚发生的记录和子步骤在前、所属父步骤在后，目标步骤最后；只有目标步骤到源 Pause 的片段被重新执行并产生新的步骤运行编号，目标之前的有效结果和原步骤运行编号保留且不重复执行，源 Pause 之后的步骤不会提前执行；查询结果能够识别本次退回的新实例、源 Pause 步骤运行编号、目标步骤运行编号和退回原因，并区分沿用记录与重新执行记录；退回前真实发生的记录仍可查询，新的 Pause 归属于新实例并可正常恢复，后置步骤使用重新执行的片段结果。结束：新实例已完成，原实例已停止，历史保留本次退回及退回前后的真实步骤记录，没有等待恢复的 Pause。 |
| S11 用户在新的 Pause 再次退回 | `s11UserRewindsAgainFromTheNewPause` | PASS | 已核对：第二次退回以第一次新实例中已创建且正在等待的 Pause 作为新源，并以用户第二次选定的当前有效历史步骤运行记录作为新目标；两次退回分别产生不同编号的新实例，每次的原实例停止，由新实例接替重新执行；两次返回结果都给出各次新运行编号、该次退回受理时的原实例快照和按业务回滚顺序排列的受影响步骤运行编号列表；每个列表只反映该次退回发起时的当前有效运行路径，不混入已经被替代的上一段运行记录；两次退回的源步骤运行编号、目标步骤运行编号、退回原因和真实执行记录均可查询；每次只重新执行对应的退回片段并产生新的步骤运行编号，最终后置步骤使用最新实例的片段结果。结束：最新实例已完成，前两个实例均已停止，两次退回及各次真实步骤记录均保留，没有等待恢复的 Pause。 |

## 执行命令与结果

实际 JDK 25.0.4（不证明 Java 21 兼容）；测试库 `jdbc:postgresql://127.0.0.1:64991/flow_inline_lock`、用户 flow；Pulsar `pulsar://127.0.0.1:56659`、admin `http://127.0.0.1:65093`。密码由环境注入，`FLOW_POSTGRES_TEST_CLEANUP=true`。运行时数据源和 Broker 同步指向这些专属资源，Gradle/AOT 保留默认数据源和消费者禁用守卫，UC 夹具显式启用真实消费者；最终 Core Test 的两项误继承环境已按下文隔离移除。未访问默认 5432 业务库。

实际执行：

```bash
FLOW_POSTGRES_TEST_PASSWORD='<scratch 密码>' bash core/build/cas-inline-lock-uc-pvi5G0/run.sh uc04 :core:test --rerun --tests '*Uc04PauseFlowTest'
```

runner 使用 `./gradlew -I /private/tmp/flow-cas-refactor.4sTnkB/isolated-tests.gradle --offline --max-workers=1 --console=plain --no-build-cache`。init 只隔离 binary/XML/HTML 与标准流展示，不排除编译源码、不替换实现或断言。该类 XML：[独立执行](</System/Volumes/Data/workspace/java/flow/core/build/cas-inline-lock-uc-pvi5G0/uc04/core/test/xml/TEST-org.cses.flow.core.services.executions.Uc04PauseFlowTest.xml>)；日志：`/System/Volumes/Data/workspace/java/flow/core/build/cas-inline-lock-uc-pvi5G0/uc04/run.log`。

| 阶段 | 本类实际结果 |
| --- | --- |
| 独立执行 | 11 tests / 10 passed / 1 failed / 0 skipped |
| Core 模块 `:core:test --rerun` | 11 tests / 10 passed / 1 failed / 0 skipped |
| 完整 `test --rerun build --continue` + probe | 11 tests / 10 passed / 1 failed / 0 skipped |

S8 单独重跑 1 failed，保持原输入与断言；其他场景不受重跑计数影响。 原 Core 全局环境导致另 8 项数据源/队列技术失败（原总数 419 passed / 16 failed / 1 skipped）。经安全核对，仅在独占 `test-environment.gradle` 为 Core Test 移除误继承的 `DATASOURCES_DEFAULT_ENABLED` 和 `PULSAR_CONSUMER_ENABLED`，保留测试原有 properties/mock 契约及 AOT/Server 守卫。两类 10/10 复核及最终完整测试通过，原失败日志仍保留。

完整阶段实际命令：`FLOW_POSTGRES_TEST_PASSWORD='<scratch 密码>' bash core/build/cas-inline-lock-uc-pvi5G0/run.sh full -I core/build/cas-inline-lock-uc-pvi5G0/test-environment.gradle -I docs/harness/flow-operation-cost/probe.gradle test --rerun build --continue :server:flowQueryProbe`。本类最终 XML：`/System/Volumes/Data/workspace/java/flow/core/build/cas-inline-lock-uc-pvi5G0/full/core/test/xml/TEST-org.cses.flow.core.services.executions.Uc04PauseFlowTest.xml`。

## 失败与反例

CE-01（历史未解决，本轮再次复现）：UC-04 S8 通过公开 Service 发布 Pause → ThrowingTask → never-run，启动并查询 Pause，提交 `{"decision":"APPROVED"}`；后置任务抛出 `uc04-downstream-failure`。UC 要求异常明确返回、根与目标 Pause 保持提交前 PAUSED、不产生部分后续结果，再由用户取消完成收尾。实际根进入 FAILED，`Uc04PauseFlowTest.java:273` 报 `expected: <PAUSED> but was: <FAILED>`，断言中断，未执行用户取消。独立整类、同一方法重跑、Core 和完整回归均保留该反例；没有修改 UC、业务代码、超时或断言。影响是失败恢复场景不满足 UC 的原子用户体验；不能用 fixture 物理清理代替用户闭环。此方法同预期/实际已有 2026-09-09 记录，未经版本二分，不据此宣称本次 CAS 改造造成或修复了问题。

复现（需按本报告环境启动独占依赖）：

```bash
FLOW_POSTGRES_TEST_PASSWORD='<scratch 密码>' bash core/build/cas-inline-lock-uc-pvi5G0/run.sh retry-uc04-s8 :core:test --rerun --tests '*Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance'
```

已执行目录有覆盖保护；再次复现请使用新的 stage 名。单独重跑 XML：`/System/Volumes/Data/workspace/java/flow/core/build/cas-inline-lock-uc-pvi5G0/retry-uc04-s8/core/test/xml/TEST-org.cses.flow.core.services.executions.Uc04PauseFlowTest.xml`。

## 数量汇总

场景：PASS 8 / FAIL 1 / NOT_COVERED 2 / BLOCKED 0。JUnit 调用和场景数量分开统计；参数化调用不算新增场景，测试通过不补足缺失断言。

## 测试结论

本 UC 尚不能完整验收；失败与观察缺口如上，未放宽预期。 最终完整回归 Core 436 tests / 427 passed / 8 failed / 1 skipped，Server 20/20 passed；合计 456 tests / 447 passed / 8 failed / 1 skipped。整个 `test build --continue` 因 Core 失败 exit 1，不宣称全仓通过；正常 query probe 完成，AOT/打包任务本轮为 UP-TO-DATE，不宣称重新生成产物。

## 未解决事项

S4：重复恢复被拒绝，根 State、TaskRun ID/数量及 Pause 输出不变；没有逐条比较 TaskRun 状态历史。

S6：已验证第二实例先完成、第一仍等待，第一随后保存 FIRST；未断言第二实例实际结果为 SECOND。
2026-09-10 13:10–13:11 只读审计本轮专属 flow_inline_lock：flows=0、flow_tasks=0、executions=0、task_runs=0、inherited_task_runs=0；活动 Execution=0、PAUSED Execution=0、活动 TaskRun=0、等待 TaskRun=0。专属 Pulsar 的 flow-executor-command 和 flow-executor-event 均为 backlog=0、unacked=0、consumers=0、publishers=0。审计证据为 `/System/Volumes/Data/workspace/java/flow/core/build/cas-inline-lock-uc-pvi5G0/resource-audit.json`，SQL 为同目录 resource-audit.sql。审计时保留 2 个本轮专属容器和 1 个 PG 匿名卷。随后主 Agent 按核对的完整 ID 删除 flow-cas-lock-20260910、flow-cas-pulsar-20260910 及专属 PG 卷，并确认容器/卷列表无匹配项；最终本轮容器=0、卷=0，临时测试数据销毁，日志/XML/报告及备份保留。未操作其他容器或默认开发库。复现须重新建立独占依赖，原测试端点已关闭。 物理测试清理不替代失败/未覆盖场景的用户收尾。未执行真实外部 UI、负载或 Broker 集群故障验收。

滚动保留：本组新增 1 份、清理 1 份、保留 3 份；本批共新增 13 份、清理 12 份，所有规范命名组均不超过 3 份。本组被删旧报告 `UC-04-2026-09-09-1702.md` 已预先复制并验证 SHA-256，可从 `/System/Volumes/Data/workspace/java/flow/core/build/cas-inline-lock-uc-pvi5G0/retained-old-reports/UC-04-2026-09-09-1702.md` 恢复。 UC-03、非规范秒级旧名及其他领域未改动。
