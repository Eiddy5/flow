# UC-09 用户定义并运行循环流程：删除旧队列后的验证记录

## 基本信息与范围

时间 2026-09-10-1208 Asia/Shanghai。目标UC [UC-09 用户定义并运行循环流程.md](</private/tmp/flow-queue-cleanup-working-c0_grzzq/docs/uc/flow/UC-09 用户定义并运行循环流程.md>)（ACCEPTED），唯一主类 [Uc09LoopOrchestrationTest](/private/tmp/flow-queue-cleanup-working-c0_grzzq/core/src/test/java/org/cses/flow/core/services/executions/Uc09LoopOrchestrationTest.java)。本次主类与UC未修改；上面链接指向冻结树，便于与共享区后续并行编辑区分。

UC SHA256 `c5f210064c607b5ef93c3d8ea5d67eb5959bb5451e2068c2b8bb947f74847426`；主类SHA256 `7b47ac39b507fd48eacdda83ae3b4e8c7be759073bcad285261a110e5342cdef`。

独立UC命令计划 `./gradlew :core:test --rerun --tests '*Uc09LoopOrchestrationTest'`，因生产编译阻断未执行（not_run）。本轮没有该UC新的JUnit执行快照，不引用旧结果充当本轮通过。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 用户按固定次数完整执行循环体 | [s1FixedLoopRunsEveryBodyStepThreeTimesBeforeFollowingStep](/private/tmp/flow-queue-cleanup-working-c0_grzzq/core/src/test/java/org/cses/flow/core/services/executions/Uc09LoopOrchestrationTest.java:40) | BLOCKED | 本轮未运行；完整冻结树生产编译阻断，没有用户操作、断言执行或业务终态证据。 |
| S2 条件在后续轮次满足后结束循环 | [s2LoopUntilStopsAfterSecondCompletedIteration](/private/tmp/flow-queue-cleanup-working-c0_grzzq/core/src/test/java/org/cses/flow/core/services/executions/Uc09LoopOrchestrationTest.java:141) | BLOCKED | 本轮未运行；完整冻结树生产编译阻断，没有用户操作、断言执行或业务终态证据。 |
| S3 条件首次满足时仍完整执行一轮 | [s3LoopUntilCompletesOneFullIterationWhenConditionMatchesFirst](/private/tmp/flow-queue-cleanup-working-c0_grzzq/core/src/test/java/org/cses/flow/core/services/executions/Uc09LoopOrchestrationTest.java:228) | BLOCKED | 本轮未运行；完整冻结树生产编译阻断，没有用户操作、断言执行或业务终态证据。 |
| S4 达到最大轮数仍不满足条件 | [s4LoopUntilFailsAfterMaximumCompletedIterations](/private/tmp/flow-queue-cleanup-working-c0_grzzq/core/src/test/java/org/cses/flow/core/services/executions/Uc09LoopOrchestrationTest.java:269) | BLOCKED | 本轮未运行；完整冻结树生产编译阻断，没有用户操作、断言执行或业务终态证据。 |
| S5 循环体失败后停止创建后续轮次 | [s5BodyFailureStopsFixedAndConditionalLoops](/private/tmp/flow-queue-cleanup-working-c0_grzzq/core/src/test/java/org/cses/flow/core/services/executions/Uc09LoopOrchestrationTest.java:316) | BLOCKED | 本轮未运行；完整冻结树生产编译阻断，没有用户操作、断言执行或业务终态证据。 |
| S6 用户发布无效循环定义 | [s6InvalidLoopDraftsAreRejectedAndDeletedWithoutResources](/private/tmp/flow-queue-cleanup-working-c0_grzzq/core/src/test/java/org/cses/flow/core/services/executions/Uc09LoopOrchestrationTest.java:353) | BLOCKED | 本轮未运行；完整冻结树生产编译阻断，没有用户操作、断言执行或业务终态证据。 |

## 数量汇总与结论

6场景：PASS0 / FAIL0 / NOT_COVERED0 / BLOCKED6。本轮统一按未能运行的编译阻断判定；表中已知静态断言缺口另列，不表示已覆盖。该UC未完成验收。

## 环境、执行命令与快照边界

本次 Test Agent 只删除旧队列专属测试、将四种命令及 ExecutorEvent 的原有字段/具体类型/身份断言迁移到 PAAS JacksonSchema、移除 PostgresJooqTestAdapter 的旧表清理、更新队列架构断言并增加测试引导。未修改生产或 UC/主测试类预期。业务验证使用真实 PostgreSQL + Pulsar，不以旧 PG 队列或内存队列替代。

- 代码基线 `01cc2d400b3597ab49e2cddb6a81865fa2dfb1e7`，共享工作区 **dirty**，有其他任务正在修改 Output、CAS、SubFlow。首轮执行没有完整冻结全部源码，其通过结论仅对应首轮 XML。
- HEAD + 本次 32 项清理的隔离快照 `/private/tmp/flow-queue-cleanup-snapshot-b_qfwksq`：32/32 清理指纹匹配。该 HEAD 已有不完整 Output 迁移，不能作为可编译基线。
- 完整冻结工作树 `/private/tmp/flow-queue-cleanup-working-c0_grzzq`：包含并行 Output/CAS/SubFlow 源码；556/556 文件指纹未变。此快照用于最终 Core/full 命令，但也在生产编译阶段失败。不会把首轮成功计入该快照。
- manifest 与核对证据：`/System/Volumes/Data/workspace/java/flow/core/build/queue-cleanup-20260910/queue-cleanup-snapshot.json`、`snapshot-hash-check.json`、`working-snapshot.json`、`working-hash-check.json`；UC/主类指纹在 `working-source-hashes.json`。

本次专属 PostgreSQL 17 容器 `flow-queue-cleanup-gen-0910`，端口 55171，两个库 `flow` 与 `flow_origin` 均执行新四表基线；后者用于保留 Generator 数据库元数据并作为可选第二数据源。主 Agent 执行基线两次与真实 JOOQ 生成，日志 `/private/tmp/flow-queue-cleanup-schema.log`。没有对任何已有环境 DROP 旧表或清空旧消息。

Pulsar 容器 `flow-queue-cleanup-broker-0910`，镜像 apachepulsar/pulsar:4.0.7；容器内外二进制端口56650，admin58081→8080，`standalone --advertised-address 127.0.0.1 --no-functions-worker --no-stream-storage`。使用真实 Micronaut、PAAS Client/Factory/Schema/Runner。实际 JDK25.0.4，不是Java21通过证据。

```bash
export JAVA_HOME=/Users/cses-7/Library/Java/JavaVirtualMachines/ms-25.0.4/Contents/Home
export FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:55171/flow
export FLOW_POSTGRES_TEST_USER=flow
export FLOW_POSTGRES_TEST_CLEANUP=true
export FLOW_POSTGRES_DEFAULT_TEST_URL=jdbc:postgresql://127.0.0.1:55171/flow_origin
export FLOW_POSTGRES_DEFAULT_TEST_USER=flow
export FLOW_PULSAR_TEST_URL=pulsar://127.0.0.1:56650
export FLOW_PULSAR_TEST_ADMIN_URL=http://127.0.0.1:58081
# 密码通过对应 FLOW_POSTGRES_*_PASSWORD / FLOW_DATASOURCE_PASSWORD 注入
```

Wrapper 公共选项：`-I /private/tmp/flow-pulsar-uc-isolated.gradle --offline --max-workers=1 --console=plain`；按 `FLOW_TEST_RUN_DIR=/System/Volumes/Data/workspace/java/flow/core/build/queue-cleanup-20260910/<阶段>` 隔离结果，init script只控制输出目录和日志，不替换业务实现。三个runner分别为 `/private/tmp/flow-queue-cleanup-run.sh`、`flow-queue-cleanup-snapshot-run.sh`、`flow-queue-cleanup-working-run.sh`；各自cwd分别为共享区、HEAD+cleanup快照和完整冻结快照。

专项选择器 T：`--tests '*ExecutorPulsarIntegrationTest' --tests '*PulsarQueueTest'`，加 `org.cses.flow.executor.commands.{Create,Resume,Cancel,Rewind}Test` 四个单独 `--tests`、`org.cses.flow.executor.ExecutorEventTest`，以及 `CoreArchitectureStandardTest` 的 `queueContractsRemainTopLevelPeers`、`executorUsesTheAnnotationDrivenPulsarQueues`、`queueImplementationHasNoDatabaseAdapter`、`postgresBatchWritesBuildOneValuesInsert` 四个方法选择器。后续阶段再加入 `--tests '*Uc04PauseFlowTest.s1UserQueriesAndResumesOnePausedFlow'`。

| 阶段 / 日志 | Wrapper 主体 | 实际结果 |
| --- | --- | --- |
| technical | `./gradlew :core:test` + T | exit1；18 tests / 13 passed / 5 failed / 0 skipped |
| technical-fixed | 同上 | exit1；core:compileJava blocked，测试not_run |
| technical-final | 同上 + UC04 S1 | exit1；core:compileJava blocked，测试not_run |
| snapshot-technical | 同上 + UC04 S1，HEAD+cleanup快照 | exit1；core:compileJava blocked，测试not_run |
| working-technical | 同上 + UC04 S1，完整冻结快照 | exit1；core:compileJava blocked，测试not_run |
| working-core | `./gradlew :core:test --rerun`，完整冻结快照 | exit1；core:compileJava blocked，测试not_run |
| working-full | `./gradlew test --rerun --continue`，完整冻结快照 | exit1；Core/Server测试not_run；gen/processor test为NO-SOURCE |

日志均为 `/System/Volumes/Data/workspace/java/flow/core/build/queue-cleanup-20260910/<阶段>.log`；只有首轮technical产生JUnit XML：`technical/core/test/xml/TEST-*.xml`。模块/全量没有测试计数，不能写成0失败或全部通过。11UC独立执行及S8反例复验因统一生产编译阻断而not_run，未重复对每个选择器触发相同编译。双数据源隔离未运行，不计实际skipped。首轮实际skipped=0；负载、集群故障、HTTP界面及通知均not_run。

## 首轮技术证据与未完成修正

首轮9项Pulsar测试通过：`ExecutorPulsarIntegrationTest` 2/2（真实Broker的命令/事件Schema、Queue/PulsarQueue同实例注入、阻塞回调未ACK、跨队列消息、抛错后NACK重投及最终ACK）；`PulsarQueueTest` 7/7（真实PAAS但受控网络边界）。另4项架构通过，合计13通过。这不是UC验收，也不代表最终冻结快照通过。

迁移的Create、Resume、Cancel、Rewind、ExecutorEvent五个测试均失败：PAAS JacksonSchema调用 `org.paas.json.Jackson.instance`，原独立测试只初始化JsonFactory，产生NullPointerException。测试引导已改为通过真实Micronaut容器初始化PAAS Jackson与JsonFactory；不手建ObjectMapper、不启用消费者。该修正，以及QueuesRecord/QueuesObject不存在检查的精确路径修正，之后均未进入测试执行，必须在生产可编译后复验。5个失败没有被删掉或放宽，也没有被记为修复通过。

## 编译阻断证据

1. 共享区 technical-fixed：Execution.java:751，copy调用12参数构造器，但当时构造器已新增Long参数。
2. 共享区 technical-final：ExecutionCommandEventHandler.java:100引用已删除的FlowDatabase.execute方法。
3. HEAD+cleanup快照：LoopUntil.java:157/161的Output类型冲突/getType不存在；WorkerDispatcher.java:20的Output不能赋给RunResult。
4. 完整冻结快照 working-technical、working-core、working-full：ExecutorService.java:346新增 `completeSubFlow(ExecutorContext, Flow, Execution)` 找不到Flow类型；同一冻结源码未作修补。

这些生产文件均不属于本次旧队列清理的两消息类型变更，Test Agent没有修复、回滚或继续组合基线。本轮只报告实际可复核边界。

## 反例与未解决事项

CE-01（历史未解决，本轮 not_run）：UC04 S8 通过公开 Service 发布 Pause → ThrowingTask → never-run，启动并查询 Pause，提交 `{"decision":"APPROVED"}`；后置任务抛出 `uc04-downstream-failure`。UC 要求异常明确返回、根与目标 Pause 回到提交前 PAUSED、不产生部分后续结果，再由用户取消完成清理。上轮真实 Pulsar 整类执行实际进入 FAILED，断言 `expected PAUSED but was FAILED`；本轮因编译阻断，没有运行该方法，也没有执行用户取消，不能称已复验、修复或本轮新增失败。复现命令：`./gradlew :core:test --rerun --tests '*Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance'`，须在可编译源码及下述真实环境运行。物理清理不能代替用户闭环。历史差异也曾发生在替换前的 PostgreSQL 队列，未经二分，不将其归因为本次清理。

## 活动资源与清理

### PostgreSQL flow

```text
BEGIN
executions_total=0, active=0
task_runs_total=0, paused=0, other_active=0
inherited_total=0, paused=0, other_active=0
queue_table_exists=false
public_tables=executions,flow_tasks,flows,task_runs
flows_total=0
flow_tasks_total=0
COMMIT
```

### PostgreSQL flow_origin

```text
BEGIN
executions_total=0, active=0
task_runs_total=0, paused=0, other_active=0
inherited_total=0, paused=0, other_active=0
queue_table_exists=false
public_tables=executions,flow_tasks,flows,task_runs
flows_total=0
flow_tasks_total=0
COMMIT
```

Broker最终 `public/default` Topic清单为 `[]`（`broker-topics.json`），两个Executor Topic stats均返回404（`broker-command-status.log`、`broker-event-status.log`）。因此没有残留Topic/订阅可供继续消费；不伪造不存在Topic的backlog计数。首轮实际消息callback/ACK断言见technical XML。最终11UC均未运行，数据库空状态不构成UC成功证明。

两个专属容器及匿名卷已删除：`flow-queue-cleanup-gen-0910`、`flow-queue-cleanup-broker-0910`，删除证据 `/System/Volumes/Data/workspace/java/flow/core/build/queue-cleanup-20260910/container-cleanup.log`。其他容器、已有业务数据库/Broker未操作。最终运行流程0、等待任务0（包括继承快照）；物理清理不作为用户业务收尾证据。

本轮未扩展并行新增的UC13 SubFlow需求：它后来出现在完整冻结树索引，但不属于本次已约定的11UC/82场景范围，未在本报告建立它的场景证据。测试清理完成后，仍需在完整可编译版本重新执行5项消息测试、受影响队列技术检查、逐UC、模块与全量。

## 报告滚动保留

本轮新增12份，按落盘时现有清单删除并备份超额12份。各组保留数量：`{"BATCH": 3, "UC-01": 3, "UC-02": 3, "UC-03": 2, "UC-04": 3, "UC-05": 3, "UC-06": 3, "UC-07": 3, "UC-08": 3, "UC-09": 3, "UC-10": 3, "UC-11": 3, "UC-12": 3, "UC-13": 0}`。其他领域、非规范命名及并行UC13报告不在本次删除范围；历史未解决CE-01已带入本次每份报告。
