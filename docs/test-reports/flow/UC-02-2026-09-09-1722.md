# UC-02 用户启动、查询与取消 Flow：默认队列切换 Pulsar 后的真实回归

## 基本信息与范围

时间 2026-09-09-1722 Asia/Shanghai；代码 5ce8b478676dd17c215bec83427bceb494cebff6 **dirty**。目标 [UC-02 用户启动、查询与取消 Flow.md](</System/Volumes/Data/workspace/java/flow/docs/uc/flow/UC-02 用户启动、查询与取消 Flow.md>)（ACCEPTED）；UC SHA256 `97e9be0b54ec52f05066607c805914b3915726c52d691be100a05023cba05cb6`。唯一主类 [Uc02ExecutionLifecycleTest](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java)，SHA256 `b30ad245fabef2ea9627f10adb21e84a777b78c34652d1da89f92863a470b3c2`。

本 Test Agent 保留 UC/主类预期，仅修改共用 WorkflowUcFixture 使实际执行走 Pulsar 并管理专属环境；另新增普通技术测试/辅助类和迁移必要架构断言。没有修改生产实现或 UC。相对前次报告已有的 Pause 字段、业务输入及 lineage 改动按当前源码保留，不归入队列实现变更。

独立命令 `./gradlew :core:test --rerun --tests '*Uc02ExecutionLifecycleTest'`，追加公共选项；日志 `/System/Volumes/Data/workspace/java/flow/core/build/pulsar-replacement-20260909/Uc02ExecutionLifecycleTest.log`；XML `/System/Volumes/Data/workspace/java/flow/core/build/pulsar-replacement-20260909/Uc02ExecutionLifecycleTest/core/test/xml/TEST-org.cses.flow.core.services.executions.Uc02ExecutionLifecycleTest.xml`（timestamp `2026-09-09T09:06:49.430Z`），10 tests / 10 passed / 0 failed / 0 skipped。之后单方法复验/模块/完整回归在编译阶段阻断，未再次执行。未补齐的历史断言缺口仍标 NOT_COVERED。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 用户启动并完成当前版本 | [s1UserStartsCurrentVersionAndCompletesItThroughPause](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java:36) | PASS | JUnit PASS。发布 v2，公开启动并查询到同一版本 Pause；APPROVED 恢复后 SUCCESS、三条历史、无活动或等待。 |
| S2 新版本发布前后的 Flow 分别完整运行 | [s2OldAndNewVersionsRunIndependently](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java:78) | PASS | JUnit PASS。旧 v2 与新 v4 独立运行，重建上下文分别恢复；OLD/NEW 输出、ID 与 TaskRun 历史隔离，均 SUCCESS。 |
| S3 用户取消准确的目标 Flow | [s3CancelTargetsOnlyTheSelectedExecution](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java:125) | PASS | JUnit PASS。只取消指定实例，根与 Pause KILLED；合法对照继续恢复至 SUCCESS，无等待。 |
| S4 用户启动不可用或不可见的 Flow | [s4UnavailableOrInvisibleFlowsAreRejectedWithoutRuns](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java:166) | PASS | JUnit PASS。未发布、删除、缺失及异租户 Flow 启动均拒绝；删除版本 v3 不覆盖历史 v2，无运行实例。 |
| S5 用户取消不存在、越权或已经结束的 Flow | [s5InvalidCancellationKeepsTerminalAndControlExecutionsUnchanged](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java:229) | NOT_COVERED | JUnit PASS。缺失/异租户/已完成/已取消实例的取消均拒绝；已完成与已取消 current 不变。 覆盖缺口：只比较终态 current，未比较拒绝前后的完整任务历史/输入/输出；合法对照被取消而未按 UC 完成，异租户等待实例未通过用户操作收尾。 |
| S6 Flow 处于 Pause 期间发布新版本 | [s6PausedOldVersionDoesNotSwitchAfterNewVersionIsPublished](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java:289) | PASS | JUnit PASS。旧 Pause 绑定 v2，新版发布为 v4；重启后旧版两条历史、新版三条历史，均完成且不切版本。 |
| S7 恢复 Pause 与取消 Flow 发生竞争 | [s7ResumeAndCancelRaceProducesOneConsistentTerminalResult](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java:329) | NOT_COVERED | JUnit PASS。两线程由闩锁同时提交恢复和取消，至少一方受理；公开查询最终 SUCCESS 或 KILLED，根和 Pause 一致、无活动或等待。 覆盖缺口：真实双线程恢复/取消竞争已构造，但只核对最终根/ Pause 状态与无活动任务，未断言至多一次推进及完整历史没有交错、重复或覆盖。 |
| S8 同一 Flow 的两个运行实例独立完成 | [s8TwoExecutionsOfOneFlowKeepIndependentCompleteHistories](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java:389) | NOT_COVERED | JUnit PASS。两个公开启动的实例保持不同 ID、同 v2、各三条且互异 TaskRun ID，均 SUCCESS、无活动。 覆盖缺口：两个实例的 ID、版本、终态、三条任务数量和互异 TaskRun ID 已检查；未设置可区分输入/输出，也未比较每条历史完整内容和归属。 |
| S9 系统重启后用户启动已发布 Flow | [s9FreshServerStartsPersistedPublishedFlowInOrder](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java:431) | PASS | JUnit PASS。真实关闭并重建上下文，按 key 读取持久化 v2；三步顺序准确、各 SUCCESS、无活动。 |
| S10 用户明确选择历史正式版本启动 | [s10UserExplicitlyStartsHistoricalPublishedVersion](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java:479) | PASS | JUnit PASS。当前 v4 后显式选择历史 v2，暂停与恢复保持 v2，全部执行 ID 属于历史定义、不混入 v4，V2-HISTORY 输出准确，最终完成。 |

## 数量汇总与结论

10 场景：PASS 7 / FAIL 0 / NOT_COVERED 3 / BLOCKED 0。该 UC 未达到全部场景验收通过，失败或缺口见表。 这不表示全项目测试通过。

## 环境、命令与证据

本轮使用真实 Micronaut、PAAS Client/Factory/PulsarConsumerRunner、Pulsar 4.0.7 standalone 和 PostgreSQL 17。默认 Executor 的命令/事件均由注解接入 Pulsar；没有使用 PostgreSQL DispatchQueue 或内存队列作为 UC 运行路径。旧 DispatchQueue 只保留独立适配器技术测试。宿主配置由环境变量传入，无新增队列 YAML。

```bash
export JAVA_HOME=/Users/cses-7/Library/Java/JavaVirtualMachines/ms-25.0.4/Contents/Home
export FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:55170/flow
export FLOW_POSTGRES_TEST_USER=flow
export FLOW_POSTGRES_TEST_CLEANUP=true
export FLOW_POSTGRES_DEFAULT_TEST_URL=
export FLOW_PULSAR_TEST_URL=pulsar://127.0.0.1:56650
export FLOW_PULSAR_TEST_ADMIN_URL=http://127.0.0.1:58081
# PostgreSQL 测试密码由 FLOW_POSTGRES_TEST_PASSWORD / FLOW_DATASOURCE_PASSWORD 注入
```

测试专属容器 `flow-pulsar-replacement-postgres-0909` 与 `flow-pulsar-replacement-broker-0909`，未使用已有业务库/Broker。Broker 镜像 digest `sha256:4614ddfddb711796314911fd1447d33523f2f2b498e56620dfdcdd627f94aeec`。Broker 容器内外均使用 56650，`advertised-address=127.0.0.1`；admin 映射 58081→8080，启动 `standalone --no-functions-worker --no-stream-storage`。完整可复用命令见 `docs/harness/pulsar-queues.md`。实际 JDK 25.0.4，不是 Java 21 验证证据。

所有 Gradle 顺序执行。脚本 `/private/tmp/flow-pulsar-replacement-run.sh`；公共选项 `-I /private/tmp/flow-pulsar-uc-isolated.gradle --offline --max-workers=1 --console=plain`，每阶段 `FLOW_TEST_RUN_DIR=/System/Volumes/Data/workspace/java/flow/core/build/pulsar-replacement-20260909/<阶段>`。init script 只隔离 binary/XML/HTML 并关闭标准流展示，不替换业务实现或断言。

| 阶段 | Wrapper 命令主体 | 结果 | 证据目录或日志 |
| --- | --- | --- | --- |
| 定向技术（包含编译） | `./gradlew :core:test --tests '*ExecutorPulsarIntegrationTest' --tests '*PulsarQueueTest'` | 9 tests / 9 passed / 0 failed / 0 skipped，exit 0 | `technical/`、`technical.log` |
| 生产闭环与架构 smoke | `./gradlew :core:test --tests '*Uc04PauseFlowTest.s1UserQueriesAndResumesOnePausedFlow' --tests '*CoreArchitectureStandardTest.executorUsesTheAnnotationDrivenPulsarQueues' --tests '*CoreArchitectureStandardTest.queueContractsRemainTopLevelPeers'` | 3 tests / 3 passed / 0 failed / 0 skipped，exit 0 | `smoke2/`、`smoke2.log` |
| 首次业务失败复验 | `./gradlew :core:test --rerun --tests '*Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance'` | not_run（core:compileJava blocked），exit 1 | `retry-Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance/` |
| Core 模块 | `./gradlew :core:test --rerun` | not_run（core:compileJava blocked），exit 1 | `core/`、`core.log` |
| 全项目 | `./gradlew test --rerun --continue` | Core/Server：not_run（core:compileJava blocked），exit 1 | `full/`、`full.log` |

上表相对证据路径均位于 `/System/Volumes/Data/workspace/java/flow/core/build/pulsar-replacement-20260909`；XML 路径为 `<阶段>/<模块>/test/xml/TEST-*.xml`。新增架构检查最初缺少静态 assertFalse 导入，仅测试编译失败（`smoke.log`），修正后 smoke2 通过；不把未运行计为失败业务场景。

## 编译阻断与快照边界

全部独立 UC 完成后，另一项并行任务把 RunnableTask.run 的返回类型改为 Output，而 WorkerDispatcher.java:20 仍接收 RunResult。S8 复验、Core 和全项目三次 Wrapper 命令均报“推论变量 T 具有不兼容的上限：RunResult/Object；下限：Output”，停在 core:compileJava；因此三个阶段都是 not_run/compile blocked，没有新的模块或全量测试统计。日志保留完整编译错误。

主 Agent 保存的 10 项队列生产 SHA256 核对全部通过；Test Agent 未修改/回退并行任务源码。独立 UC 与 9 项专项属于编译前快照，不能说明当前整个工作区可编译或通过。最后仅在专项测试 finally 增补清理已有生产订阅积压，避免真实固定 Topic 测试消息影响后续运行；这个测试清理调整尚未重新执行，因为当前生产编译阻断。测试与报告静态 diff --check 通过。

| 类与方法 | 本轮状态 |
| --- | --- |
| `core:compileJava` | 编译阻断：WorkerDispatcher.java:20，Output 不能赋给 RunResult；没有运行测试。 |

本轮模块/全量没有执行，不能重新确认旧普通技术失败或断言其消失。之前报告中的 State、旧架构路径、NestedPause、Log 差异仅作为历史待处理事项。新架构方法已在编译前 smoke 中验证两条注解绑定及 Queue 发布接口；旧缺失路径检查仍保留，未修旧实现或改变 UC 预期。

`skipped`：已执行独立 UC、技术与 smoke 阶段均为 0。双数据库隔离所需 FLOW_POSTGRES_DEFAULT_TEST_URL 未配置，但本轮完整测试因编译阻断未执行到该测试，不能记作实际 skipped。`not_run`：默认 opt-in queue-load、负载、集群故障、真实外部 HTTP/界面与通知验收。所有选定 UC 主类均已执行，不能把 skipped 或未运行项记为通过。S8单方法/Core/full 的退出码均为1，测试均 not_run，不把编译阻断算作已执行业务场景 FAIL。

## 失败、反例与未解决事项

CE-01（既有、未解决）：UC04 S8 通过公开 Service 发布 Pause → ThrowingTask → never-run，启动并查询 Pause，提交 `{"decision":"APPROVED"}`；后置任务抛出 `uc04-downstream-failure`。UC 要求明确错误、回到提交前 PAUSED 且不留下部分后置处理，随后由用户取消。实际执行进入 FAILED，断言 `expected: <PAUSED> but was: <FAILED>`。本轮真实 Pulsar 下整类执行保留该差异；随后单方法复验、Core 和完整回归均在编译阶段阻断，没有再次运行。用户取消收尾未执行。复现：`./gradlew :core:test --rerun --tests '*Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance'`（使用下述环境和公共选项）。物理清理不补足用户闭环。历史 BATCH-2026-09-09-1629.md 在替换前也记录同一差异；没有做 git 二分，不把历史对照视为严格归因。

- S5 用户取消不存在、越权或已经结束的 Flow：只比较终态 current，未比较拒绝前后的完整任务历史/输入/输出；合法对照被取消而未按 UC 完成，异租户等待实例未通过用户操作收尾。
- S7 恢复 Pause 与取消 Flow 发生竞争：真实双线程恢复/取消竞争已构造，但只核对最终根/ Pause 状态与无活动任务，未断言至多一次推进及完整历史没有交错、重复或覆盖。
- S8 同一 Flow 的两个运行实例独立完成：两个实例的 ID、版本、终态、三条任务数量和互异 TaskRun ID 已检查；未设置可区分输入/输出，也未比较每条历史完整内容和归属。

## 活动资源、清理和验证边界

最终 PostgreSQL 只读审计：

```text
BEGIN
executions_total=0, active=0
task_runs_total=0, paused=0, other_active=0
inherited_total=0, paused=0, other_active=0
queues_total=0
flows_total=0
flow_tasks_total=0
COMMIT
```

最终真实 Broker 只读审计：

```json
{
  "command": {
    "messagesIn": 208,
    "messagesOut": 208,
    "publishers": 0,
    "subscriptions": {
      "flow-executor-command": {
        "backlog": 0,
        "unacked": 0,
        "consumers": 0
      }
    }
  },
  "event": {
    "messagesIn": 818,
    "messagesOut": 818,
    "publishers": 0,
    "subscriptions": {
      "flow-executor-event": {
        "backlog": 0,
        "unacked": 0,
        "consumers": 0
      }
    }
  }
}
```

证据 `/System/Volumes/Data/workspace/java/flow/core/build/pulsar-replacement-20260909/resources.sql`、`resources.log`、`broker-*.json`。Fixture 重启保留同一持久订阅，最终关闭真实消费者/生产者/Client 后清理两条测试订阅积压，再按随机 companyId 清理 PostgreSQL。失败或拒绝命令的积压物理清理不能作为用户终态证据。两个本次专属容器及匿名卷已删除，其他容器未操作，删除输出在 `container-cleanup.log`。

真实技术测试证实四种 ExecutionCommand 与 ExecutorEvent 经 PAAS schema 往返、Queue/PulsarQueue 同实例注入、回调未完成时 Broker backlog 保留、跨队列发布、抛错一次后 Broker 重投和最终 ACK 后积压及未确认数清零。原 7 项受控网络替身测试仍单独存在，不能代替真实 Broker 结果。尚未验证集群故障转移、Broker 重启恢复或 exactly-once；PAAS 异步 ACK 失败/在途关闭的已有边界不因本次替换消失。

## 本轮滚动保留

新增 12 份，按写入时当前目录清单备份并删除超额 12 份。保留数量：`{"BATCH": 3, "UC-01": 3, "UC-02": 3, "UC-03": 2, "UC-04": 3, "UC-05": 3, "UC-06": 3, "UC-07": 3, "UC-08": 3, "UC-09": 3, "UC-10": 3, "UC-11": 3, "UC-12": 3}`。非规范时间格式与其他领域文件保持不变。未解决 CE-01 已完整带入每份报告。
