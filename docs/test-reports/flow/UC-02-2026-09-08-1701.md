# UC-02 用户启动、查询与取消 Flow测试报告

## 基本信息

- UC：[UC-02 用户启动、查询与取消 Flow](../../uc/flow/UC-02%20%E7%94%A8%E6%88%B7%E5%90%AF%E5%8A%A8%E3%80%81%E6%9F%A5%E8%AF%A2%E4%B8%8E%E5%8F%96%E6%B6%88%20Flow.md)，S1–S10；索引状态 ACCEPTED。
- 唯一主测试类：[Uc02ExecutionLifecycleTest](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc02ExecutionLifecycleTest.java)；本轮没有修改主测试类或 UC。
- 日期：2026-09-08，Asia/Shanghai；本类 JUnit 开始时间 `2026-09-08T08:38:04.414Z`（UTC），用时 24.396 秒。
- 代码版本：`64d381a425143f1a1d71ec8ee200a5880c3f5c26 + dirty`；包含本次最终作用域 CAS / `lock` 改造。
- 环境：Java 25.0.4、Gradle wrapper 9.5.1、真实 Micronaut 装配和 PostgreSQL 17.10；唯一数据库 `127.0.0.1:64628/flow`，容器 `flow-cas-20260908`。数据库测试未指向或清理默认 5432 实例。Java 21 没有本轮通过证据。

## 测试范围

通过公开 FlowService / ExecutionService 启动、选择当前或历史正式版本、查询、恢复 Pause、取消及跨租户隔离；包括真实两线程恢复/取消竞争、重启与多实例。

所有现有场景均有唯一、未禁用的测试方法；没有新增、失效或错误归属方法。逐预期审计与方法运行结果分别统计，覆盖缺口保留为 NOT_COVERED，不以绿色方法替代完整验收。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 用户启动并完成当前版本 | `s1UserStartsCurrentVersionAndCompletesItThroughPause`（源码 36 行） | PASS | JUnit PASS。只创建一个 v2 实例；查询 Pause、提交结果并再次查询，根和 Pause 结果正确，SUCCESS 且无活动步骤。 |
| S2 新版本发布前后的 Flow 分别完整运行 | `s2OldAndNewVersionsRunIndependently`（源码 78 行） | PASS | JUnit PASS。草稿/发布依次 v1/v2/v3/v4；旧新实例绑定 v2/v4，Pause 输出 OLD/NEW 与 TaskRun ID 集合隔离，均 SUCCESS。 |
| S3 用户取消准确的目标 Flow | `s3CancelTargetsOnlyTheSelectedExecution`（源码 125 行） | PASS | JUnit PASS。只取消目标并查询 KILLED；对照独立恢复至 SUCCESS，最后没有等待步骤。 |
| S4 用户启动不可用或不可见的 Flow | `s4UnavailableOrInvisibleFlowsAreRejectedWithoutRuns`（源码 166 行） | PASS | JUnit PASS。未发布、删除、不存在、越租户启动均拒绝；每次查询无实例或等待；历史正式版本与删除记录区分。 |
| S5 用户取消不存在、越权或已经结束的 Flow | `s5InvalidCancellationKeepsTerminalAndControlExecutionsUnchanged`（源码 229 行） | NOT_COVERED | JUnit PASS。无效取消方法已执行，终态检查通过；完整 UC 证据缺口见下文。 仅断言错误取消后的 current state；未比较完整历史，也未按 UC 恢复其他租户的合法对照实例。fixture 清理不能替代场景完成证据。 |
| S6 Flow 处于 Pause 期间发布新版本 | `s6PausedOldVersionDoesNotSwitchAfterNewVersionIsPublished`（源码 289 行） | PASS | JUnit PASS。旧实例始终 v2、新实例 v4，TaskRun 数分别 2/3，重启后分别恢复至 SUCCESS。 |
| S7 恢复 Pause 与取消 Flow 发生竞争 | `s7ResumeAndCancelRaceProducesOneConsistentTerminalResult`（源码 329 行） | NOT_COVERED | JUnit PASS。两个线程真实竞争，至少一个受理；最终根与 Pause 为一致 SUCCESS/KILLED，无活动步骤；历史缺口见下文。 已真实并发并比较根与 Pause 的最终状态；没有断言两个状态历史中仅一个操作生效，不能证明不存在矛盾历史。 |
| S8 同一 Flow 的两个运行实例独立完成 | `s8TwoExecutionsOfOneFlowKeepIndependentCompleteHistories`（源码 389 行） | NOT_COVERED | JUnit PASS。两个独立实例编号及 TaskRun ID 集合不同，各 3 个步骤，均 SUCCESS 无活动步骤；输入输出缺口见下文。 已检查两个运行 ID、TaskRun ID 集合、数量和终态；缺少各自输入、输出及相互独立的完整结果断言。 |
| S9 系统重启后用户启动已发布 Flow | `s9FreshServerStartsPersistedPublishedFlowInOrder`（源码 431 行） | PASS | JUnit PASS。重建 ApplicationContext 后同一正式定义/v2 可查；三个 Task 按定义顺序各一次 SUCCESS，无活动步骤。 |
| S10 用户明确选择历史正式版本启动 | `s10UserExplicitlyStartsHistoricalPublishedVersion`（源码 479 行） | PASS | JUnit PASS。当前 v4、历史 v2 均可查；明确选择 Optional.of(2L)，仅创建 v2 实例，步骤与 v4 无交集，输出 V2-HISTORY，最终 SUCCESS。 |

## 执行命令与结果

使用以下配置（口令仅通过环境注入，不写入报告）：

```bash
export JAVA_HOME=/Users/cses-7/Library/Java/JavaVirtualMachines/ms-25.0.4/Contents/Home
export FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:64628/flow
export FLOW_POSTGRES_TEST_USER=flow
export FLOW_POSTGRES_TEST_CLEANUP=true
# FLOW_POSTGRES_TEST_PASSWORD 由本地测试环境提供
```

1. `./gradlew :core:testClasses --console=plain --offline --max-workers=1`：exit 0，编译成功。
2. `./gradlew :core:test --tests org.cses.flow.core.services.executions.Uc02ExecutionLifecycleTest --console=plain --offline --max-workers=1`：exit 0，10 tests，10 PASS、0 FAIL、0 SKIPPED，24.396 秒。

随后执行 `./gradlew :core:test --console=plain --offline --max-workers=1`：exit 1，399 tests，389 PASS、9 FAIL、1 SKIPPED，4 分 15 秒。最后以共享任务图执行 `./gradlew test build --continue --console=plain --offline --max-workers=1`：exit 1，7 分 28 秒；core 再次 399 tests，389 PASS、9 FAIL、1 SKIPPED，本 UC 方法结果与定向一致。

Server AOT、打包及 assemble 成功，但 `server:compileTestJava` 在 `AdminSessionArgumentBinderTest.java:6` 因缺少 `org.paas.session.Device` 失败，Server 测试未运行；整体 build 不通过。已有 Server XML 时间为 2026-09-01，不计入本轮。gen:test 为 NO-SOURCE。core 的唯一 SKIPPED 是缺少第二个数据源配置的 FlowDatabaseIntegrationTest，不是本 UC 场景。

原始证据目录：`core/build/cas-uc-regression-e6X33w`。本类快照 `Uc02ExecutionLifecycleTest-results/TEST-org.cses.flow.core.services.executions.Uc02ExecutionLifecycleTest.xml`；模块快照 `core-results/`；最终快照 `full-test-build-results/` 与 `full-test-build.log`。本报告已写明关键结果，不依赖更早报告。

## 失败与反例

本 UC 没有观察到稳定违反用户预期的生产反例；本轮存在的证据缺口（如有）不分配 CE。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 10 | 7 | 0 | 3 | 0 |

JUnit 主类：10 方法，10 PASS、0 FAIL、0 SKIPPED；这不是上表的 UC 验收数量。

## 活动资源与滚动保留

2026-09-08 17:02:02 Asia/Shanghai，在完整 test/build 结束后，只读查询隔离容器 `flow-cas-20260908`：`company_id LIKE 'uc-%'` 的 Flow、FlowTask、Execution、TaskRun、Queue 均为 0；整个隔离库 Execution=0（活动=0）、TaskRun=0（PAUSED=0）、Queue=0。所有 UC fixture 已关闭并按所属租户清理，未删除其他数据库数据或停止容器。

物理清理的 0 不替代逐场景的业务结束断言；表中 NOT_COVERED 仍保持未覆盖。上述为 Test Agent 核对时状态；随后主 Agent 已删除本次隔离容器及专属测试卷，默认开发库保留。

本批新增 9 份 UC 报告与 1 份 BATCH。按 `^(UC-\d+|BATCH)-\d{4}-\d{2}-\d{2}-\d{4}\.md$` 识别并滚动保留最新 3 份；只删除 `UC-04-2026-08-31-1253.md`，未解决 CE-01 已完整带入本批。清理后各活跃 UC 组及 BATCH 各 3 份，历史 UC-03 组 2 份，共 32 份匹配报告；另保留 9 份非匹配旧 HHmmss 文件，总计 41 份。

## 测试结论

UC-02 尚未完整验收：3 个场景缺少充分证据。

## 未解决事项

- S5：仅断言错误取消后的 current state；未比较完整历史，也未按 UC 恢复其他租户的合法对照实例。fixture 清理不能替代场景完成证据。
- S7：已真实并发并比较根与 Pause 的最终状态；没有断言两个状态历史中仅一个操作生效，不能证明不存在矛盾历史。
- S8：已检查两个运行 ID、TaskRun ID 集合、数量和终态；缺少各自输入、输出及相互独立的完整结果断言。

- 项目回归仍有既有架构、State、NestedPause 和 Log 测试失败；未改写测试以隐藏失败。完整 test/build 命令退出 1：core 既有 9 个失败；server 测试编译在 AdminSessionArgumentBinderTest.java:6 缺少 org.paas.session.Device，server 行为测试未执行。FlowDatabaseIntegrationTest 因未提供独立的 FLOW_POSTGRES_DEFAULT_TEST_URL 而 SKIPPED；gen:test 为 NO-SOURCE。
- 主 Agent 本轮限定既有缺口只报告、不扩展修复；Test Agent 仅新增报告，复用原有真实数据库测试资源。
