# UC-01 Flow 草稿生命周期与多租户管理测试报告

## 基本信息

- UC 文档：`docs/uc/flow/UC-01 Flow 草稿生命周期与多租户管理.md`
- 主测试类：`server/src/test/java/org/cses/flow/core/services/flows/Uc01FlowLifecycleTest.java`
- 执行时间：`2026-07-29 17:07-17:16 CST`
- 代码版本：`4cce0b07544366cf9d74efb7534f46fb56048baa`，工作区 `dirty`
- 环境：Java 21、PostgreSQL 17.10、真实 Micronaut 装配；当前无 Flow HTTP Controller，使用公开 `FlowService`
- 主测试类是否修改：否；当前 13 个测试方法与 S1-S13 一一对应

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 | `s1UserCompletesTheSourceLifecycle` | PASS | 草稿编号稳定，修改后返回最新原文，正式 Flow 查询为空，删除后草稿不可见 |
| S2 | `s2UserRecoversFromAnInvalidDeployment` | PASS | 无效原文可保存；发布明确失败且不产生正式版本；草稿可继续修改并删除 |
| S3 | `s3TenantsManageIdenticalSourcesIndependently` | PASS | 两个租户相同原文的数据、修改与删除互相隔离，最终均无草稿 |
| S4 | `s4UsersInOneTenantPreserveCreationAudit` | PASS | 同租户编辑不改变创建人/创建时间，最新编辑人、时间和内容正确 |
| S5 | `s5WrongSourceIdsDoNotAffectTheControlSource` | PASS | 不存在或错误编号不返回其他资源，对照草稿保持不变并正常删除 |
| S6 | `s6MissingTenantIdentityCannotManageSources` | PASS | 无租户身份的创建、查询、修改、删除均被拒绝且无副作用 |
| S7 | `s7AnotherTenantCannotQueryTheSource` | PASS | 其他租户查询不可见，所有者草稿保持不变并完成删除 |
| S8 | `s8AnotherTenantCannotModifyTheSource` | PASS | 跨租户修改被拒绝，原文与审计信息不变 |
| S9 | `s9AnotherTenantCannotDeleteTheSource` | PASS | 跨租户删除被拒绝，所有者仍可查询、修改并删除 |
| S10 | `s10AnotherTenantCannotDeployTheSource` | PASS | 跨租户发布被拒绝，不产生正式版本，原草稿不变 |
| S11 | `s11DeletedSourceCannotBeModifiedOrDeployed` | PASS | 已删除草稿不能恢复、修改或发布，正式版本仍为空 |
| S12 | `s12SourceAndDeployedFlowQueriesStaySeparate` | PASS | 正式 Flow 查询不返回草稿，草稿查询仍返回原文，最后删除 |
| S13 | `s13SystemFieldsRemainOpaqueSourceAndCannotBeInjected` | PASS | 用户填写的系统字段仅保留在原文中，不能注入系统身份、版本、状态或审计数据，发布失败后删除 |

## 执行命令与结果

- `./gradlew :server:test --tests '*Uc01FlowLifecycleTest' --console=plain`：首次因测试库旧结构缺少 `flow_drafts.raw` 而 13/13 环境失败。
- 应用 `gen/sql/production-release/flow/2026-07-29/001_align_flow_source_and_reversion.sql`；旧 `ck_flows_status` 阻止 `CLOSE` 更新为 `CLOSED`，在测试库移除旧约束并转换数据后完整重跑迁移成功。
- 相同 UC 命令重跑：13 tests，0 failures，`BUILD SUCCESSFUL`。
- 模块、完整测试和构建回归均只失败于 UC-04 S3，与本 UC 无关。

## 失败与反例

无 UC-01 失败或反例。首次失败属于测试数据库版本滞后，不计入场景结论。

## 数量汇总

- PASS：13
- FAIL：0
- NOT_COVERED：0
- BLOCKED：0

## 测试结论

PASS。UC-01 的 13 个已接受场景均满足预期。

## 未解决事项

生产发布迁移脚本在仍保留旧 `ck_flows_status` 时先更新状态、后删除约束；本次需手工调整测试库顺序，建议单独复核迁移升级路径。
