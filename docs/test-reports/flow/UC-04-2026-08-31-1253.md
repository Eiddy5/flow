# UC-04 用户恢复处于 Pause 的 Flow 测试报告

## 基本信息

- UC：`docs/uc/flow/UC-04 用户恢复处于 Pause 的 Flow.md`
- 主测试类：`core/src/test/java/org/cses/flow/core/services/executions/Uc04PauseFlowTest.java`
- 主测试类修改：是，新增 S10/S11，并为 S1 补充正常 Execution 无当前退回片段的断言
- 测试辅助修改：`WorkflowUcFixture` 新增通过 `ExecutionService.rewind(...)` 提交命令并等待公开状态变化的辅助方法
- 执行时间：2026-08-31 12:51～13:02 +08:00
- 代码版本：`c50ff0d`，工作区 dirty（包含本功能生产实现、UC、测试和用户既有修改）
- 环境：Java 21、真实 Micronaut 装配、PostgreSQL 17.10，`jdbc:postgresql://localhost:55440/flow`
- 公开入口：`FlowService`、`ExecutionService`、持久化重启后的 `ExecutionService.execution(...)`
- 清理：`FLOW_POSTGRES_TEST_CLEANUP=true`

## 测试范围

本次同步并重点验证 S10/S11，同时整类执行 UC-04 全部 11 个场景。所有写操作均经
公开 Service 和 Dispatch Queue；没有直接调用 Repository、Executor 或领域状态方法。
S10/S11 通过关闭并重建 `ApplicationContext` 验证 Execution Generation 的 PostgreSQL
往返。另以最小领域测试验证完成回退后旧片段 WARNING/输出不再属于最终有效结果。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 查询并恢复单个 Pause | `s1UserQueriesAndResumesOnePausedFlow` | PASS | 查询到唯一 Pause；正常执行的 `generation.current` 为空且 history 为空；恢复结果被后续步骤读取；Execution SUCCESS，等待步骤 0。 |
| S2 只推进当前 Flow | `s2ResumeResultIsConsumedOnceByOnlyTheCurrentFlow` | PASS | 目标 Pause 与后续步骤各一次，结果只进入当前 Execution；最终 SUCCESS，等待步骤 0。 |
| S3 非法输入后正确恢复 | `s3IllegalResumeInputsAreRejectedUntilValidResultIsSubmitted` | PASS | 三类非法请求均被拒绝且每次重查仍为 PAUSED、TaskRun 数量与输出不变；合法恢复后 SUCCESS。 |
| S4 重复恢复结束 Pause | `s4RepeatedResumeOfFinishedPauseIsRejectedWithoutMutation` | PASS | 第二次恢复被拒绝；状态、TaskRun ID/数量和原结果不变，Execution 保持 SUCCESS。 |
| S5 其他租户查询或恢复 | `s5OtherTenantCannotQueryOrResumeThePausedFlow` | PASS | 其他租户查询为空且恢复被拒绝；原租户重查无变化并正常完成。 |
| S6 两个实例精确选择 | `s6SameUserResumesTheSelectedInstanceOnly` | PASS | 先恢复第二个实例时第一个仍 PAUSED；重新查询后恢复第一个，两个实例均 SUCCESS。 |
| S7 取消后恢复原 Pause | `s7CancelledFlowRejectsResumeOfItsFormerPause` | PASS | 取消后恢复被拒绝，Execution 保持 KILLED，等待步骤 0。 |
| S8 后续处理异常 | `s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance` | FAIL | 两次真实执行均在恢复后的公开查询得到 Execution=`FAILED`；UC 预期回到原 Pause=`PAUSED`。见 CE-01。 |
| S9 重启后继续恢复 | `s9FreshServerCanRequeryAndResumeTheSamePause` | PASS | 新 `ApplicationContext` 从 PostgreSQL 查询到同一 Execution/Pause，恢复后 SUCCESS，等待步骤 0。 |
| S10 从 Pause 退回指定历史步骤 | `s10UserRewindsFromPauseToHistoricalStepAndContinues` | PASS | 当前版本为 1，源/目标 TaskRun ID 和原因精确可查；只有 target→source Pause 重跑，片段前步骤仍一次、下游未提前执行；重启后 Current 完整恢复；最终 current 为空、history 保留一条、下游读取重跑 token，等待步骤 0。 |
| S11 在新 Pause 再次退回 | `s11UserRewindsAgainFromTheNewPause` | PASS | 第二次 Current 版本为 2，以新 Pause 和第二次选择的目标为边界；history 保留 v1 的源/目标/原因；片段执行版本为 null/1/2，片段前步骤仍一次；最终 current 为空、history 保留 v1/v2，下游读取 v2 最新 token；重启后历史保持，等待步骤 0。 |

## 执行命令与结果

以下命令均设置 `FLOW_POSTGRES_TEST_URL=jdbc:postgresql://localhost:55440/flow`、
`FLOW_POSTGRES_TEST_USER=flow`、`FLOW_POSTGRES_TEST_PASSWORD=flow` 和
`FLOW_POSTGRES_TEST_CLEANUP=true`：

1. `./gradlew :core:compileTestJava`：exit 0。
2. `./gradlew :core:test --tests 'org.cses.flow.core.services.executions.Uc04PauseFlowTest.s10UserRewindsFromPauseToHistoricalStepAndContinues'`：exit 0，1/1 PASS。
3. `./gradlew :core:test --tests 'org.cses.flow.core.services.executions.Uc04PauseFlowTest.s11UserRewindsAgainFromTheNewPause'`：exit 0，1/1 PASS。
4. `./gradlew :core:test --tests 'org.cses.flow.core.services.executions.Uc04PauseFlowTest'`：exit 1，11 tests completed，10 PASS、S8 FAIL。
5. `./gradlew :core:test --tests 'org.cses.flow.core.services.executions.Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance'`：exit 1，单独复验仍 FAIL。
6. `./gradlew :core:test --tests 'org.cses.flow.core.domains.executions.ExecutionTest' --tests 'org.cses.flow.core.domains.executions.GenerationTest' --tests 'org.cses.flow.infrastructure.repositories.executions.codec.GenerationJsonCodecTest' --tests 'org.cses.flow.executor.commands.RewindTest'`：exit 0，17/17 PASS；其中完成回退后的有效结果断言确认旧 WARNING/旧输出被排除、最新代输出保留。
7. 根工程 `./gradlew test`：exit 1，376 tests completed，5 failed，1 skipped；UC-04
   S8 为本报告 CE-01，UC-04 其余十个场景（含 S10/S11）均通过。其余四个失败属于
   本 UC 之外的回归观察，详见批量报告。

## 失败与反例

### CE-01 Pause 恢复后的后置异常没有回滚到原等待点

- 场景：UC-04 S8。
- 用户输入：在唯一 Pause 上通过 `ExecutionService.resume(...)` 提交
  `{"decision":"APPROVED"}`；后续公开 RunnableTask 抛出
  `uc04-downstream-failure`。
- 预期：异常明确返回，Execution 和原 Pause 回到提交前 PAUSED，且不产生部分后续结果。
- 实际：异步处理后 `ExecutionService.execution(...)` 查询到 Execution=`FAILED`；
  测试在首个状态断言 `expected PAUSED but was FAILED` 处失败。
- 稳定性：整类执行失败一次，随后同一方法单独复验再次失败。
- 影响：UC-04 不能整体验收通过；S10/S11 的 rewind 路径不受该反例影响并已分别通过。
- 处理：Test Agent 未修改生产实现或放宽断言。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 11 | 10 | 1 | 0 | 0 |

## 测试结论

**FAIL**。新增退回场景 S10/S11 和正常 Generation 语义均通过真实 PostgreSQL 与服务
重启验证，且旧代 WARNING/输出排除边界通过；但 UC-04 S8 存在稳定反例 CE-01。

## 活动资源与未解决事项

- 本轮夹具全部关闭，且 cleanup=true 删除本轮随机租户数据；最终仍在运行的本轮
  Execution=0，等待恢复的本轮 TaskRun=0。
- 未解决：CE-01。
- 全量回归的 UC 外 REG-01～REG-03 见同批次报告。
