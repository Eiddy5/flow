# Flow 全场景功能测试批次报告

## 基本信息

- 执行时间：2026-08-28 18:40～19:05 +08:00
- 代码版本：`6e80de1`；测试启动前工作区 clean，报告文件为本轮新增内容
- 环境：Java 21.0.12、真实 Micronaut 装配、PostgreSQL 17.10、本地 `flow` 数据库
- 范围：`docs/uc/flow/README.md` 中全部 9 个 `ACCEPTED` UC，共 63 个场景
- 推荐顺序：UC-01 → UC-02 → UC-03 → UC-04 → UC-06 → UC-09 → UC-05 → UC-07 → UC-08
- 约束：未修改生产代码、UC 预期或测试断言；未调用其他 Agent
- 公开入口核对：Core 使用 `FlowService`、`ExecutionService`；HTTP 入口为 `server` 模块的 `FlowController`。UC-02/04/05 的任务协作闭环当前没有符合规范的公开入口证据。

## UC 场景汇总

| UC | 主测试类 | 场景数 | PASS | FAIL | NOT_COVERED | BLOCKED |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| UC-01 | `Uc01FlowLifecycleTest` | 13 | 11 | 0 | 2 | 0 |
| UC-02 | `Uc02ExecutionLifecycleTest` | 9 | 2 | 0 | 7 | 0 |
| UC-03 | `Uc03AutomaticTaskFlowTest` | 3 | 3 | 0 | 0 | 0 |
| UC-04 | 无 | 9 | 0 | 0 | 9 | 0 |
| UC-05 | 无 | 6 | 0 | 0 | 6 | 0 |
| UC-06 | `Uc06ConditionalRouteTest` | 7 | 1 | 0 | 6 | 0 |
| UC-07 | `Uc07NestedTaskFlowTest` | 7 | 1 | 0 | 6 | 0 |
| UC-08 | `Uc08DynamicLogFlowTest` | 3 | 3 | 0 | 0 | 0 |
| UC-09 | `Uc09LoopOrchestrationTest` | 6 | 6 | 0 | 0 | 0 |
| 合计 |  | 63 | 27 | 0 | 36 | 0 |

`NOT_COVERED` 表示没有符合 Test Agent 契约的唯一主测试方法或公开用户闭环，不等同于实现失败。UC-01 S8、S13 的现有方法虽各自测试通过，但操作路径与当前 UC 预期不一致，故保持 NOT_COVERED。

## 主测试类与执行结果

所有已建立的主测试类均未修改，并使用真实 PostgreSQL 单独执行：

| 主测试类 | 执行结果 |
| --- | --- |
| `Uc01FlowLifecycleTest` | 13 tests passed，0 failed |
| `Uc02ExecutionLifecycleTest` | 2 tests passed，0 failed |
| `Uc03AutomaticTaskFlowTest` | 3 tests passed，0 failed |
| `Uc06ConditionalRouteTest` | 1 test passed，0 failed |
| `Uc07NestedTaskFlowTest` | 1 test passed，0 failed |
| `Uc08DynamicLogFlowTest` | 3 tests passed，0 failed |
| `Uc09LoopOrchestrationTest` | 6 tests passed，0 failed |

UC-04、UC-05 没有合规主测试类，没有伪造执行证据。没有主类断言失败，因此没有“首次失败单独复验”命令。

## 所有命令及结果

1. `./gradlew :core:compileTestJava`：exit 0，目标测试编译成功。
2. `FLOW_POSTGRES_TEST_URL=jdbc:postgresql://localhost:5432/flow FLOW_POSTGRES_TEST_USER=flow FLOW_POSTGRES_TEST_PASSWORD=flow ./gradlew :core:test --tests '*Uc01FlowLifecycleTest'`：exit 0，13/13 通过。
3. 同环境 `./gradlew :core:test --tests '*Uc02ExecutionLifecycleTest'`：exit 0，2/2 通过。
4. 同环境 `./gradlew :core:test --tests '*Uc03AutomaticTaskFlowTest'`：exit 0，3/3 通过。
5. 同环境 `./gradlew :core:test --tests '*Uc06ConditionalRouteTest'`：exit 0，1/1 通过。
6. 同环境 `./gradlew :core:test --tests '*Uc09LoopOrchestrationTest'`：exit 0，6/6 通过。
7. 同环境 `./gradlew :core:test --tests '*Uc07NestedTaskFlowTest'`：exit 0，1/1 通过。
8. 同环境 `./gradlew :core:test --tests '*Uc08DynamicLogFlowTest'`：exit 0，3/3 通过。
9. `FLOW_POSTGRES_TEST_CLEANUP=true` 同环境执行 `./gradlew :core:test --tests '*ExecutionPauseLifecycleIntegrationTest' --tests '*ExecutionResumeIntegrationTest' --tests '*ParallelPauseResumeIntegrationTest' --tests '*ConditionalRouteResumeIntegrationTest' --tests '*NestedPauseResumeIntegrationTest'`：exit 0，34/34 技术测试通过；不替代 UC-04/05/06/07 主场景证据。
10. `FLOW_POSTGRES_TEST_CLEANUP=true` 同环境执行 `./gradlew :core:test`：运行约 2 分钟后无新增测试结果进度，安全 Ctrl-C，exit 130。未出现 JUnit 失败汇总，标记为全量阶段 BLOCKED/中止。
11. 同环境 `./gradlew :server:test`：exit 0；server 编译成功，4 suites/9 tests 全部通过，包含 `FlowControllerTest` 6 tests 与 `ServerModuleArchitectureTest` 1 test。
12. `FLOW_POSTGRES_TEST_CLEANUP=true` 同环境执行精确 `./gradlew test`：进入 `:core:test` 后约 2 分钟无新增结果进度，安全 Ctrl-C，exit 130。未出现 JUnit 失败汇总，标记为全项目阶段 BLOCKED/中止。
13. 只读 PostgreSQL JDBC 盘点：执行状态为 `FAILED=6`、`SUCCESS=9`，没有 `CREATED/RUNNING/PAUSED/RESTARTED/KILLING`；TaskRun 状态为 `FAILED=6`、`KILLED=4`、`SUCCESS=53`；`queues` 无行。
14. 进程盘点：无残留 `gradlew`、Gradle Test Executor 或 Flow 队列 worker。

## 失败反例与 CE

- UC 场景断言 FAIL：0。
- 稳定失败 `CE-*`：0。
- 未将全量命令的中止误记为断言 FAIL；阻塞命令为 `./gradlew :core:test` 和 `./gradlew test`，均 exit 130，原因是长时间无新测试结果进度。

## 非失败告警

- Logback 尝试写入 `/var/log/cses/cses.log` 时父目录不存在，产生 appender 错误日志，不影响测试断言。
- 多个 Micronaut 测试上下文关闭时出现 `flow-executor-event` 队列关闭、Hikari 数据源关闭或待投递消息重试日志；相关独立技术测试 exit 0，数据库盘点无队列残留。
- 数据源配置测试主动访问 `127.0.0.1:1` 并收到 connection refused，这是其预期故障路径。
- Gradle/Lombok 弃用或建议提示不是测试失败。

## 活动资源与等待任务

最终只读结果：活动 Execution（`CREATED/RUNNING/PAUSED/RESTARTED/KILLING`）= **0**；PAUSED TaskRun（等待任务）= **0**；数据库待投递 Queue message = **0**；运行中的 Gradle/Flow 进程 = **0**。本轮没有执行全库清理或删除用户数据。

## 结论

**NOT_COVERED**。已建立证据的 27 个场景全部通过，没有断言失败；但 63 个确认场景中 36 个仍缺少合规主测试证据，且两项全量回归命令因无进展被中止，因此不能宣称 63/63 全场景通过。

## 未解决事项

- 按当前 UC 预期重建 UC-01 S8、S13 主测试操作路径。
- 为 UC-02 S1～S7 建立公开任务查询、受理、权限校验和结果提交闭环及主测试。
- 建立 UC-04、UC-05 主测试类并补齐全部场景。
- 为 UC-06、UC-07 补齐各自缺失的 UC 主场景测试。
- 单独调查 `:core:test`/`./gradlew test` 无新增结果进度的具体测试阶段；本轮未修改生产代码。
- 后续治理测试上下文收尾时的队列关闭日志和 Logback 文件目录告警。

## 独立报告

- `UC-01-2026-08-28-190527.md`
- `UC-02-2026-08-28-190527.md`
- `UC-03-2026-08-28-190527.md`
- `UC-04-2026-08-28-190527.md`
- `UC-05-2026-08-28-190527.md`
- `UC-06-2026-08-28-190527.md`
- `UC-07-2026-08-28-190527.md`
- `UC-08-2026-08-28-190527.md`
- `UC-09-2026-08-28-190527.md`

