# UC-10 用户处理包含嵌套编排的审批 Flow测试报告

## 基本信息

- UC：[UC-10 用户处理包含嵌套编排的审批 Flow](../../uc/flow/UC-10%20%E7%94%A8%E6%88%B7%E5%A4%84%E7%90%86%E5%8C%85%E5%90%AB%E5%B5%8C%E5%A5%97%E7%BC%96%E6%8E%92%E7%9A%84%E5%AE%A1%E6%89%B9%20Flow.md)，S1–S13；索引状态 ACCEPTED。
- 唯一主测试类：[Uc10NestedApprovalFlowTest](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java)；本轮没有修改主测试类或 UC。
- 日期：2026-09-08，Asia/Shanghai；本类 JUnit 开始时间 `2026-09-08T08:41:07.585Z`（UTC），用时 55.906 秒。
- 代码版本：`64d381a425143f1a1d71ec8ee200a5880c3f5c26 + dirty`；包含本次最终作用域 CAS / `lock` 改造。
- 环境：Java 25.0.4、Gradle wrapper 9.5.1、真实 Micronaut 装配和 PostgreSQL 17.10；唯一数据库 `127.0.0.1:64628/flow`，容器 `flow-cas-20260908`。数据库测试未指向或清理默认 5432 实例。Java 21 没有本轮通过证据。

## 测试范围

独立发布嵌套顺序、条件、并行和循环审批 Flow，通过公开查询/恢复/回退验证解锁、短路、汇聚、取消、跨条件及并行回退、重复回退与旧审批失效。

所有现有场景均有唯一、未禁用的测试方法；没有新增、失效或错误归属方法。逐预期审计与方法运行结果分别统计，覆盖缺口保留为 NOT_COVERED，不以绿色方法替代完整验收。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 多级串签审批通过 | `s1SequentialNestedApprovalsCompleteOnlyAfterAllApprovals`（源码 38 行） | PASS | JUnit PASS。初审→财务→法务逐项解锁，后置记录/结束不提前；完整步骤列表与次数正确，最终审批通过、无未处理任务。 |
| S2 初审驳回后短路结束 | `s2RejectedInitialApprovalShortCircuitsNestedApprovalBranch`（源码 137 行） | PASS | JUnit PASS。初审驳回只走拒绝记录/结束，财务/法务/批准记录均不存在，最终完成、无未处理。 |
| S3 并行会签后进入终审 | `s3ParallelNestedApprovalsJoinBeforeFinalApproval`（源码 180 行） | PASS | JUnit PASS。两个会签分支分别按负责人→复核推进，全分支完成前无终审；终审后汇总/结束各一次，最终完成。 |
| S4 并行会签中途取消 | `s4CancelingParallelNestedApprovalsPreventsRemainingAndFollowingSteps`（源码 293 行） | PASS | JUnit PASS。一个分支进入复核后取消；两未完成审批恢复均拒绝，终审/汇总/发布/通知均无记录，KILLED、无未完成。 |
| S5 补充材料后重新审批 | `s5ConditionalNestedReviewLoopReachesFinalApprovalAfterSupplement`（源码 373 行） | PASS | JUnit PASS。每轮材料后才复核，首轮 NEEDS_MORE 后创建第二轮，第二轮 APPROVED 后才唯一终审，最终完成。 |
| S6 补充材料超过最大次数 | `s6ConditionalNestedReviewLoopFailsAtMaximumWithoutFinalApproval`（源码 466 行） | PASS | JUnit PASS。两轮材料/复核达到上限后 FAILED；不创建第三轮、终审或后续，无未完成。 |
| S7 同一条件分支内退回上一审批 | `s7SameConditionalBranchRewindsAndRejectsOldSource`（源码 526 行） | PASS | JUnit PASS。同条件分支越过通知退回；旧源恢复/退回均拒绝且当前路径不变，只重做目标片段，最终消费新结果并保留原因。 |
| S8 条件分支首个审批退回分流前审批 | `s8FirstConditionalApprovalRewindsBeforeTheSplit`（源码 569 行） | PASS | JUnit PASS。跨条件首审批退回分流前；目标前步骤只一次，固定金额仍进原分支，后置仅一次且用新结果，最终完成。 |
| S9 条件汇合后退回实际命中分支的最后审批 | `s9AfterConditionalMergeRewindsOnlyTheActuallyExecutedBranch`（源码 602 行） | PASS | JUnit PASS。两个金额实例分别重回实际命中分支，公开候选仅对应本次有效审批；另一实例不受影响、未命中无运行，后置各一次。 |
| S10 深层条件内审批退回外层分支审批 | `s10DeepConditionalApprovalRewindsToOuterApproval`（源码 654 行） | PASS | JUnit PASS。四层条件内退回外层成功，各未命中分支不执行；目标前一次，深层完成前无后置，最终使用最新结果并收敛。 |
| S11 并行分支内退回不使其他等待分支失效 | `s11BranchRewindKeepsTheOtherPausedBranchValid`（源码 699 行） | PASS | JUnit PASS。退回第一并行分支保留第二原 Pause ID；第二可按原记录完成且结果保持，汇总等待新第一分支并只一次。 |
| S12 并行汇合后只退回用户选定的上一分支审批 | `s12AfterParallelMergeRewindsOnlyTheSelectedPredecessor`（源码 739 行） | PASS | JUnit PASS。公开候选含两个并行前驱，明确选择早完成的第一分支；第二记录/结果不变且不重复，新终审消费两边有效结果。 |
| S13 跨条件退回后再次退回使用当前有效审批 | `s13RepeatedConditionalRewindUsesOnlyTheCurrentEffectiveApprovals`（源码 784 行） | PASS | JUnit PASS。第二次退回拒绝旧目标且路径不变，只使用当前有效源/目标；旧源不能恢复，两次原因与三轮记录保留，最终用 NEWEST。 |

## 执行命令与结果

使用以下配置（口令仅通过环境注入，不写入报告）：

```bash
export JAVA_HOME=/Users/cses-7/Library/Java/JavaVirtualMachines/ms-25.0.4/Contents/Home
export FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:64628/flow
export FLOW_POSTGRES_TEST_USER=flow
export FLOW_POSTGRES_TEST_CLEANUP=true
# FLOW_POSTGRES_TEST_PASSWORD 由本地测试环境提供
```

1. `./gradlew :core:testClasses --console=plain --offline --max-workers=1`：exit 0，编译成功。
2. `./gradlew :core:test --tests org.cses.flow.core.services.executions.Uc10NestedApprovalFlowTest --console=plain --offline --max-workers=1`：exit 0，13 tests，13 PASS、0 FAIL、0 SKIPPED，55.906 秒。

随后执行 `./gradlew :core:test --console=plain --offline --max-workers=1`：exit 1，399 tests，389 PASS、9 FAIL、1 SKIPPED，4 分 15 秒。最后以共享任务图执行 `./gradlew test build --continue --console=plain --offline --max-workers=1`：exit 1，7 分 28 秒；core 再次 399 tests，389 PASS、9 FAIL、1 SKIPPED，本 UC 方法结果与定向一致。

Server AOT、打包及 assemble 成功，但 `server:compileTestJava` 在 `AdminSessionArgumentBinderTest.java:6` 因缺少 `org.paas.session.Device` 失败，Server 测试未运行；整体 build 不通过。已有 Server XML 时间为 2026-09-01，不计入本轮。gen:test 为 NO-SOURCE。core 的唯一 SKIPPED 是缺少第二个数据源配置的 FlowDatabaseIntegrationTest，不是本 UC 场景。

原始证据目录：`core/build/cas-uc-regression-e6X33w`。本类快照 `Uc10NestedApprovalFlowTest-results/TEST-org.cses.flow.core.services.executions.Uc10NestedApprovalFlowTest.xml`；模块快照 `core-results/`；最终快照 `full-test-build-results/` 与 `full-test-build.log`。本报告已写明关键结果，不依赖更早报告。

## 失败与反例

本 UC 没有观察到稳定违反用户预期的生产反例；本轮存在的证据缺口（如有）不分配 CE。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 13 | 13 | 0 | 0 | 0 |

JUnit 主类：13 方法，13 PASS、0 FAIL、0 SKIPPED；这不是上表的 UC 验收数量。

## 活动资源与滚动保留

2026-09-08 17:02:02 Asia/Shanghai，在完整 test/build 结束后，只读查询隔离容器 `flow-cas-20260908`：`company_id LIKE 'uc-%'` 的 Flow、FlowTask、Execution、TaskRun、Queue 均为 0；整个隔离库 Execution=0（活动=0）、TaskRun=0（PAUSED=0）、Queue=0。所有 UC fixture 已关闭并按所属租户清理，未删除其他数据库数据或停止容器。

物理清理的 0 不替代逐场景的业务结束断言；表中 NOT_COVERED 仍保持未覆盖。上述为 Test Agent 核对时状态；随后主 Agent 已删除本次隔离容器及专属测试卷，默认开发库保留。

本批新增 9 份 UC 报告与 1 份 BATCH。按 `^(UC-\d+|BATCH)-\d{4}-\d{2}-\d{2}-\d{4}\.md$` 识别并滚动保留最新 3 份；只删除 `UC-04-2026-08-31-1253.md`，未解决 CE-01 已完整带入本批。清理后各活跃 UC 组及 BATCH 各 3 份，历史 UC-03 组 2 份，共 32 份匹配报告；另保留 9 份非匹配旧 HHmmss 文件，总计 41 份。

## 测试结论

UC-10 本次逐场景 PASS；结论仅覆盖本 UC 的公开 Service 路径，不等于整个项目 build 或 HTTP 验收通过。

## 未解决事项

- 本 UC 无新增覆盖缺口。

- 项目回归仍有既有架构、State、NestedPause 和 Log 测试失败；未改写测试以隐藏失败。完整 test/build 命令退出 1：core 既有 9 个失败；server 测试编译在 AdminSessionArgumentBinderTest.java:6 缺少 org.paas.session.Device，server 行为测试未执行。FlowDatabaseIntegrationTest 因未提供独立的 FLOW_POSTGRES_DEFAULT_TEST_URL 而 SKIPPED；gen:test 为 NO-SOURCE。
- 主 Agent 本轮限定既有缺口只报告、不扩展修复；Test Agent 仅新增报告，复用原有真实数据库测试资源。
