# UC-10 用户处理包含嵌套编排的审批 Flow测试报告

## 基本信息

时间：2026-09-14-1153 Asia/Shanghai；代码 `e6317f2 dirty`。UC：[原始UC](</System/Volumes/Data/workspace/java/flow/docs/uc/flow/UC-10 用户处理包含嵌套编排的审批 Flow.md>)，ACCEPTED；唯一主类：[Uc10NestedApprovalFlowTest.java](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java)，本轮未修改。报告只以原UC定义的用户观察判断状态。

## 测试范围

本轮删除OrchestrationPlan/OrchestrationState，resolveNexts只返回Task/TaskRun配对列表，resolveState独立返回Optional<State.Type>。测试同步了能力冲突样本、LoopUntil下一轮TaskRun及上限异常、ExecutorContext和接口结构断言；删除失效OrchestrationStateTest，新增ResolvedNextTask身份配对测试。原SummarySequence携带编排Output的技术样本改为标准Sequence→具体Runnable汇总→后置消费，明确断言编排节点无业务结果、三个业务节点各保留approved结果。没有新增outputs hook，UC主类与UC预期均未修改。

JDK25.0.4、Gradle Wrapper；真实Micronaut、生产PostgreSQL Repository、Pulsar、Executor和Worker，经公开FlowService/ExecutionService定义、发布、启动、重新查询、恢复/取消/退回和追溯。独占PostgreSQL17容器 `flow-nexts-pg-0914`（127.0.0.1:55176/flow_nexts，另建空host库flow_nexts_host）；独占Pulsar4.0.7 `flow-nexts-broker-0914`（56656/58086）。空库执行完整gen/sql/flow基线，Broker healthcheck为ok，FLOW_POSTGRES_TEST_CLEANUP=true；未访问其他任务容器或业务库。不把本轮JDK25结果称为Java21兼容证明。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 多级串签审批通过 | `s1SequentialNestedApprovalsCompleteOnlyAfterAllApprovals` | PASS | 已核对：初审通过前不会出现财务或法务审批；财务审批完成前不会出现法务审批；全部审批完成前不会执行记录和结束动作；财务审批、法务审批、记录和结束动作均只执行一次。结束：Flow 已以审批通过的完成终态结束，没有未处理审批。 |
| S2 初审驳回后短路结束 | `s2RejectedInitialApprovalShortCircuitsNestedApprovalBranch` | PASS | 已核对：初审驳回后只出现驳回处理并结束；财务审批、法务审批和通过后的记录动作均不出现，Flow 不继续后续审批。结束：Flow 已以驳回处理的完成终态结束，没有未处理审批或通过分支步骤。 |
| S3 并行会签后进入终审 | `s3ParallelNestedApprovalsJoinBeforeFinalApproval` | PASS | 已核对：两个会签分支可以分别推进，但每个分支的复核审批都只能在本分支负责人审批完成后出现；两个分支的负责人审批和复核审批全部完成前不会出现总负责人终审；总负责人终审只出现并执行一次，之后才执行一次汇总和结束步骤。结束：Flow 已完成，没有未处理的会签审批或终审。 |
| S4 并行会签中途取消 | `s4CancelingParallelNestedApprovalsPreventsRemainingAndFollowingSteps` | PASS | 已核对：取消后未完成的会签审批不能继续；总负责人终审以及后续汇总、发布和通知都不出现，已完成的部分审批不会重新触发其他分支或后续步骤。结束：Flow 已取消，没有仍可处理的会签审批或后续步骤。 |
| S5 补充材料后重新审批 | `s5ConditionalNestedReviewLoopReachesFinalApprovalAfterSupplement` | PASS | 已核对：每一轮都先完成补充材料再进行材料复核；第一轮需要补充时才创建下一轮；第二轮材料复核通过前不会出现最终审批；最终审批只在循环满足通过条件后出现并执行一次。结束：Flow 已以审批通过的完成终态结束，没有未完成轮次或审批。 |
| S6 补充材料超过最大次数 | `s6ConditionalNestedReviewLoopFailsAtMaximumWithoutFinalApproval` | PASS | 已核对：每一轮都先完成补充材料再进行材料复核；达到最大轮数后 Flow 明确失败，不创建下一轮；最终审批以及后续步骤都不出现。结束：Flow 已处于失败终态，没有未完成轮次、最终审批或后续步骤。 |
| S7 同一条件分支内退回上一审批 | `s7SameConditionalBranchRewindsAndRejectsOldSource` | PASS | 已核对：每次退回后原实例停止，由新实例整体接替；条件内的复核可以退回同分支初审，自动通知不阻断退回；不同编号的新实例中，初审、通知和复核按顺序重新执行，未命中分支不执行；使用原实例编号恢复旧复核和再次退回的请求均被明确拒绝，原实例保持停止，新初审及新实例当前路径不受拒绝请求影响，也不额外产生退回实例；新实例后续记录只在新的复核通过后执行一次，并使用重新审批的结果；原初审、通知、复核及退回原因的真实历史仍可查询。结束：新实例已完成，退回前后的真实历史保留；本次退回的原实例均已停止，没有等待恢复的 Pause。 |
| S8 条件分支首个审批退回分流前审批 | `s8FirstConditionalApprovalRewindsBeforeTheSplit` | PASS | 已核对：每次退回后原实例停止，由新实例整体接替；分支首个审批可以跨过条件和通知退回分流前初审，退回后的重做和后续流程由不同编号的新实例推进；目标初审之前的有效结果保留且步骤不重复，重新初审完成前分支审批不可处理；申请金额不变时仍只进入原命中分支，未命中分支的审批不执行；新实例后续记录使用重新审批结果并只在分支完成后执行一次，原有审批和通知的真实记录仍可查询。结束：新实例已完成，退回前后的真实审批历史可查询；本次退回的原实例均已停止，没有等待恢复的 Pause。 |
| S9 条件汇合后退回实际命中分支的最后审批 | `s9AfterConditionalMergeRewindsOnlyTheActuallyExecutedBranch` | PASS | 已核对：每次退回后原实例停止，由新实例整体接替；每次退回都产生不同编号的新实例，由它重新进入本次实际命中分支并继续后续流程，汇合与通知不阻断退回；未命中分支没有可供退回的审批运行记录，也不会因退回产生审批；两条退回路径互不影响；目标分支之前的有效结果保留且步骤不重复，新分支审批完成后才出现新的终审，各新实例最终结果使用各自重新审批的结果，原有真实历史保留。结束：两个新实例均已完成，各自只执行一次最终记录；本次退回的原实例均已停止，没有等待恢复的 Pause。 |
| S10 深层条件内审批退回外层分支审批 | `s10DeepConditionalApprovalRewindsToOuterApproval` | PASS | 已核对：每次退回后原实例停止，由新实例整体接替；退回不因条件嵌套层数受阻，不同编号的新实例从外层审批重新推进，外层审批完成后才按原条件路径再次到达深层审批；各层未命中分支不执行，目标之前的有效结果保留且步骤不重复；新实例的深层新审批完成前外层记录和最终结束步骤不执行，完成后各执行一次并采用最新审批结果；原有层级及真实路径仍可从历史查询。结束：新实例已完成且所有条件层级均已结束；本次退回的原实例均已停止，没有等待恢复的 Pause。 |
| S11 并行分支内退回保留其他分支的未完成进度 | `s11BranchRewindKeepsTheOtherPausedBranchValid` | PASS | 已核对：原实例整体停止，由不同编号的新实例整体接替，只重做第一分支初审至复核片段并继续后续流程；第二分支准备步骤及审批的原运行编号、已完成结果和当前进度保留，查询明确区分沿用记录和第一分支重新执行的新记录，第二分支已完成动作不重做；使用旧实例编号提交第二分支结果被明确拒绝，新实例的审批仍可处理、进度和结果不变，原实例保持停止；改用新实例编号与第二分支原审批运行编号后可以完成审批，结果只推进新实例，原实例历史中的审批不会被改为此次完成；第一分支重新审批完成前不能提前汇总；新实例最终汇总使用第一分支最新结果和第二分支有效结果，且只执行一次。结束：新实例已完成，第二分支的有效进度及真实历史保留；本次退回的原实例均已停止，没有等待恢复的 Pause。 |
| S12 并行汇合后只退回用户选定的上一分支审批 | `s12AfterParallelMergeRewindsOnlyTheSelectedPredecessor` | PASS | 已核对：每次退回后原实例停止，由新实例整体接替；退回使用用户选定的第一分支审批，不因第二分支更晚完成而改选目标；不同编号的新实例只重新审批一个选定分支，第二分支已完成记录和结果仍有效且不重复，其历史不会被当作新实例重新执行的审批；第一分支新审批完成前不会出现新的终审；新实例终审可以结合第一分支新结果和第二分支原结果正常完成，最终记录只执行一次。结束：新实例已完成，没有第二分支重复审批；本次退回的原实例均已停止，没有等待恢复的 Pause。 |
| S13 跨条件退回后再次退回使用当前有效审批 | `s13RepeatedConditionalRewindUsesOnlyTheCurrentEffectiveApprovals` | PASS | 已核对：每次退回后原实例停止，由新实例整体接替；已被替代的旧分支审批不能作为新实例本次退回目标，拒绝后当前终审和有效路径不变，也不额外创建运行实例；第二次合法退回使用当前终审和当前已完成的分支审批，再产生不同编号的新实例，只重做当前有效路径的相应片段并继续后续流程；两次退回的原因及各次真实审批历史均保留，已被替代的旧审批不能重新成为新实例的可处理步骤；最新实例最终记录使用最新一轮审批结果，未命中分支不执行。结束：最新实例已完成，历史保留两次退回及各实例真实记录；本次退回的原实例均已停止，没有等待恢复的 Pause。 |

## 执行命令与结果

参数脚本 `/tmp/flow-nexts-env.sh`；`/tmp/flow-nexts.init.gradle` 隔离每阶段XML/HTML/binary与标准流，并仅在Core Test移除会覆盖显式fixture的两个消费者/默认数据源环境开关；构建守卫保留，不排除源码、不替换实现、不启用ignoreFailures。

```bash
source /tmp/flow-nexts-env.sh
# 首轮与修复后技术目标：ResolvedNextTask、ExecutorService/EventHandler、Loop/LoopUntil、SubFlow、架构、内置样例
FLOW_TEST_STAGE=technical-fixed ./gradlew :core:test -I /tmp/flow-nexts.init.gradle \
  --tests '*ResolvedNextTaskTest' --tests '*ExecutorServiceTest' \
  --tests '*ExecutorEventMessageHandlerTest' --tests '*LoopUntilTest' --tests '*LoopTest' \
  --tests '*SubFlowTest' --tests '*CoreArchitectureStandardTest' --tests '*BuiltInPluginExamplesTest'
# 7个UC主类逐类执行、逐类隔离输出；完整脚本保留在/tmp/flow-nexts-uc.sh
zsh /tmp/flow-nexts-uc.sh
FLOW_TEST_STAGE=full ./gradlew test --continue -I /tmp/flow-nexts.init.gradle
# 完整失败方法复验命令见build/flow-nexts-0914/retry-command.txt
```

结果目录 `build/flow-nexts-0914/<阶段>/<模块>/test/{xml,html,binary}`；逐方法摘要 `build/flow-nexts-0914/results-summary.json`。日志 `/tmp/flow-nexts-<阶段或主类>.log`。源码/测试/UC快照 `source-before-uc.json`、`source-before-full.json`、`source-after-full.json`；UC阶段到全量开始哈希无变化，全量结束快照也无差异。

| 阶段 | tests | failures | errors | skipped |
| --- | --- | --- | --- | --- |
| technical | 67 | 3 | 0 | 0 |
| technical-fixed | 67 | 0 | 0 | 0 |
| Uc04PauseFlowTest | 11 | 1 | 0 | 0 |
| Uc05ParallelPauseFlowTest | 6 | 0 | 0 | 0 |
| Uc06ConditionalRouteTest | 7 | 0 | 0 | 0 |
| Uc07NestedTaskFlowTest | 7 | 0 | 0 | 0 |
| Uc09LoopOrchestrationTest | 6 | 0 | 0 | 0 |
| Uc10NestedApprovalFlowTest | 13 | 0 | 0 | 0 |
| Uc13SubFlowTest | 6 | 0 | 0 | 0 |
| full | 462 | 2 | 0 | 0 |
| retry | 2 | 2 | 0 | 0 |


本类独立执行 13项/0失败/0跳过，XML `build/flow-nexts-0914/Uc10NestedApprovalFlowTest/core/test/xml/TEST-org.cses.flow.core.services.executions.Uc10NestedApprovalFlowTest.xml`；完整回归 13项/0失败/0跳过，XML `build/flow-nexts-0914/full/core/test/xml/TEST-org.cses.flow.core.services.executions.Uc10NestedApprovalFlowTest.xml`。

完整根回归 `BUILD FAILED in 13m 52s`：Core 442项中440通过、2失败，Server 20/20通过；无错误或跳过。两失败方法独立复验 `BUILD FAILED in 13s`，2/2保持原失败。修复后技术目标 `BUILD SUCCESSFUL in 32s`，67/67通过。

## 失败与反例

首轮技术目标67项（SubFlow通配符同时包含UC13的6次调用）有3项本次引入的失败：标准Sequence完成后的汇总链、并行容器完成后继、UC13 S1子Pause恢复。共同原因为容器刚完成而后继尚待下一周期解析，handleEnd提前判定“无可运行任务但流程未收敛”；UC13由于事务回滚和Broker重投停在RESTARTED并超时。主Agent修复完成周期的进展识别，保留所有原断言后67/67通过；后续独立UC及全量继续覆盖。该问题已修复，不混同历史CE-01。

StateTest.rejectsInvalidHistoryAndIllegalGenericTransitions仍期待State.created().withState(PAUSED)抛WorkflowException，实际不抛；State与该测试本轮未改，保留断言并按实际全量/复验记录，不为消除失败改领域规则。

CE-01（UC04 S8，历史反例，本轮保持原预期复验）：用户发布 Pause→ThrowingTask→never-run，公开启动、重新查询Pause并提交 `{"decision":"APPROVED"}`；下游抛出 `uc04-downstream-failure`。UC要求异常返回、根与Pause保持提交前PAUSED、没有部分后续结果，再由用户取消。实际根为FAILED，断言 `expected: <PAUSED> but was: <FAILED>`；断言中止后未完成用户取消。独立UC类、全量和失败方法复验的结果见证据；fixture物理删除不算用户场景闭环，未修改UC或放宽该断言。

## 数量汇总

13场景：PASS 13、FAIL 0、NOT_COVERED 0、BLOCKED 0。JUnit成功不补足缺失观察；UC13 S3两次参数化调用只算一个场景。

## 测试结论

本UC全部场景通过。 整体完整回归的失败事实保留，不用目标专项替代全量结果。

## 未解决事项与收尾

本轮55场景范围内继续保留4个缺口：UC04 S4缺逐TaskRun历史比较、S6缺SECOND实际输出；UC07 S1缺前端复核结果归属、S6缺原层级修改过程与前端层级不变对照。UC02不属本轮目标，其历史S5/S7/S8缺口未扩展，不能由本轮全量JUnit通过推导完整覆盖。CE-01原样保留。

通过场景的终态/等待状态以公开查询断言为准；失败与覆盖缺口不会因fixture清理而变成闭环。全部验证后只读独占库：active_executions=0、paused_task_runs=0。仅删除flow-nexts-pg-0914、flow-nexts-broker-0914及匿名卷，删除命令返回两个准确名称；其他任务容器和业务资源未动。复现需重新创建独占PG/Pulsar，在空库加载当前完整gen/sql/flow基线；IF NOT EXISTS不能升级旧Schema，不得据此清空业务数据库。日志、XML/HTML、源码哈希及环境/命令脚本均保留。


滚动保留：本轮新增7份UC和1份BATCH报告，清理对应组最旧8份，各对应组保留3份；其他UC组和其他目录未修改。
