# UC-04 用户恢复处于 Pause 的 Flow测试报告

## 基本信息

时间：2026-09-14-1153 Asia/Shanghai；代码 `e6317f2 dirty`。UC：[原始UC](</System/Volumes/Data/workspace/java/flow/docs/uc/flow/UC-04 用户恢复处于 Pause 的 Flow.md>)，ACCEPTED；唯一主类：[Uc04PauseFlowTest.java](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc04PauseFlowTest.java)，本轮未修改。报告只以原UC定义的用户观察判断状态。

## 测试范围

本轮删除OrchestrationPlan/OrchestrationState，resolveNexts只返回Task/TaskRun配对列表，resolveState独立返回Optional<State.Type>。测试同步了能力冲突样本、LoopUntil下一轮TaskRun及上限异常、ExecutorContext和接口结构断言；删除失效OrchestrationStateTest，新增ResolvedNextTask身份配对测试。原SummarySequence携带编排Output的技术样本改为标准Sequence→具体Runnable汇总→后置消费，明确断言编排节点无业务结果、三个业务节点各保留approved结果。没有新增outputs hook，UC主类与UC预期均未修改。

JDK25.0.4、Gradle Wrapper；真实Micronaut、生产PostgreSQL Repository、Pulsar、Executor和Worker，经公开FlowService/ExecutionService定义、发布、启动、重新查询、恢复/取消/退回和追溯。独占PostgreSQL17容器 `flow-nexts-pg-0914`（127.0.0.1:55176/flow_nexts，另建空host库flow_nexts_host）；独占Pulsar4.0.7 `flow-nexts-broker-0914`（56656/58086）。空库执行完整gen/sql/flow基线，Broker healthcheck为ok，FLOW_POSTGRES_TEST_CLEANUP=true；未访问其他任务容器或业务库。不把本轮JDK25结果称为Java21兼容证明。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 用户查询并恢复单个处于 Pause 的 Flow | `s1UserQueriesAndResumesOnePausedFlow` | PASS | 已核对：Pause 状态可以从 Flow 中查询；本次正常运行没有退回历史；正常恢复后同一个运行实例继续执行，后置步骤取得提交结果。结束：Flow 已完成，没有仍在等待恢复的 Pause 步骤。 |
| S2 用户提交 Pause 结果后只推进当前 Flow | `s2ResumeResultIsConsumedOnceByOnlyTheCurrentFlow` | PASS | 已核对：提交结果只用于当前 Flow 的目标 Pause，目标 Pause 和后续步骤各执行一次。结束：Flow 已完成，没有重复步骤或等待恢复的 Pause。 |
| S3 非法输入后用户使用正确结果恢复 Flow | `s3IllegalResumeInputsAreRejectedUntilValidResultIsSubmitted` | PASS | 已核对：每个非法请求都被明确拒绝，拒绝期间 Flow 和 Pause 状态不变；合法结果能够恢复 Flow。结束：Flow 已完成，没有遗留等待恢复的 Pause。 |
| S4 用户重复恢复已经结束的 Pause | `s4RepeatedResumeOfFinishedPauseIsRejectedWithoutMutation` | NOT_COVERED | 重复恢复被拒绝，根 State、TaskRun ID/数量及 Pause 输出不变；没有逐条比较 TaskRun 状态历史。 测试调用通过，但不足以确认完整预期。 |
| S5 其他租户查询或恢复处于 Pause 的 Flow | `s5OtherTenantCannotQueryOrResumeThePausedFlow` | PASS | 已核对：租户 B 看不到租户 A 的运行实例且恢复请求被拒绝，租户 A 的 Flow 不受影响。结束：租户 A 的 Flow 已完成。 |
| S6 同一用户在两个处于 Pause 的 Flow 间选择准确实例 | `s6SameUserResumesTheSelectedInstanceOnly` | NOT_COVERED | 已验证第二实例先完成、第一仍等待，第一随后保存 FIRST；未断言第二实例实际结果为 SECOND。 测试调用通过，但不足以确认完整预期。 |
| S7 Flow 取消后用户恢复原 Pause | `s7CancelledFlowRejectsResumeOfItsFormerPause` | PASS | 已核对：恢复请求被明确拒绝，不会重新启动已取消的 Flow 或创建后续步骤。结束：Flow 保持已取消，没有等待恢复的 Pause。 |
| S8 Pause 结果提交后的后续处理发生异常 | `s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance` | FAIL | 要求恢复到提交前 PAUSED 并无部分推进；实际 FAILED（断言 PAUSED/FAILED）。用户取消收尾未执行，CE-01。 |
| S9 系统重启后用户继续恢复处于 Pause 的 Flow | `s9FreshServerCanRequeryAndResumeTheSamePause` | PASS | 已核对：无需依赖重启前保存的页面对象或内部信息，也能通过 Flow 查询找到同一 Pause 并继续同一运行实例。结束：Flow 已完成，没有等待恢复的 Pause。 |
| S10 用户从 Pause 退回到指定历史步骤并在新实例继续 | `s10UserRewindsFromPauseToHistoricalStepAndContinues` | PASS | 已核对：退回发起源是用户当时查询到的已创建且正在等待的 Pause，退回目标是用户选定的历史步骤运行记录；每次合法退回只产生一个不同编号的新运行实例，原实例停止且不再推进，由新实例接替重新执行及后续流程；退回返回结果给出新运行编号、退回受理时的原实例快照和受影响片段的步骤运行编号列表；列表只包含退回发起时当前有效运行路径上从目标步骤到源 Pause 的运行记录且两端均包含，同时包含这些步骤下真实发生的子步骤运行记录；未命中条件路径的已跳过运行记录作为叶子包含在列表中，其未创建的子步骤不出现；列表按业务回滚顺序排列，较晚发生的记录和子步骤在前、所属父步骤在后，目标步骤最后；只有目标步骤到源 Pause 的片段被重新执行并产生新的步骤运行编号，目标之前的有效结果和原步骤运行编号保留且不重复执行，源 Pause 之后的步骤不会提前执行；查询结果能够识别本次退回的新实例、源 Pause 步骤运行编号、目标步骤运行编号和退回原因，并区分沿用记录与重新执行记录；退回前真实发生的记录仍可查询，新的 Pause 归属于新实例并可正常恢复，后置步骤使用重新执行的片段结果。结束：新实例已完成，原实例已停止，历史保留本次退回及退回前后的真实步骤记录，没有等待恢复的 Pause。 |
| S11 用户在新的 Pause 再次退回 | `s11UserRewindsAgainFromTheNewPause` | PASS | 已核对：第二次退回以第一次新实例中已创建且正在等待的 Pause 作为新源，并以用户第二次选定的当前有效历史步骤运行记录作为新目标；两次退回分别产生不同编号的新实例，每次的原实例停止，由新实例接替重新执行；两次返回结果都给出各次新运行编号、该次退回受理时的原实例快照和按业务回滚顺序排列的受影响步骤运行编号列表；每个列表只反映该次退回发起时的当前有效运行路径，不混入已经被替代的上一段运行记录；两次退回的源步骤运行编号、目标步骤运行编号、退回原因和真实执行记录均可查询；每次只重新执行对应的退回片段并产生新的步骤运行编号，最终后置步骤使用最新实例的片段结果。结束：最新实例已完成，前两个实例均已停止，两次退回及各次真实步骤记录均保留，没有等待恢复的 Pause。 |

## 执行命令与结果

参数脚本 `/tmp/flow-nexts-env.sh`；`/tmp/flow-nexts.init.gradle` 隔离每阶段XML/HTML/binary与标准流，并仅在Core Test移除会覆盖显式fixture的两个消费者/默认数据源环境开关；构建守卫保留，不排除源码、不替换实现、不启用ignoreFailures。

```bash
source /tmp/flow-nexts-env.sh
# 首轮与修复后技术目标：ResolvedNextTask、ExecutorService/EventHandler、Loop/LoopUntil、SubFlow、架构、内置样例
FLOW_TEST_STAGE=technical-fixed ./gradlew :core:test -I /tmp/flow-nexts.init.gradle \
  --tests '*ResolvedNextTaskTest' --tests '*ExecutorServiceTest' \
  --tests '*ExecutorEventMessageHandlerTest' --tests '*LoopUntilTest' --tests '*LoopTest' \
  --tests '*SubFlowTest' --tests '*CoreArchitectureStandardTest' --tests '*BuiltInPluginExamplesTest'
# 7个UC主类逐类执行、逐类隔离输出；完整脚本保留在/tmp/flow-nexts-uc.sh
zsh /tmp/flow-nexts-uc.sh
FLOW_TEST_STAGE=full ./gradlew test --continue -I /tmp/flow-nexts.init.gradle
# 完整失败方法复验命令见build/flow-nexts-0914/retry-command.txt
```

结果目录 `build/flow-nexts-0914/<阶段>/<模块>/test/{xml,html,binary}`；逐方法摘要 `build/flow-nexts-0914/results-summary.json`。日志 `/tmp/flow-nexts-<阶段或主类>.log`。源码/测试/UC快照 `source-before-uc.json`、`source-before-full.json`、`source-after-full.json`；UC阶段到全量开始哈希无变化，全量结束快照也无差异。

| 阶段 | tests | failures | errors | skipped |
| --- | --- | --- | --- | --- |
| technical | 67 | 3 | 0 | 0 |
| technical-fixed | 67 | 0 | 0 | 0 |
| Uc04PauseFlowTest | 11 | 1 | 0 | 0 |
| Uc05ParallelPauseFlowTest | 6 | 0 | 0 | 0 |
| Uc06ConditionalRouteTest | 7 | 0 | 0 | 0 |
| Uc07NestedTaskFlowTest | 7 | 0 | 0 | 0 |
| Uc09LoopOrchestrationTest | 6 | 0 | 0 | 0 |
| Uc10NestedApprovalFlowTest | 13 | 0 | 0 | 0 |
| Uc13SubFlowTest | 6 | 0 | 0 | 0 |
| full | 462 | 2 | 0 | 0 |
| retry | 2 | 2 | 0 | 0 |


本类独立执行 11项/1失败/0跳过，XML `build/flow-nexts-0914/Uc04PauseFlowTest/core/test/xml/TEST-org.cses.flow.core.services.executions.Uc04PauseFlowTest.xml`；完整回归 11项/1失败/0跳过，XML `build/flow-nexts-0914/full/core/test/xml/TEST-org.cses.flow.core.services.executions.Uc04PauseFlowTest.xml`。

完整根回归 `BUILD FAILED in 13m 52s`：Core 442项中440通过、2失败，Server 20/20通过；无错误或跳过。两失败方法独立复验 `BUILD FAILED in 13s`，2/2保持原失败。修复后技术目标 `BUILD SUCCESSFUL in 32s`，67/67通过。

## 失败与反例

首轮技术目标67项（SubFlow通配符同时包含UC13的6次调用）有3项本次引入的失败：标准Sequence完成后的汇总链、并行容器完成后继、UC13 S1子Pause恢复。共同原因为容器刚完成而后继尚待下一周期解析，handleEnd提前判定“无可运行任务但流程未收敛”；UC13由于事务回滚和Broker重投停在RESTARTED并超时。主Agent修复完成周期的进展识别，保留所有原断言后67/67通过；后续独立UC及全量继续覆盖。该问题已修复，不混同历史CE-01。

StateTest.rejectsInvalidHistoryAndIllegalGenericTransitions仍期待State.created().withState(PAUSED)抛WorkflowException，实际不抛；State与该测试本轮未改，保留断言并按实际全量/复验记录，不为消除失败改领域规则。

CE-01（UC04 S8，历史反例，本轮保持原预期复验）：用户发布 Pause→ThrowingTask→never-run，公开启动、重新查询Pause并提交 `{"decision":"APPROVED"}`；下游抛出 `uc04-downstream-failure`。UC要求异常返回、根与Pause保持提交前PAUSED、没有部分后续结果，再由用户取消。实际根为FAILED，断言 `expected: <PAUSED> but was: <FAILED>`；断言中止后未完成用户取消。独立UC类、全量和失败方法复验的结果见证据；fixture物理删除不算用户场景闭环，未修改UC或放宽该断言。

## 数量汇总

11场景：PASS 8、FAIL 1、NOT_COVERED 2、BLOCKED 0。JUnit成功不补足缺失观察；UC13 S3两次参数化调用只算一个场景。

## 测试结论

本UC仍存在失败或覆盖缺口，不能宣称完整验收。 整体完整回归的失败事实保留，不用目标专项替代全量结果。

## 未解决事项与收尾

本轮55场景范围内继续保留4个缺口：UC04 S4缺逐TaskRun历史比较、S6缺SECOND实际输出；UC07 S1缺前端复核结果归属、S6缺原层级修改过程与前端层级不变对照。UC02不属本轮目标，其历史S5/S7/S8缺口未扩展，不能由本轮全量JUnit通过推导完整覆盖。CE-01原样保留。

通过场景的终态/等待状态以公开查询断言为准；失败与覆盖缺口不会因fixture清理而变成闭环。全部验证后只读独占库：active_executions=0、paused_task_runs=0。仅删除flow-nexts-pg-0914、flow-nexts-broker-0914及匿名卷，删除命令返回两个准确名称；其他任务容器和业务资源未动。复现需重新创建独占PG/Pulsar，在空库加载当前完整gen/sql/flow基线；IF NOT EXISTS不能升级旧Schema，不得据此清空业务数据库。日志、XML/HTML、源码哈希及环境/命令脚本均保留。


滚动保留：本轮新增7份UC和1份BATCH报告，清理对应组最旧8份，各对应组保留3份；其他UC组和其他目录未修改。
