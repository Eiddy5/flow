# UC-08 用户在流程中输出动态日志测试报告

## 基本信息

- 时间：2026-09-09 18:12，Asia/Shanghai。
- 代码：`01cc2d400b3597ab49e2cddb6a81865fa2dfb1e7 dirty`。
- UC：[UC-08 用户在流程中输出动态日志](../../uc/flow/UC-08%20用户在流程中输出动态日志.md)，ACCEPTED，3 个场景，预期未修改。
- 唯一主类：`core/src/test/java/org/cses/flow/core/services/executions/Uc08DynamicLogFlowTest.java`，已修改。
- 本轮目标：将测试任务从 RunResult 迁移为具体 Output，删除 Task 可配置 outputs；验证具体输出进入后续 RunContext、只读输出元数据和既有动态日志场景。

## 测试范围

测试任务返回具体 record/POJO 输出；空任务返回 VoidOutput。移除 Task YAML、Map 和 builder 的 outputs 配置，保留 Flow.outputs 和 WorkerTaskResult 的结果 Map 信封。需要确定业务字段的测试使用具体任务，避免借 Log 声明虚构产出。Pause 的动态元数据继续来自 onResume。

UC08 前置及观察任务返回明确的 LogInputOutput(result)，S1 补充后置步骤总次数为 1 的断言，补齐上一份报告的断言缺口。本轮未修改生产代码、UC 或预期；主 Agent 完成生产迁移及恢复 Pause 的暂停能力。

普通技术测试新增/调整：

- ExecutorEventMessageHandlerTest：无 outputs 配置时，Runnable 结果经过编排任务的具体 Output，进入后续任务 RunContext；三条 TaskRun 结果均为 decision=approved，流程 SUCCESS 且无活动任务。此测试使用技术仓储替身，不是 UC 验收证据。
- WorkerDispatcherTest：具体 POJO 业务字段投影，state/error 不进入结果；空成功、明确失败、缺少失败错误和非法成功错误组合。
- TaskTest：泛型任务父类与泛型输出父类中的 value 解析为 String，PAAS JSON 输出业务值；元数据只读、输出值类型校验。
- PluginControllerTest：具体 ResultTask 发布 result/STRING 只读元数据，schema.properties 不再出现 outputs；结果列表不可修改。
- PluginDeserializerTest、PluginSchemaGeneratorTest、PauseTest、LoopUntilTest、FlowMaterializationTest 和 TaskDataPersistenceMappingTest：配置移除、派生元数据、Pause 动态字段、路由条件和持久化映射同步。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 输出包含前置结果的日志并继续流程 | s1RendersPrecedingResultOnceAndContinuesTheFlow | BLOCKED | 已保留目标日志一次、精确消息、后置 SUCCESS、根 SUCCESS、无活动步骤断言，并补后置总次数一次。两次执行都在 WorkflowUcFixture.open 前置环境检查失败，未创建流程，不能证明最终断言通过。 |
| S2 发布包含无效消息表达式的流程 | s2RejectsInvalidMessageExpressionBeforePublishing | BLOCKED | 发布/启动拒绝、无正式版、无实例及日志副作用、删除草稿断言保留；环境检查失败，未执行业务入口。 |
| S3 日志表达式引用不存在的运行值 | s3MissingRuntimePathFailsWithoutWritingAMisleadingLog | BLOCKED | 目标与根 FAILED、错误路径、后置不执行、无日志和无活动任务断言保留；环境检查失败，未执行业务入口。 |

3 项异常均为 `FLOW_POSTGRES_TEST_URL is required for UC tests`。真实 PostgreSQL/Micronaut/Pulsar 路径不能在缺少环境配置时完成，不以技术测试成功代替 UC 验收。

## 执行命令与结果

实际 JDK 为 25.0.4，不构成 Java 21 兼容性证明。使用 Gradle Wrapper。当前未提供有效的测试 PostgreSQL/Pulsar 地址，先前任务专属容器已删除；本轮未复用其失效地址，也未新建外部服务。

运行脚本 `/private/tmp/flow-output-run.sh` 使用 `JAVA_HOME=/Users/cses-7/Library/Java/JavaVirtualMachines/ms-25.0.4/Contents/Home`，隔离旧测试 URL，并附加 `-I /private/tmp/flow-pause-results.gradle --offline --max-workers=1 --console=plain`。init script 仅隔离结果目录和标准流输出，不替换实现。

证据根目录：`core/build/task-output-0909/`。每阶段日志 `<阶段>/run.log`，XML/HTML 为 `<阶段>/<模块>/test/{xml,html}`。

| 阶段 | Wrapper 命令主体 | 实际结果 |
| --- | --- | --- |
| 目标编译 | `./gradlew :core:testClasses :server:testClasses` | PASS。最初沿用前任务脚本，日志在 `core/build/pause-rename-0909/output-compile/run.log`。 |
| 首次输出定向 | `./gradlew :core:test` 选择下述 11 类，`:server:test` 选择 2 类，`--continue` | `targeted/`：Core 90/90，Server 4/4，全部通过。 |
| UC08 | `./gradlew :core:test --rerun --tests '*Uc08DynamicLogFlowTest'` | `uc08/`：JUnit 3 失败，均为环境前置阻塞；UC 3 BLOCKED。 |
| UC08 失败复验 | `./gradlew :core:test` 分别以 `--tests` 指定 S1/S2/S3 方法 | `uc08-retry/`：3 项重复同一 PostgreSQL 配置错误；包含补齐后的 S1 断言版本。 |
| Core 模块 | `./gradlew :core:test --rerun` | `module/`：448 项，269 passed / 138 failed / 41 skipped，exit 1。 |
| 泛型继承定向 | `./gradlew :core:test --tests '*TaskTest'` | `generic/`：8/8，通过。最初测试误把 fields 的 Map 返回值接为 List，修正测试后编译及执行成功。 |
| 全项目 | `./gradlew test --continue` | `full/`：Core 449 项，270 passed / 138 failed / 41 skipped；Server 20/20；总 469 项，290 passed / 138 failed / 41 skipped，exit 1。 |
| 夹具第一次命名修正后全项目 | `./gradlew test --continue` | `final-full/`：同上总计。新的 StatusOutput 简单名冲突仍触发 1 项架构检查，见下文。Server 结果 FROM-CACHE，未重复执行。 |
| 构建 | `./gradlew build --continue` | `build/`：exit 1，仅因 Core test 失败；Core 449 项同上，Server 20/20。Server assemble/build 和 JAR/发行包任务完成。耗时 2 分 30 秒，没有额外打包阻塞。 |
| 最终命名修正后定向 | `./gradlew :core:test` 选择 11 类及 record 工厂架构方法，`:server:test` 选择 2 类，`--continue` | `final-targeted/`：Core 92/92、Server 4/4，总 96 passed / 0 failed / 0 skipped，exit 0。包含全部 95 项输出相关定向及 1 项架构工厂检查。 |

11 个 Core 类：WorkerDispatcherTest、ExecutorEventMessageHandlerTest、ExecutorServiceTest、TaskTest、PauseTest、LoopUntilTest、LogTest、PluginDeserializerTest、PluginSchemaGeneratorTest、FlowMaterializationTest、TaskDataPersistenceMappingTest。2 个 Server 类：PluginControllerTest、InputViewTest。最终追加方法为 `CoreArchitectureStandardTest.javaSourcesFollowRecordAndTechnicalIdRules`。

最终 96 项通过属于最终夹具命名版本。最后没有再次执行完整 test/build；不得将先前全套统计改写为全绿，也不得从失败总数中扣除后续通过项目后宣称得到了新的完整执行计数。原 94 项定向及新增泛型继承用例在全量同名结果中未新增失败，schema、序列化及上下文链路保持通过。

## 失败与反例

完整测试/构建的 138 项失败分类：

| 数量 | 分类 | 证据及归因边界 |
| --- | --- | --- |
| 132 | 环境 | `FLOW_POSTGRES_TEST_URL is required for UC tests`，在夹具创建真实运行环境前失败，包括 UC08；不是已确认的实现失败。 |
| 2 | 环境 | ExecutorPulsarIntegrationTest 缺 `FLOW_PULSAR_TEST_ADMIN_URL`；另一 Pulsar 集成测试缺 `FLOW_PULSAR_TEST_URL`。真实 Broker 验证未完成。 |
| 2 | 架构路径断言 | CoreArchitectureStandardTest.executorAndWorkerAreTopLevelPeersOfCore 要求旧 executor 路径；taskCapabilitiesKeepOrchestrationOutOfWorkers 读取缺失 `executor/handlers/ExecutorEventHandler.java`。旧报告已有同类差异，本轮保持断言；没有二分归因。 |
| 1 | 状态转换断言 | StateTest.rejectsInvalidHistoryAndIllegalGenericTransitions 期望 WorkflowException，实际未抛。旧报告已有该失败，本轮仍重现；本任务未修复或放宽断言，没有二分归因。 |
| 1 | 本轮测试夹具名称冲突，最终已修正 | 检查器按 record 简单名匹配，最初把 flows.Output 普通类构造误认成 record；第一次改名后又把 WorkerDispatcherTest.StatusOutput POJO 误认成 record。最终夹具统一 FixtureDecisionOutput 等唯一名字，工厂检查与输出定向通过。未修改生产 flows.Output 或弱化检查器。 |

首次字段定向出现的 4 项 Pause 行为失败，由主 Agent 恢复 Pause.pausesTaskRun() 后全部复验通过。另修正了两个测试迁移错误：Pause 元数据以无顺序集合校验；typedRoute 使用真正布尔输出任务。首次失败日志保留于 `core/build/pause-rename-0909/output-targeted/`。这些修正均不代表 UC08 已通过。

CE-01（历史未解决，按保留规则完整携带；本轮非目标场景且未获得新的真实执行证据）：UC04 S8 通过公开 Service 发布 Pause → ThrowingTask → never-run，启动并查询 Pause，提交 `{"decision":"APPROVED"}`；后置任务抛出 `uc04-downstream-failure`。UC 要求明确错误、回到提交前 PAUSED 且不留下部分后置处理，随后由用户取消。此前真实执行实际进入 FAILED，断言 `expected: <PAUSED> but was: <FAILED>`，用户取消收尾未完成。复现命令 `./gradlew :core:test --rerun --tests '*Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance'`，需要 harness 中的真实 PostgreSQL 和 Pulsar 配置。本轮模块/全量因缺少 PostgreSQL URL 在夹具前置检查阻塞，无法确认反例修复或变化；物理清理和技术定向通过均不能补足该用户闭环。

本轮 UC08 未建立新增 CE：环境阻塞不记实现反例。

## 数量汇总

- UC08 场景：PASS 0 / FAIL 0 / NOT_COVERED 0 / BLOCKED 3。S1 历史断言缺口已补，仍需真实环境执行。
- 最终定向：96 passed / 0 failed / 0 skipped。
- 全量最近实际统计：469 tests / 290 passed / 138 failed / 41 skipped。
- 41 skipped：DefaultDispatchQueueIntegrationTest 15、DefaultDispatchQueueTransactionIntegrationTest 6、ExecutionCasIntegrationTest 12、PostgresRepositoryIntegrationTest 7、FlowDatabaseIntegrationTest 1，缺少各自真实数据库测试配置；不计通过。
- gen、processor 测试任务 NO-SOURCE；真实外部 UI、负载、集群和故障恢复没有执行。

## 活动资源与滚动清理

本轮没有创建外部数据库/Broker 容器。UC 测试均在环境前置检查失败，本轮由这些 UC 新建的运行流程 0、等待任务 0；没有可连接的真实环境，不能将此解释为全局数据库运行/等待数量已审计为 0。所有本轮 Gradle 命令已退出，没有本任务遗留测试进程；技术上下文/替身由测试关闭。

新增本报告 1 份，按文件名时间删除 `UC-08-2026-09-09-1222.md` 1 份；规范格式 UC08 报告保留 1629、1722、1812 共 3 份。非规范时间格式报告保持不变。其他 UC 和 BATCH 组未新增、未删除。静态 `git diff --check` 通过。

## 测试结论与未解决事项

测试侧 RunResult 和 Task 可配置 outputs 的迁移已完成，最终 95 项输出相关定向与 1 项工厂架构检查通过。真实 UC08 尚未验收；需要配置 PostgreSQL/Pulsar 后复跑其 3 个场景和数据库集成测试。两个旧架构路径断言、State 转换差异、历史 CE-01 仍待处理。完整测试与根 build 均未通过。实际 JVM 为 25，未验证 Java 21。
