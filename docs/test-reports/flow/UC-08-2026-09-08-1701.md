# UC-08 用户在流程中输出动态日志测试报告

## 基本信息

- UC：[UC-08 用户在流程中输出动态日志](../../uc/flow/UC-08%20%E7%94%A8%E6%88%B7%E5%9C%A8%E6%B5%81%E7%A8%8B%E4%B8%AD%E8%BE%93%E5%87%BA%E5%8A%A8%E6%80%81%E6%97%A5%E5%BF%97.md)，S1–S3；索引状态 ACCEPTED。
- 唯一主测试类：[Uc08DynamicLogFlowTest](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc08DynamicLogFlowTest.java)；本轮没有修改主测试类或 UC。
- 日期：2026-09-08，Asia/Shanghai；本类 JUnit 开始时间 `2026-09-08T08:42:11.445Z`（UTC），用时 4.047 秒。
- 代码版本：`64d381a425143f1a1d71ec8ee200a5880c3f5c26 + dirty`；包含本次最终作用域 CAS / `lock` 改造。
- 环境：Java 25.0.4、Gradle wrapper 9.5.1、真实 Micronaut 装配和 PostgreSQL 17.10；唯一数据库 `127.0.0.1:64628/flow`，容器 `flow-cas-20260908`。数据库测试未指向或清理默认 5432 实例。Java 21 没有本轮通过证据。

## 测试范围

按标准 Flow 发布动态消息表达式、通过公开运行/恢复入口驱动目标 Log，并观察日志、下游与终态。没有脱离 Flow 直接调用目标 Task。

所有现有场景均有唯一、未禁用的测试方法；没有新增、失效或错误归属方法。逐预期审计与方法运行结果分别统计，覆盖缺口保留为 NOT_COVERED，不以绿色方法替代完整验收。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 输出包含前置结果的日志并继续流程 | `s1RendersPrecedingResultOnceAndContinuesTheFlow`（源码 35 行） | NOT_COVERED | JUnit FAIL。目标日志与后置步骤执行至 SUCCESS，目标次数 1；日志捕获断言与实际差异及最终证据缺口见下文。 日志捕获范围是整个 Log 类，而场景自身 observer 也输出 test step；整类/单方法均实际 [处理结果：ready, test step]，不违反目标动态日志只有一次的用户预期。过宽列表断言中断了后续无活动步骤检查，因此记录为观察证据缺口，不以此建立生产反例。 |
| S2 发布包含无效消息表达式的流程 | `s2RejectsInvalidMessageExpressionBeforePublishing`（源码 79 行） | NOT_COVERED | JUnit PASS。无效表达式发布拒绝、无正式版本及日志，草稿删除；启动尝试与公开运行查询证据缺口见下文。 未执行发布拒绝后的启动尝试；运行数量使用 fixture.executionCount 的 Repository 查询，缺少公开运行查询与拒绝启动无副作用证据。 |
| S3 日志表达式引用不存在的运行值 | `s3MissingRuntimePathFailsWithoutWritingAMisleadingLog`（源码 123 行） | PASS | JUnit PASS。缺失运行路径时目标与根 FAILED，错误指明路径；后续未创建，没有误导日志，无活动步骤。 |

## 执行命令与结果

使用以下配置（口令仅通过环境注入，不写入报告）：

```bash
export JAVA_HOME=/Users/cses-7/Library/Java/JavaVirtualMachines/ms-25.0.4/Contents/Home
export FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:64628/flow
export FLOW_POSTGRES_TEST_USER=flow
export FLOW_POSTGRES_TEST_CLEANUP=true
# FLOW_POSTGRES_TEST_PASSWORD 由本地测试环境提供
```

1. `./gradlew :core:testClasses --console=plain --offline --max-workers=1`：exit 0，编译成功。
2. `./gradlew :core:test --tests org.cses.flow.core.services.executions.Uc08DynamicLogFlowTest --console=plain --offline --max-workers=1`：exit 1，3 tests，2 PASS、1 FAIL、0 SKIPPED，4.047 秒。
3. `./gradlew :core:test --tests org.cses.flow.core.services.executions.Uc08DynamicLogFlowTest.s1RendersPrecedingResultOnceAndContinuesTheFlow --console=plain --offline --max-workers=1`：单独复验 exit 1，1 FAIL，3.147 秒，保持同一断言差异。

随后执行 `./gradlew :core:test --console=plain --offline --max-workers=1`：exit 1，399 tests，389 PASS、9 FAIL、1 SKIPPED，4 分 15 秒。最后以共享任务图执行 `./gradlew test build --continue --console=plain --offline --max-workers=1`：exit 1，7 分 28 秒；core 再次 399 tests，389 PASS、9 FAIL、1 SKIPPED，本 UC 方法结果与定向一致。

Server AOT、打包及 assemble 成功，但 `server:compileTestJava` 在 `AdminSessionArgumentBinderTest.java:6` 因缺少 `org.paas.session.Device` 失败，Server 测试未运行；整体 build 不通过。已有 Server XML 时间为 2026-09-01，不计入本轮。gen:test 为 NO-SOURCE。core 的唯一 SKIPPED 是缺少第二个数据源配置的 FlowDatabaseIntegrationTest，不是本 UC 场景。

原始证据目录：`core/build/cas-uc-regression-e6X33w`。本类快照 `Uc08DynamicLogFlowTest-results/TEST-org.cses.flow.core.services.executions.Uc08DynamicLogFlowTest.xml`；模块快照 `core-results/`；最终快照 `full-test-build-results/` 与 `full-test-build.log`。本报告已写明关键结果，不依赖更早报告。

## 失败与反例

日志 S1 的整类与单方法均为 JUnit FAIL；实际列表 `[处理结果：ready, test step]`，其中 `test step` 来自本场景自己的后置 observer。目标动态消息正确且仅一次；整类 Log 捕获范围过宽，不能据此建立生产 CE。断言失败中断最终无活动步骤检查，故 S1 标记 NOT_COVERED。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 3 | 1 | 0 | 2 | 0 |

JUnit 主类：3 方法，2 PASS、1 FAIL、0 SKIPPED；这不是上表的 UC 验收数量。

## 活动资源与滚动保留

2026-09-08 17:02:02 Asia/Shanghai，在完整 test/build 结束后，只读查询隔离容器 `flow-cas-20260908`：`company_id LIKE 'uc-%'` 的 Flow、FlowTask、Execution、TaskRun、Queue 均为 0；整个隔离库 Execution=0（活动=0）、TaskRun=0（PAUSED=0）、Queue=0。所有 UC fixture 已关闭并按所属租户清理，未删除其他数据库数据或停止容器。

物理清理的 0 不替代逐场景的业务结束断言；表中 NOT_COVERED 仍保持未覆盖。上述为 Test Agent 核对时状态；随后主 Agent 已删除本次隔离容器及专属测试卷，默认开发库保留。

本批新增 9 份 UC 报告与 1 份 BATCH。按 `^(UC-\d+|BATCH)-\d{4}-\d{2}-\d{2}-\d{4}\.md$` 识别并滚动保留最新 3 份；只删除 `UC-04-2026-08-31-1253.md`，未解决 CE-01 已完整带入本批。清理后各活跃 UC 组及 BATCH 各 3 份，历史 UC-03 组 2 份，共 32 份匹配报告；另保留 9 份非匹配旧 HHmmss 文件，总计 41 份。

## 测试结论

UC-08 尚未完整验收：2 个场景缺少充分证据。

## 未解决事项

- S1：日志捕获范围是整个 Log 类，而场景自身 observer 也输出 test step；整类/单方法均实际 [处理结果：ready, test step]，不违反目标动态日志只有一次的用户预期。过宽列表断言中断了后续无活动步骤检查，因此记录为观察证据缺口，不以此建立生产反例。
- S2：未执行发布拒绝后的启动尝试；运行数量使用 fixture.executionCount 的 Repository 查询，缺少公开运行查询与拒绝启动无副作用证据。

- 项目回归仍有既有架构、State、NestedPause 和 Log 测试失败；未改写测试以隐藏失败。完整 test/build 命令退出 1：core 既有 9 个失败；server 测试编译在 AdminSessionArgumentBinderTest.java:6 缺少 org.paas.session.Device，server 行为测试未执行。FlowDatabaseIntegrationTest 因未提供独立的 FLOW_POSTGRES_DEFAULT_TEST_URL 而 SKIPPED；gen:test 为 NO-SOURCE。
- 主 Agent 本轮限定既有缺口只报告、不扩展修复；Test Agent 仅新增报告，复用原有真实数据库测试资源。
