# UC-06 用户提交外派结果后的条件路径测试报告

## 基本信息

- UC：`docs/uc/flow/UC-06 用户提交外派结果后的条件路径.md`
- 主测试类：`core/src/test/java/org/cses/flow/core/services/executions/Uc06ConditionalRouteTest.java`
- 执行时间：2026-08-27 14:04～14:17 +08:00
- 版本：`070da94`，工作区 `dirty`
- 环境：Java 21.0.12、真实 Micronaut、PostgreSQL 17.10、临时库 `flow_uc_20260827_140410`

## 测试范围

验证 UC-06 S1～S7，覆盖外派结果驱动的批准、拒绝、无匹配、非法条件、缺失字段、大小写和跨流程选择。当前主测试类只映射 S4。

## 场景验证

| 场景 | 测试方法 | 状态 | 结束证据 |
| --- | --- | --- | --- |
| S1 用户选择批准路径 | — | NOT_COVERED | 主测试类没有 S1 方法。 |
| S2 用户选择拒绝路径 | — | NOT_COVERED | 主测试类没有 S2 方法。 |
| S3 用户提交没有匹配路径的结果 | — | NOT_COVERED | 主测试类没有 S3 方法。 |
| S4 用户发布包含非法条件的 Flow | `s4RejectsInvalidRouteWhenDeployingFlow` | FAIL | 建立待发布 Flow 时命中 CE-01，未到达非法条件拒绝证据。 |
| S5 用户提交结果时缺少决定字段 | — | NOT_COVERED | 主测试类没有 S5 方法。 |
| S6 用户改变决定值的大小写 | — | NOT_COVERED | 主测试类没有 S6 方法。 |
| S7 用户选择另一个流程中的外派任务 | — | NOT_COVERED | 主测试类没有 S7 方法。 |

## 执行命令与结果

- 主类：1 test，1 failed。
- 命令：`FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:5432/flow_uc_20260827_140410 FLOW_POSTGRES_TEST_USER=flow FLOW_POSTGRES_TEST_PASSWORD=flow FLOW_POSTGRES_TEST_CLEANUP=true ./gradlew :core:test --tests '*Uc06ConditionalRouteTest' --rerun-tasks --max-workers=1 --no-daemon --console=plain`。

## 失败与反例

- CE-01 输入：S4 使用包含非法条件表达式的 YAML Flow 并尝试发布。
- 预期：发布被业务校验拒绝，且不留下正式或运行资源。
- 实际：建立持久化草稿时先抛出 `ActorRef` 无可用 Jackson serializer 的 `InvalidDefinitionException`。
- 影响：未到达非法条件验证；同一方法又在 Core 全量回归中复现。
- S1～S3、S5～S7 缺少主方法，属于覆盖缺失，不分配反例编号。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 7 | 0 | 1 | 6 | 0 |

## 测试结论

FAIL。S4 被稳定持久化故障截断，其余六个场景没有 UC 主测试证据。

## 未解决事项

- 修复 CE-01 后原样复跑 S4。
- 为 S1～S3、S5～S7 补齐外派结果提交公开入口和唯一主测试方法。
