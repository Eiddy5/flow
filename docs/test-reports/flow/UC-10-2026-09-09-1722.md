# UC-10 用户处理包含嵌套编排的审批 Flow：默认队列切换 Pulsar 后的真实回归

## 基本信息与范围

时间 2026-09-09-1722 Asia/Shanghai；代码 5ce8b478676dd17c215bec83427bceb494cebff6 **dirty**。目标 [UC-10 用户处理包含嵌套编排的审批 Flow.md](</System/Volumes/Data/workspace/java/flow/docs/uc/flow/UC-10 用户处理包含嵌套编排的审批 Flow.md>)（ACCEPTED）；UC SHA256 `619a6fb52e353dc832ac40709b422c11b80eb2db81d9e43e8d169228f416fe61`。唯一主类 [Uc10NestedApprovalFlowTest](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java)，SHA256 `2cf8ea86386a8f2072c01fd9cee7b4dc089629c0ab0e666d79cdbd16bf1f1c9b`。

本 Test Agent 保留 UC/主类预期，仅修改共用 WorkflowUcFixture 使实际执行走 Pulsar 并管理专属环境；另新增普通技术测试/辅助类和迁移必要架构断言。没有修改生产实现或 UC。相对前次报告已有的 Pause 字段、业务输入及 lineage 改动按当前源码保留，不归入队列实现变更。

独立命令 `./gradlew :core:test --rerun --tests '*Uc10NestedApprovalFlowTest'`，追加公共选项；日志 `/System/Volumes/Data/workspace/java/flow/core/build/pulsar-replacement-20260909/Uc10NestedApprovalFlowTest.log`；XML `/System/Volumes/Data/workspace/java/flow/core/build/pulsar-replacement-20260909/Uc10NestedApprovalFlowTest/core/test/xml/TEST-org.cses.flow.core.services.executions.Uc10NestedApprovalFlowTest.xml`（timestamp `2026-09-09T09:14:07.690Z`），13 tests / 13 passed / 0 failed / 0 skipped。之后单方法复验/模块/完整回归在编译阶段阻断，未再次执行。未补齐的历史断言缺口仍标 NOT_COVERED。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 多级串签审批通过 | [s1SequentialNestedApprovalsCompleteOnlyAfterAllApprovals](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java:38) | PASS | JUnit PASS。初审→财务→法务逐步解锁，未解锁审批/记录/结束均不存在；完整顺序、审批/记录/结束各一次，SUCCESS、无待办。 |
| S2 初审驳回后短路结束 | [s2RejectedInitialApprovalShortCircuitsNestedApprovalBranch](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java:137) | PASS | JUnit PASS。初审 REJECTED 只执行驳回记录及结束，无财务/法务/批准记录，正式 SKIPPED 分支历史保留，SUCCESS。 |
| S3 并行会签后进入终审 | [s3ParallelNestedApprovalsJoinBeforeFinalApproval](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java:180) | PASS | JUnit PASS。两个会签分支独立按负责人→复核推进，全部完成前无终审；终审后汇总/收尾各一次、SUCCESS。 |
| S4 并行会签中途取消 | [s4CancelingParallelNestedApprovalsPreventsRemainingAndFollowingSteps](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java:293) | PASS | JUnit PASS。一支负责人通过后取消，两未完审批恢复均拒绝；根 KILLED，无其他复核、终审、汇总、发布或通知。 |
| S5 补充材料后重新审批 | [s5ConditionalNestedReviewLoopReachesFinalApprovalAfterSupplement](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java:373) | PASS | JUnit PASS。第一轮补材/复核 NEEDS_MORE 后才建第二轮，第二轮通过后仅一次终审/记录，无未完成轮次或审批。 |
| S6 补充材料超过最大次数 | [s6ConditionalNestedReviewLoopFailsAtMaximumWithoutFinalApproval](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java:466) | PASS | JUnit PASS。两轮补材与复核未通过后 FAILED，准确两轮且无终审/记录，无未完成任务。 |
| S7 同一条件分支内退回上一审批 | [s7SameConditionalBranchRewindsAndRejectsOldSource](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java:526) | PASS | JUnit PASS。同条件分支越过通知退回，新实例接替、旧实例停止；旧实例恢复/退回拒绝无额外实例，片段重做、未命中不执行、最终记录使用 NEW。 |
| S8 条件分支首个审批退回分流前审批 | [s8FirstConditionalApprovalRewindsBeforeTheSplit](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java:571) | PASS | JUnit PASS。分支首个审批跨通知/条件退回分流前审批，新实例接替；前置仅一次，原命中分支重做、未命中不执行，最新结果被消费并完成。 |
| S9 条件汇合后退回实际命中分支的最后审批 | [s9AfterConditionalMergeRewindsOnlyTheActuallyExecutedBranch](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java:606) | PASS | JUnit PASS。两金额实例分别退回实际命中分支、互不影响；公开候选准确，不执行未命中分支，前置一次，新终审消费本路径 NEW，两个新实例完成。 |
| S10 深层条件内审批退回外层分支审批 | [s10DeepConditionalApprovalRewindsToOuterApproval](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java:660) | PASS | JUnit PASS。四层条件内审批退回外层，先外层再重新到深层，各未命中分支均不执行；记录和结束各一次、采用最新值、旧实例停止。 |
| S11 并行分支内退回保留其他分支的未完成进度 | [s11BranchRewindKeepsTheOtherPausedBranchValid](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java:707) | PASS | JUnit PASS。第一支退回时沿用第二支原审批/准备 ID 和进度；旧实例提交拒绝，新 ID+原审批 ID 可恢复且不改旧历史；不提前汇合，最终消费 NEW/KEPT。 |
| S12 并行汇合后只退回用户选定的上一分支审批 | [s12AfterParallelMergeRewindsOnlyTheSelectedPredecessor](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java:761) | PASS | JUnit PASS。汇合后明确选择更早完成的第一支，仅该支重做；第二支原 ID/结果有效并标为继承，终审消费第一支 NEW 与第二支 KEPT，完成。 |
| S13 跨条件退回后再次退回使用当前有效审批 | [s13RepeatedConditionalRewindUsesOnlyTheCurrentEffectiveApprovals](/System/Volumes/Data/workspace/java/flow/core/src/test/java/org/cses/flow/core/services/executions/Uc10NestedApprovalFlowTest.java:810) | PASS | JUnit PASS。连续两次条件退回只接受当前有效目标，旧目标/旧源恢复拒绝且无新实例副作用；三实例分离、未命中不执行、两次原因保留，最终 NEWEST。 |

## 数量汇总与结论

13 场景：PASS 13 / FAIL 0 / NOT_COVERED 0 / BLOCKED 0。全部场景在编译前真实队列执行快照有通过证据；之后的工作区编译被并行改动阻断。 这不表示全项目测试通过。

## 环境、命令与证据

本轮使用真实 Micronaut、PAAS Client/Factory/PulsarConsumerRunner、Pulsar 4.0.7 standalone 和 PostgreSQL 17。默认 Executor 的命令/事件均由注解接入 Pulsar；没有使用 PostgreSQL DispatchQueue 或内存队列作为 UC 运行路径。旧 DispatchQueue 只保留独立适配器技术测试。宿主配置由环境变量传入，无新增队列 YAML。

```bash
export JAVA_HOME=/Users/cses-7/Library/Java/JavaVirtualMachines/ms-25.0.4/Contents/Home
export FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:55170/flow
export FLOW_POSTGRES_TEST_USER=flow
export FLOW_POSTGRES_TEST_CLEANUP=true
export FLOW_POSTGRES_DEFAULT_TEST_URL=
export FLOW_PULSAR_TEST_URL=pulsar://127.0.0.1:56650
export FLOW_PULSAR_TEST_ADMIN_URL=http://127.0.0.1:58081
# PostgreSQL 测试密码由 FLOW_POSTGRES_TEST_PASSWORD / FLOW_DATASOURCE_PASSWORD 注入
```

测试专属容器 `flow-pulsar-replacement-postgres-0909` 与 `flow-pulsar-replacement-broker-0909`，未使用已有业务库/Broker。Broker 镜像 digest `sha256:4614ddfddb711796314911fd1447d33523f2f2b498e56620dfdcdd627f94aeec`。Broker 容器内外均使用 56650，`advertised-address=127.0.0.1`；admin 映射 58081→8080，启动 `standalone --no-functions-worker --no-stream-storage`。完整可复用命令见 `docs/harness/pulsar-queues.md`。实际 JDK 25.0.4，不是 Java 21 验证证据。

所有 Gradle 顺序执行。脚本 `/private/tmp/flow-pulsar-replacement-run.sh`；公共选项 `-I /private/tmp/flow-pulsar-uc-isolated.gradle --offline --max-workers=1 --console=plain`，每阶段 `FLOW_TEST_RUN_DIR=/System/Volumes/Data/workspace/java/flow/core/build/pulsar-replacement-20260909/<阶段>`。init script 只隔离 binary/XML/HTML 并关闭标准流展示，不替换业务实现或断言。

| 阶段 | Wrapper 命令主体 | 结果 | 证据目录或日志 |
| --- | --- | --- | --- |
| 定向技术（包含编译） | `./gradlew :core:test --tests '*ExecutorPulsarIntegrationTest' --tests '*PulsarQueueTest'` | 9 tests / 9 passed / 0 failed / 0 skipped，exit 0 | `technical/`、`technical.log` |
| 生产闭环与架构 smoke | `./gradlew :core:test --tests '*Uc04PauseFlowTest.s1UserQueriesAndResumesOnePausedFlow' --tests '*CoreArchitectureStandardTest.executorUsesTheAnnotationDrivenPulsarQueues' --tests '*CoreArchitectureStandardTest.queueContractsRemainTopLevelPeers'` | 3 tests / 3 passed / 0 failed / 0 skipped，exit 0 | `smoke2/`、`smoke2.log` |
| 首次业务失败复验 | `./gradlew :core:test --rerun --tests '*Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance'` | not_run（core:compileJava blocked），exit 1 | `retry-Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance/` |
| Core 模块 | `./gradlew :core:test --rerun` | not_run（core:compileJava blocked），exit 1 | `core/`、`core.log` |
| 全项目 | `./gradlew test --rerun --continue` | Core/Server：not_run（core:compileJava blocked），exit 1 | `full/`、`full.log` |

上表相对证据路径均位于 `/System/Volumes/Data/workspace/java/flow/core/build/pulsar-replacement-20260909`；XML 路径为 `<阶段>/<模块>/test/xml/TEST-*.xml`。新增架构检查最初缺少静态 assertFalse 导入，仅测试编译失败（`smoke.log`），修正后 smoke2 通过；不把未运行计为失败业务场景。

## 编译阻断与快照边界

全部独立 UC 完成后，另一项并行任务把 RunnableTask.run 的返回类型改为 Output，而 WorkerDispatcher.java:20 仍接收 RunResult。S8 复验、Core 和全项目三次 Wrapper 命令均报“推论变量 T 具有不兼容的上限：RunResult/Object；下限：Output”，停在 core:compileJava；因此三个阶段都是 not_run/compile blocked，没有新的模块或全量测试统计。日志保留完整编译错误。

主 Agent 保存的 10 项队列生产 SHA256 核对全部通过；Test Agent 未修改/回退并行任务源码。独立 UC 与 9 项专项属于编译前快照，不能说明当前整个工作区可编译或通过。最后仅在专项测试 finally 增补清理已有生产订阅积压，避免真实固定 Topic 测试消息影响后续运行；这个测试清理调整尚未重新执行，因为当前生产编译阻断。测试与报告静态 diff --check 通过。

| 类与方法 | 本轮状态 |
| --- | --- |
| `core:compileJava` | 编译阻断：WorkerDispatcher.java:20，Output 不能赋给 RunResult；没有运行测试。 |

本轮模块/全量没有执行，不能重新确认旧普通技术失败或断言其消失。之前报告中的 State、旧架构路径、NestedPause、Log 差异仅作为历史待处理事项。新架构方法已在编译前 smoke 中验证两条注解绑定及 Queue 发布接口；旧缺失路径检查仍保留，未修旧实现或改变 UC 预期。

`skipped`：已执行独立 UC、技术与 smoke 阶段均为 0。双数据库隔离所需 FLOW_POSTGRES_DEFAULT_TEST_URL 未配置，但本轮完整测试因编译阻断未执行到该测试，不能记作实际 skipped。`not_run`：默认 opt-in queue-load、负载、集群故障、真实外部 HTTP/界面与通知验收。所有选定 UC 主类均已执行，不能把 skipped 或未运行项记为通过。S8单方法/Core/full 的退出码均为1，测试均 not_run，不把编译阻断算作已执行业务场景 FAIL。

## 失败、反例与未解决事项

CE-01（既有、未解决）：UC04 S8 通过公开 Service 发布 Pause → ThrowingTask → never-run，启动并查询 Pause，提交 `{"decision":"APPROVED"}`；后置任务抛出 `uc04-downstream-failure`。UC 要求明确错误、回到提交前 PAUSED 且不留下部分后置处理，随后由用户取消。实际执行进入 FAILED，断言 `expected: <PAUSED> but was: <FAILED>`。本轮真实 Pulsar 下整类执行保留该差异；随后单方法复验、Core 和完整回归均在编译阶段阻断，没有再次运行。用户取消收尾未执行。复现：`./gradlew :core:test --rerun --tests '*Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance'`（使用下述环境和公共选项）。物理清理不补足用户闭环。历史 BATCH-2026-09-09-1629.md 在替换前也记录同一差异；没有做 git 二分，不把历史对照视为严格归因。


## 活动资源、清理和验证边界

最终 PostgreSQL 只读审计：

```text
BEGIN
executions_total=0, active=0
task_runs_total=0, paused=0, other_active=0
inherited_total=0, paused=0, other_active=0
queues_total=0
flows_total=0
flow_tasks_total=0
COMMIT
```

最终真实 Broker 只读审计：

```json
{
  "command": {
    "messagesIn": 208,
    "messagesOut": 208,
    "publishers": 0,
    "subscriptions": {
      "flow-executor-command": {
        "backlog": 0,
        "unacked": 0,
        "consumers": 0
      }
    }
  },
  "event": {
    "messagesIn": 818,
    "messagesOut": 818,
    "publishers": 0,
    "subscriptions": {
      "flow-executor-event": {
        "backlog": 0,
        "unacked": 0,
        "consumers": 0
      }
    }
  }
}
```

证据 `/System/Volumes/Data/workspace/java/flow/core/build/pulsar-replacement-20260909/resources.sql`、`resources.log`、`broker-*.json`。Fixture 重启保留同一持久订阅，最终关闭真实消费者/生产者/Client 后清理两条测试订阅积压，再按随机 companyId 清理 PostgreSQL。失败或拒绝命令的积压物理清理不能作为用户终态证据。两个本次专属容器及匿名卷已删除，其他容器未操作，删除输出在 `container-cleanup.log`。

真实技术测试证实四种 ExecutionCommand 与 ExecutorEvent 经 PAAS schema 往返、Queue/PulsarQueue 同实例注入、回调未完成时 Broker backlog 保留、跨队列发布、抛错一次后 Broker 重投和最终 ACK 后积压及未确认数清零。原 7 项受控网络替身测试仍单独存在，不能代替真实 Broker 结果。尚未验证集群故障转移、Broker 重启恢复或 exactly-once；PAAS 异步 ACK 失败/在途关闭的已有边界不因本次替换消失。

## 本轮滚动保留

新增 12 份，按写入时当前目录清单备份并删除超额 12 份。保留数量：`{"BATCH": 3, "UC-01": 3, "UC-02": 3, "UC-03": 2, "UC-04": 3, "UC-05": 3, "UC-06": 3, "UC-07": 3, "UC-08": 3, "UC-09": 3, "UC-10": 3, "UC-11": 3, "UC-12": 3}`。非规范时间格式与其他领域文件保持不变。未解决 CE-01 已完整带入每份报告。
