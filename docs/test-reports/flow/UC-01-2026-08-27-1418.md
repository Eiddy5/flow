# UC-01 Flow 草稿生命周期与多租户管理测试报告

## 基本信息

- UC：`docs/uc/flow/UC-01 Flow 草稿生命周期与多租户管理.md`
- 主测试类：`core/src/test/java/org/cses/flow/core/services/flows/Uc01FlowLifecycleTest.java`
- 执行时间：2026-08-27 14:04～14:17 +08:00
- 版本：`070da94`，工作区 `dirty`
- 环境：Java 21.0.12、真实 Micronaut、PostgreSQL 17.10、临时库 `flow_uc_20260827_140410`

## 测试范围

验证 UC-01 S1～S13，覆盖草稿创建、查询、修改、发布、删除、租户隔离、审计与系统字段保护；用户入口为 `FlowService`。

## 场景验证

| 场景 | 测试方法 | 状态 | 结束证据 |
| --- | --- | --- | --- |
| S1 用户完成草稿的完整生命周期 | `s1UserCompletesTheDraftLifecycle` | FAIL | 首次保存即因 `ActorRef` 无法序列化而终止，未进入生命周期闭环。 |
| S2 用户保存无效草稿并从发布失败中恢复 | `s2UserRecoversFromAnInvalidDeployment` | FAIL | 保存对照草稿时命中 CE-01，未到达恢复路径。 |
| S3 两个租户独立管理内容相同的草稿 | `s3TenantsManageIdenticalDraftsIndependently` | FAIL | 首个租户草稿无法持久化，未形成隔离证据。 |
| S4 同一租户的不同用户创建和修改草稿 | `s4UsersInOneTenantPreserveCreationAudit` | FAIL | 创建阶段命中 CE-01，未到达审计断言。 |
| S5 用户查询不存在或错误的草稿编号 | `s5WrongDraftKeysDoNotAffectTheControlDraft` | FAIL | 对照草稿无法保存，无法验证错误编号无副作用。 |
| S6 缺少租户身份的用户管理草稿 | `s6MissingTenantIdentityCannotManageDrafts` | FAIL | 建立对照草稿时命中 CE-01，场景闭环未完成。 |
| S7 其他租户查询草稿 | `s7AnotherTenantCannotQueryTheDraft` | FAIL | 所有者草稿无法保存，无法验证跨租户查询。 |
| S8 其他租户修改草稿 | `s8AnotherTenantGetsAnIndependentDraft` | FAIL | 首个草稿保存失败，未到达越权修改证据。 |
| S9 其他租户删除草稿 | `s9AnotherTenantCannotDeleteTheDraft` | FAIL | 对照草稿保存失败，未到达越权删除证据。 |
| S10 其他租户发布草稿 | `s10AnotherTenantCannotDeployTheDraft` | FAIL | 对照草稿保存失败，未到达越权发布证据。 |
| S11 用户在删除后继续修改或发布草稿 | `s11DeletedDraftCannotBeModifiedOrDeployed` | FAIL | 初始草稿无法持久化，未到达删除终态。 |
| S12 用户混用草稿查询和正式 Flow 查询 | `s12DraftAndDeployedFlowQueriesStaySeparate` | FAIL | 草稿保存失败，未形成草稿/正式查询隔离证据。 |
| S13 用户在草稿内容中填写系统管理字段 | `s13SystemFieldsCannotBeInjectedIntoDraft` | PASS | 非法系统字段被拒绝，结束时无草稿、正式 Flow、Execution 或队列资源。 |

## 执行命令与结果

- 主类：13 tests，1 passed，12 failed。
- 命令：`FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:5432/flow_uc_20260827_140410 FLOW_POSTGRES_TEST_USER=flow FLOW_POSTGRES_TEST_PASSWORD=flow FLOW_POSTGRES_TEST_CLEANUP=true ./gradlew :core:test --tests '*Uc01FlowLifecycleTest' --rerun-tasks --max-workers=1 --no-daemon --console=plain`。
- S1 单方法复跑：1 test，1 failed，异常签名不变。

## 失败与反例

- CE-01 输入：各方法内嵌的租户/用户身份与 YAML 草稿，通过 `FlowService` 保存或发布。
- 预期：S1～S12 应继续完成各自生命周期、隔离或错误恢复断言。
- 实际：均在首次建立持久化对照资源时抛出 `InvalidDefinitionException: No serializer found for class org.cses.flow.core.domains.ActorRef and no properties discovered to create BeanSerializer`。
- 影响：S1～S12 无法到达场景结束状态；S1 单方法复跑稳定失败。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 13 | 1 | 12 | 0 | 0 |

## 测试结论

FAIL。只有不进入持久化成功路径的 S13 通过；S1～S12 均被稳定的 `ActorRef` 序列化故障截断。

## 未解决事项

- 修复 CE-01 后原样复跑 S1～S12；不得放宽 UC 预期。
