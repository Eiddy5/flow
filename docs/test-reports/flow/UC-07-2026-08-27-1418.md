# UC-07 用户处理多阶段外派流程测试报告

## 基本信息

- UC：`docs/uc/flow/UC-07 用户处理多阶段外派流程.md`
- 主测试类：`core/src/test/java/org/cses/flow/core/services/executions/Uc07NestedTaskFlowTest.java`
- 执行时间：2026-08-27 14:04～14:17 +08:00
- 版本：`070da94`，工作区 `dirty`
- 环境：Java 21.0.12、真实 Micronaut、PostgreSQL 17.10、临时库 `flow_uc_20260827_140410`

## 测试范围

验证 UC-07 S1～S7，覆盖多阶段外派、重复查询、嵌套标识校验、深层取消、并行顺序和串行默认语义。当前主测试类只映射 S3。

## 场景验证

| 场景 | 测试方法 | 状态 | 结束证据 |
| --- | --- | --- | --- |
| S1 用户完成完整多阶段流程 | — | NOT_COVERED | 主测试类没有 S1 方法。 |
| S2 用户在每个等待阶段重新查询 | — | NOT_COVERED | 主测试类没有 S2 方法。 |
| S3 用户发布包含跨层重复步骤标识的 Flow | `s3RejectsDuplicateKeyAcrossNestedBranchesAtomically` | FAIL | 保存待发布 Flow 时命中 CE-01，未到达重复标识原子拒绝证据。 |
| S4 用户在深层并行等待时取消流程 | — | NOT_COVERED | 主测试类没有 S4 方法。 |
| S5 用户反转两个并行复核的完成顺序 | — | NOT_COVERED | 主测试类没有 S5 方法。 |
| S6 用户运行调整过复核层级的 Flow | — | NOT_COVERED | 主测试类没有 S6 方法。 |
| S7 用户未添加并行节点时同级步骤保持串行 | — | NOT_COVERED | 主测试类没有 S7 方法。 |

## 执行命令与结果

- 主类：1 test，1 failed。
- 命令：`FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:5432/flow_uc_20260827_140410 FLOW_POSTGRES_TEST_USER=flow FLOW_POSTGRES_TEST_PASSWORD=flow FLOW_POSTGRES_TEST_CLEANUP=true ./gradlew :core:test --tests '*Uc07NestedTaskFlowTest' --rerun-tasks --max-workers=1 --no-daemon --console=plain`。

## 失败与反例

- CE-01 输入：S3 使用跨嵌套分支含重复步骤标识的 YAML Flow 并尝试发布。
- 预期：发布被原子拒绝，且不留下正式或运行资源。
- 实际：建立持久化草稿时先抛出 `ActorRef` 无可用 Jackson serializer 的 `InvalidDefinitionException`。
- 影响：未到达跨层重复标识校验；同一方法又在 Core 全量回归中复现。
- S1～S2、S4～S7 缺少主方法，属于覆盖缺失，不分配反例编号。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 7 | 0 | 1 | 6 | 0 |

## 测试结论

FAIL。S3 被稳定持久化故障截断，其余六个场景没有 UC 主测试证据。

## 未解决事项

- 修复 CE-01 后原样复跑 S3。
- 为 S1～S2、S4～S7 补齐外派用户闭环和唯一主测试方法。
