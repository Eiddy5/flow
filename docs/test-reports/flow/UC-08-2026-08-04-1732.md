# UC-08 用户在流程中输出动态日志测试报告

## 基本信息

- UC 文档：`docs/uc/flow/UC-08 用户在流程中输出动态日志.md`
- 主测试类：`server/src/test/java/org/cses/flow/core/services/executions/Uc08DynamicLogFlowTest.java`
- 执行时间：`2026-08-04 17:30 CST`
- 代码版本：`0d11c686cdcd6b8c101f34f24cad4839f65a2fdd`，工作区 `dirty`
- 环境：Java 21；未提供 UC 所需的 PostgreSQL 连接环境变量
- 主测试类是否修改：是；新增 UC-08 三个场景的完整用户路径测试

## 测试范围

验证动态日志成功渲染并继续流程、无效表达式发布拒绝，以及运行值缺失时失败且不
输出误导日志。UC 要求使用真实 PostgreSQL 和 Micronaut 生产装配。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 | `s1RendersPrecedingResultOnceAndContinuesTheFlow` | BLOCKED | 测试在创建 PostgreSQL UC 夹具前因缺少 `FLOW_POSTGRES_TEST_URL` 停止，尚未形成真实数据库执行证据 |
| S2 | `s2RejectsInvalidMessageExpressionBeforePublishing` | BLOCKED | 测试在创建 PostgreSQL UC 夹具前因缺少 `FLOW_POSTGRES_TEST_URL` 停止，尚未形成发布拒绝和清理证据 |
| S3 | `s3MissingRuntimePathFailsWithoutWritingAMisleadingLog` | BLOCKED | 测试在创建 PostgreSQL UC 夹具前因缺少 `FLOW_POSTGRES_TEST_URL` 停止，尚未形成失败终态和日志证据 |

## 执行命令与结果

- `JAVA_HOME=/Users/cses-7/Library/Java/JavaVirtualMachines/ms-21.0.12/Contents/Home ./gradlew :server:test --tests 'org.cses.flow.core.services.executions.Uc08DynamicLogFlowTest'`：3 tests，3 tests 在夹具启动前被环境阻塞，`BUILD FAILED`。
- 阻塞信息：`FLOW_POSTGRES_TEST_URL is required for UC tests`。
- `LogFlowIntegrationTest` 使用现有内存装配验证了相同 Executor/Worker 推进和动态日志输出，执行成功；根据 UC 测试规范，该结果不替代真实 PostgreSQL UC 证据。

## 失败与反例

没有观察到实现失败或稳定反例；本次失败属于验证环境缺失。

## 数量汇总

- PASS：0
- FAIL：0
- NOT_COVERED：0
- BLOCKED：3

## 测试结论

BLOCKED。UC-08 自动化覆盖已经建立，但当前环境无法完成真实 PostgreSQL 验证。

## 未解决事项

- 配置 `FLOW_POSTGRES_TEST_URL`、`FLOW_POSTGRES_TEST_USER` 和
  `FLOW_POSTGRES_TEST_PASSWORD` 后重新运行 UC-08 主测试类。
- 当前技术测试已经验证模板语法、运行值提取、日志输出、插件 Schema、持久化映射
  和内存执行链路，不应将这些结果记为 UC PASS。
