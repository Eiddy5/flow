# UC-06 用户提交外派结果后的条件路径测试报告

## 基本信息

- UC 文档：`docs/uc/flow/UC-06 用户提交外派结果后的条件路径.md`
- 主测试类：`server/src/test/java/org/cses/flow/core/services/externaltasks/Uc06ConditionalRouteTest.java`
- 验证领域：`flow`
- 测试类型：领域模型校正后的完整回归
- 执行时间：`2026-07-29 15:55-16:03 CST`
- 代码版本：`4cce0b0`，工作区 `dirty`
- 上次报告：`docs/test-reports/flow/UC-06-2026-07-29-1146.md`

## 测试范围

验证 YAML 中的 `RouteExpression` 在部署时物化并校验，以及运行时基于 Task
`Output` 的条件匹配、无匹配路径、大小写语义与实例隔离。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 | `s1RunsOnlyApprovedBranch` | PASS | APPROVED Output 只选择批准分支，拒绝分支不运行且流程 COMPLETED |
| S2 | `s2RunsOnlyRejectedBranch` | PASS | REJECTED Output 只选择拒绝分支，批准分支不运行且流程 COMPLETED |
| S3 | `s3NoMatchCompletesWithoutCreatingCandidateRuns` | PASS | 未知值不选择候选分支、不创建候选 TaskRun，流程正常完成 |
| S4 | `s4RejectsInvalidRouteWhenDeployingFlow` | PASS | 非法条件可保留为原始来源，但部署被明确拒绝，不产生正式 Flow 或 Execution；修正后可部署 |
| S5 | `s5MissingOutputDoesNotSelectAnyBranch` | PASS | 缺少决定 Output 时不选择任何候选分支，也不采用默认路径 |
| S6 | `s6RouteComparisonIsCaseSensitive` | PASS | 小写值不匹配大写条件，不发生隐式大小写转换 |
| S7 | `s7ExternalTaskIdRoutesOnlyItsOwningExecution` | PASS | 每次提交只推进所选任务所属 Execution，两个实例的 Output 和路径不串用 |

## 执行命令与结果

- UC-01～UC-07 主测试类联合执行：56 tests，0 failures，0 errors，
  0 skipped；本主测试类 7 tests 全部通过。
- `./gradlew test`（配置真实 PostgreSQL 测试连接）：98 tests，
  0 failures，0 errors，0 skipped。

## 失败与反例

无失败；无新增或复验反例。

## 数量汇总

- PASS：7
- FAIL：0
- NOT_COVERED：0
- BLOCKED：0

## 测试结论

PASS。条件路由的部署期校验和运行期 Output 匹配满足 UC-06 全部场景。

## 未解决事项

无。
