# UC-04 用户处理外派任务并恢复流程测试报告

## 基本信息

- UC：`docs/uc/flow/UC-04 用户处理外派任务并恢复流程.md`
- 主测试类：未建立
- 执行时间：2026-08-27 14:04～14:17 +08:00
- 版本：`070da94`，工作区 `dirty`
- 环境盘点：Java 21.0.12、真实 Micronaut、PostgreSQL 17.10

## 测试范围

盘点 UC-04 S1～S9 的主测试与公开用户入口，范围包含外派任务查询、完成、隔离、幂等、取消竞争、后续失败和重启恢复。

## 场景验证

| 场景 | 测试方法 | 状态 | 结束证据 |
| --- | --- | --- | --- |
| S1 用户查询并完成单个外派任务 | — | NOT_COVERED | 没有 UC-04 主测试类和公开用户路径证据。 |
| S2 用户提交结果后只恢复对应流程 | — | NOT_COVERED | 没有唯一主测试映射。 |
| S3 非法输入后用户完成正确任务 | — | NOT_COVERED | 没有唯一主测试映射。 |
| S4 用户重复完成已经结束的外派任务 | — | NOT_COVERED | 没有唯一主测试映射。 |
| S5 其他租户查询或完成外派任务 | — | NOT_COVERED | 没有唯一主测试映射。 |
| S6 同一用户在两个流程间选择另一个任务 | — | NOT_COVERED | 没有唯一主测试映射。 |
| S7 流程取消后用户完成原外派任务 | — | NOT_COVERED | 没有唯一主测试映射。 |
| S8 外派结果提交后的后续处理发生异常 | — | NOT_COVERED | 没有唯一主测试映射。 |
| S9 系统重启后用户继续处理外派任务 | — | NOT_COVERED | 没有唯一主测试映射。 |

## 执行命令与结果

- 没有可执行的 UC-04 唯一主测试类，因此未伪造 `--tests` 目标。
- 全量 `:core:test` 中的暂停/恢复技术测试已执行，但不能替代 UC-04 用户路径证据。

## 失败与反例

当前代码中存在暂停/恢复补充技术测试，但没有 UC-04 唯一主测试类，也缺少可由用户查询、鉴权和完成外派任务的完整公开入口。按 UC 测试规范，不能读取内部 `TaskRun` 或仓储状态来冒充用户结果，因此不执行伪造场景并统一标记 `NOT_COVERED`。

覆盖缺失不是实现反例，本报告不分配 `CE-*`。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 9 | 0 | 0 | 9 | 0 |

## 测试结论

NOT_COVERED。UC-04 九个场景均缺少合规主测试与公开用户闭环证据。

## 未解决事项

- 建立 UC-04 唯一主测试类，并补齐 S1～S9 的公开查询、权限校验、提交和恢复入口证据。
