# UC-01 创建并发布 Flow PostgreSQL 复验报告

## 基本信息

- UC：`docs/uc/flow/UC-01 创建并发布 Flow.md`
- 主测试类：`server/src/test/java/org/cses/flow/core/services/flows/Uc01FlowLifecycleTest.java`
- 领域：`flow`
- 类型：真实 PostgreSQL 持久化复验
- 时间：2026-07-28 18:19-18:23 CST
- 代码版本：`b665c29`，工作区 `dirty`

## 范围与环境

测试通过 Micronaut 装配的 `FlowService -> CommandExecutor -> Handler ->
FlowPostgresRepository` 链路执行。数据库为临时 PostgreSQL 16，已执行
`001_create_flow_tables.sql` 和 `003_align_temporal_columns.sql`。未装配 Flow
内存 Repository。

## 场景与 PASS 追踪

| 场景 | 测试方法 | PASS | 状态 | 实际证据 |
| --- | --- | --- | --- | --- |
| S1 创建并编辑 DRAFT | `s1CreatesAndEditsDraftWithStableIdentity` | PASS-S1-01、PASS-S1-02、PASS-S1-03 | PASS | PostgreSQL 重读后 id 稳定、version 为空、只保留最后合法内容 |
| S2 发布、升级并关闭 | `s2PublishesUpgradesAndClosesFlow` | PASS-S2-01、PASS-S2-02、PASS-S2-03 | PASS | version 1/2、历史 CLOSE、当前关闭及草稿清理均由公开查询确认 |
| S3 非法定义与生命周期 | `s3RejectsInvalidDefinitionsAndLifecycleOperationsAtomically` | PASS-S3-01、PASS-S3-02 | PASS | 异常类型明确，事务回滚后状态、版本、内容与数据库快照不变 |
| S4 租户隔离 | `s4IsolatesFlowReadsAndWritesByTenant` | PASS-S4-01、PASS-S4-02 | PASS | 跨租户不可见且修改被拒绝，双方数据无副作用 |
| S5 精确引用 | `s5RequiresExactVersionAndStatusReferences` | PASS-S5-01、PASS-S5-02 | PASS | 错误 version/status 不返回其他持久化快照 |
| S6 Task 身份 | `s6KeepsTaskIdentityStableAcrossVersions` | PASS-S6-01、PASS-S6-02 | PASS | 保留 key 的 taskId 稳定，新增 Task 使用新 id，旧版本不变 |
| S7 陈旧写冲突 | `s7RejectsAStaleDraftSaveWithoutOverwritingCommittedContent` | PASS-S7-01、PASS-S7-02 | PASS | 旧 revision 写入被 PostgreSQL 乐观锁拒绝，已提交草稿完整可读 |

## 执行结果

- 目标类：7 tests，0 failures，0 errors，0 skipped。
- `./gradlew :server:test`：69 tests，0 failures，0 errors，0 skipped。
- `./gradlew test --rerun-tasks`：BUILD SUCCESSFUL。
- 测试后 `flows`、`flow_drafts`、`flow_tasks`、`executions`、`task_run`、
  `assignment` 均为 0 行。

JUnit XML：
`server/build/test-results/test/TEST-org.cses.flow.core.services.flows.Uc01FlowLifecycleTest.xml`。
HTML：`server/build/reports/tests/test/index.html`。

## 结论

16 条 PASS 全部通过；FAIL、NOT_COVERED、BLOCKED 均为 0。首次真实库执行发现
Task 重建后缺少快照值相等语义，修复并以原场景复验通过；无未解决反例。
