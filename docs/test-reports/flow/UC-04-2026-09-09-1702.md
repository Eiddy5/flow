# UC-04 Pause 字段更名回归报告

## 基本信息

- 时间：2026-09-09 17:02，Asia/Shanghai。
- 代码：`5ce8b478676dd17c215bec83427bceb494cebff6 dirty`；共享工作区包含已有及并行修改，未冻结。
- UC：`docs/uc/flow/UC-04 用户恢复处于 Pause 的 Flow.md`，ACCEPTED。
- 唯一主测试：`core/src/test/java/org/cses/flow/core/services/executions/Uc04PauseFlowTest.java`；本轮只将验证 YAML 的 `pause/resume` 改成 `onPause/onResume`，未修改场景、断言强度或 UC。

## 测试范围

同步直接相关 Core 测试、夹具和定义资源；复用现有反序列化、Schema、持久化往返和 Server 视图测试验证新属性。确认旧 `pause/resume` 定义字段被拒绝，新名往返只输出 `onPause/onResume`，Schema 必填关系保持，HTTP TaskView 使用新 JSON 名。

27 项定向技术测试全部通过，且完整回归中的同名 27 项仍全部通过。技术检查不替代 UC 验收。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 | `s1UserQueriesAndResumesOnePausedFlow()` | BLOCKED | 实际执行在夹具检查处抛出 `FLOW_PULSAR_TEST_URL is required for real Pulsar tests`；未进入用户业务路径，不能验证预期与结束状态。 |
| S2 | `s2ResumeResultIsConsumedOnceByOnlyTheCurrentFlow()` | BLOCKED | 实际执行在夹具检查处抛出 `FLOW_PULSAR_TEST_URL is required for real Pulsar tests`；未进入用户业务路径，不能验证预期与结束状态。 |
| S3 | `s3IllegalResumeInputsAreRejectedUntilValidResultIsSubmitted()` | BLOCKED | 实际执行在夹具检查处抛出 `FLOW_PULSAR_TEST_URL is required for real Pulsar tests`；未进入用户业务路径，不能验证预期与结束状态。 |
| S4 | `s4RepeatedResumeOfFinishedPauseIsRejectedWithoutMutation()` | BLOCKED | 实际执行在夹具检查处抛出 `FLOW_PULSAR_TEST_URL is required for real Pulsar tests`；未进入用户业务路径，不能验证预期与结束状态。 |
| S5 | `s5OtherTenantCannotQueryOrResumeThePausedFlow()` | BLOCKED | 实际执行在夹具检查处抛出 `FLOW_PULSAR_TEST_URL is required for real Pulsar tests`；未进入用户业务路径，不能验证预期与结束状态。 |
| S6 | `s6SameUserResumesTheSelectedInstanceOnly()` | BLOCKED | 实际执行在夹具检查处抛出 `FLOW_PULSAR_TEST_URL is required for real Pulsar tests`；未进入用户业务路径，不能验证预期与结束状态。 |
| S7 | `s7CancelledFlowRejectsResumeOfItsFormerPause()` | BLOCKED | 实际执行在夹具检查处抛出 `FLOW_PULSAR_TEST_URL is required for real Pulsar tests`；未进入用户业务路径，不能验证预期与结束状态。 |
| S8 | `s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance()` | BLOCKED | 实际执行在夹具检查处抛出 `FLOW_PULSAR_TEST_URL is required for real Pulsar tests`；未进入用户业务路径，不能验证预期与结束状态。 |
| S9 | `s9FreshServerCanRequeryAndResumeTheSamePause()` | BLOCKED | 实际执行在夹具检查处抛出 `FLOW_PULSAR_TEST_URL is required for real Pulsar tests`；未进入用户业务路径，不能验证预期与结束状态。 |
| S10 | `s10UserRewindsFromPauseToHistoricalStepAndContinues()` | BLOCKED | 实际执行在夹具检查处抛出 `FLOW_PULSAR_TEST_URL is required for real Pulsar tests`；未进入用户业务路径，不能验证预期与结束状态。 |
| S11 | `s11UserRewindsAgainFromTheNewPause()` | BLOCKED | 实际执行在夹具检查处抛出 `FLOW_PULSAR_TEST_URL is required for real Pulsar tests`；未进入用户业务路径，不能验证预期与结束状态。 |

场景与主类仍为 S1–S11 一一对应，无新增、删除或禁用。S4 既有逐条 TaskRun 历史对照不足、S6 既有第二实例 SECOND 输出归属断言缺口仍未补齐；本轮均因更早环境阻塞，状态为 BLOCKED。

## 环境与执行命令

使用本任务独占 PostgreSQL 17 容器 `flow-pause-rename-0909`，`jdbc:postgresql://127.0.0.1:62440/flow`。当前 `gen/sql/flow/001_create_flow_tables.sql` 及表文件已在空库成功执行；真实 Micronaut、生产 Repository 与当前队列装配未替换为 Mock。

JDK 21.0.12 实际执行 `./gradlew :core:testClasses --offline --max-workers=1 --console=plain` 失败：当前已解析 Micronaut/PAAS 依赖要求 JVM 25。日志 `/private/tmp/flow-pause-java21.log`。后续使用本机 JDK 25.0.4，仅是 JDK 25 证据，不证明 Java 21 可用。

后续通过 `/private/tmp/flow-pause-run.sh <阶段> <Wrapper 参数>` 执行；它注入上述 PostgreSQL URL、用户 flow、`FLOW_POSTGRES_TEST_CLEANUP=true`，并使用 `-I /private/tmp/flow-pause-results.gradle --offline --max-workers=1 --console=plain` 隔离输出。未配置 `FLOW_PULSAR_TEST_URL`。

| 阶段 | 实际 Wrapper 命令主体 | 结果与证据 |
| --- | --- | --- |
| 目标编译及技术测试 | `./gradlew :core:test --tests '*PauseTest' --tests '*PluginDeserializerTest' --tests '*PluginSchemaGeneratorTest' --tests '*TaskDataPersistenceMappingTest' :server:test --tests '*InputViewTest'` | exit 0；Core 编译与 Server 编译成功，Core 25 passed，Server 2 passed；`core/build/pause-rename-0909/targeted/` |
| UC04 独立执行 | `./gradlew :core:test --rerun --tests '*Uc04PauseFlowTest'` | 当前生产队列尝试 `pulsar://localhost:6650`，连接拒绝；确认环境阻塞后终止本任务测试进程。部分 XML：3 tests，2 failures（Pulsar publication failed）、1 skipped/interrupted；其余方法未完成。exit 1。`core/build/pause-rename-0909/uc04/` |
| Core 模块及项目完整回归 | `./gradlew test --rerun --continue` | 自然结束，exit 1；Core 443 tests = 307 passed / 135 failed / 1 skipped；Server 19 passed / 0 failed / 0 skipped。合计 462 tests = 326 passed / 135 failed / 1 skipped。`core/build/pause-rename-0909/full/` |

工作区漂移：独立 UC04 与完整回归之间，其他并行工作更新了 `WorkflowUcFixture`，新增真实 Pulsar 环境要求。完整回归 UC04 11 个方法全部在此检查处退出；不能把两次源码快照混为一次稳定业务回归。本轮没有修复或撤回该并行修改。

定向类及完整回归复查数量一致：PauseTest 6、PluginDeserializerTest 4、PluginSchemaGeneratorTest 6、TaskDataPersistenceMappingTest 9、InputViewTest 2，均零失败零跳过。

## 失败分类与反例

完整 Core 的 135 项 JUnit 失败中：

- 132 项明确抛出 `FLOW_PULSAR_TEST_URL is required for real Pulsar tests`，属于环境阻塞，包含全部 UC04 场景。
- `CoreArchitectureStandardTest.executorAndWorkerAreTopLevelPeersOfCore`：架构断言 expected true but false。
- `CoreArchitectureStandardTest.taskCapabilitiesKeepOrchestrationOutOfWorkers`：`src/main/java/org/cses/flow/executor/handlers/ExecutorEventHandler.java` 不存在。
- `StateTest.rejectsInvalidHistoryAndIllegalGenericTransitions`：预期 WorkflowException，但未抛出。

后三项未单独复验和归因，不能全部称为已有失败，也不能据此归因于字段更名。按主 Agent 本轮范围限制未继续无关诊断；保留 XML 供后续处理。双数据源测试 1 项 skipped，不计入通过。

CE-01（历史未解决，本轮因环境未完成复验）：UC04 S8 发布 `uc04-s8-flow`（Pause → ThrowingTask → never-run），公开查询 Pause 后提交 `{"decision":"APPROVED"}`，后置任务抛出 `uc04-downstream-failure`。UC 要求明确异常、Flow/Pause 回到提交前 PAUSED、无部分后置结果，随后用户取消；最近既有报告记录实际为 FAILED，`expected PAUSED but was FAILED`。本轮不能更新该反例为已解决或重新失败。环境就绪后的复现命令为 `./gradlew :core:test --rerun --tests '*Uc04PauseFlowTest.s8DownstreamFailureMustLeavePauseWaitingAndNotPartiallyAdvance'`。

## 数量汇总与结论

UC 场景 11：PASS 0 / FAIL 0 / NOT_COVERED 0 / BLOCKED 11。字段改名技术检查 27/27 通过；UC04 和完整业务回归受真实 Pulsar 环境阻塞，完整测试也保留 3 项未归因的其他失败。未宣称 UC04 或全套验收通过。

## 资源与滚动保留

独占数据库最终只读审计：executions_total=0、active=0、task_runs_total=0、paused=0、queues_total=0；证据 `core/build/pause-rename-0909/resources.log`。数据库清理不是用户业务终态证据。测试进程已结束，独占 PostgreSQL 容器已删除，未操作其他容器。

本报告生成后按文件名时间对 UC-04 组保留最新 3 份，删除最早 1 份；其他 UC 与 BATCH 报告未变。未解决 CE-01 和覆盖缺口已带入本报告。

## 未解决事项

提供可用真实 Pulsar 测试环境后重跑 UC04 与全套；另行确认 3 项非环境失败的原因、S4/S6 断言缺口及 CE-01。Java 21 依赖兼容问题仍未解决。
