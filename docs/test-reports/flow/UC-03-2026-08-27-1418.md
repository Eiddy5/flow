# UC-03 用户运行自动流程测试报告

## 基本信息

- UC：`docs/uc/flow/UC-03 用户运行自动流程.md`
- 主测试类：`core/src/test/java/org/cses/flow/core/services/executions/Uc03AutomaticTaskFlowTest.java`
- 执行时间：2026-08-27 14:04～14:17 +08:00
- 版本：`070da94`，工作区 `dirty`
- 环境：Java 21.0.12、真实 Micronaut、PostgreSQL 17.10、临时库 `flow_uc_20260827_140410`

## 测试范围

验证 UC-03 S1～S3，覆盖自动任务成功、明确失败和意外异常三种结束路径。

## 场景验证

| 场景 | 测试方法 | 状态 | 结束证据 |
| --- | --- | --- | --- |
| S1 目标自动任务成功并传递结果 | `s1TargetAutomaticTaskConsumesInputAndPassesOutputOnce` | FAIL | 发布测试 Flow 时命中 CE-01，自动任务未开始。 |
| S2 目标自动任务明确失败 | `s2ExplicitAutomaticTaskFailureStopsFollowingTask` | FAIL | 发布测试 Flow 时命中 CE-01，未到达显式失败断言。 |
| S3 目标自动任务发生意外异常 | `s3UnexpectedAutomaticTaskExceptionBecomesAVisibleFailedRun` | FAIL | 发布测试 Flow 时命中 CE-01，未形成可见失败运行。 |

## 执行命令与结果

- 主类：3 tests，3 failed。
- 命令：`FLOW_POSTGRES_TEST_URL=jdbc:postgresql://127.0.0.1:5432/flow_uc_20260827_140410 FLOW_POSTGRES_TEST_USER=flow FLOW_POSTGRES_TEST_PASSWORD=flow FLOW_POSTGRES_TEST_CLEANUP=true ./gradlew :core:test --tests '*Uc03AutomaticTaskFlowTest' --rerun-tasks --max-workers=1 --no-daemon --console=plain`。

## 失败与反例

- CE-01 输入：三个方法分别发布成功、明确失败和抛异常的自动任务 Flow，并准备执行。
- 预期：分别形成成功结果传递、受控失败停止和可见意外失败。
- 实际：三个方法都在发布阶段抛出 `ActorRef` 无可用 Jackson serializer 的 `InvalidDefinitionException`。
- 影响：三个自动任务均未启动；同一方法又在 Core 全量回归中复现。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 3 | 0 | 3 | 0 | 0 |

## 测试结论

FAIL。三个自动流程场景都在 Flow 持久化阶段被稳定故障截断。

## 未解决事项

- 修复 CE-01 后原样复跑 S1～S3。
