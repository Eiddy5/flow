# UC-09 用户定义并运行循环流程测试报告

## 基本信息

- UC：`docs/uc/flow/UC-09 用户定义并运行循环流程.md`
- 主测试类：`core/src/test/java/org/cses/flow/core/services/executions/Uc09LoopOrchestrationTest.java`
- 主测试类修改：是，重写 S1/S2，使用户在每轮 Pause 等待点重新查询 Generation
- 执行时间：2026-08-31 12:51～13:02 +08:00
- 代码版本：`c50ff0d`，工作区 dirty
- 环境：Java 21、真实 Micronaut 装配、PostgreSQL 17.10，`jdbc:postgresql://localhost:55440/flow`
- 公开入口：`FlowService`、`ExecutionService`、持久化重启后的 `ExecutionService.execution(...)`
- 清理：`FLOW_POSTGRES_TEST_CLEANUP=true`

## 测试范围

本次完整执行 UC-09 S1～S6。S1/S2 通过公开 Service/Command Queue 完成“发布—启动—
每轮重新查询 Pause 和 Generation—恢复—查询终态”，并通过重建 `ApplicationContext`
验证循环 TaskRun Generation 的 PostgreSQL 往返；S3～S6 继续验证首轮满足、达到最大
轮数、循环体失败以及无效定义拒绝与资源清理。

## 场景验证

| 场景 | 测试方法 | 状态 | 预期结果与结束状态证据 |
| --- | --- | --- | --- |
| S1 固定三轮循环 | `s1FixedLoopRunsEveryBodyStepThreeTimesBeforeFollowingStep` | PASS | 三个 Pause 等待点分别查询到 current v1/v2/v3；原因为 `INITIAL`、`FIXED_COUNT_NOT_REACHED`、`FIXED_COUNT_NOT_REACHED`，历史逐轮增长；首次夹具选择失败修正后同场景复验通过；重启后 v1 完整恢复；最终 current 为空、history 为三轮、循环体有序三次、后续一步一次、等待步骤 0。 |
| S2 条件第二轮满足 | `s2LoopUntilStopsAfterSecondCompletedIteration` | PASS | 第一轮 current v1/`INITIAL`，提交 WAIT 后第二轮 current v2/`CONDITION_NOT_SATISFIED` 且 history 保留 v1；重启后相同；提交 DONE 后不创建第三轮，最终 current 为空、history 两轮、后续一步一次、等待步骤 0。 |
| S3 首轮满足仍完整一轮 | `s3LoopUntilCompletesOneFullIterationWhenConditionMatchesFirst` | PASS | 首轮体与条件步骤完整成功后才判断；不创建第二轮，循环及后续步骤均 SUCCESS，等待步骤 0。 |
| S4 达最大轮数失败 | `s4LoopUntilFailsAfterMaximumCompletedIterations` | PASS | 两轮体与条件步骤完整成功且不创建第三轮；循环及 Execution 明确 FAILED，错误包含 `after 2 iterations`，后续步骤未创建。 |
| S5 循环体失败停止 | `s5BodyFailureStopsFixedAndConditionalLoops` | PASS | 固定循环和条件循环均在循环体失败后停止，无后续轮次和后续步骤；两个 Execution 均为 FAILED 且无待处理步骤。 |
| S6 无效循环定义 | `s6InvalidLoopDraftsAreRejectedAndDeletedWithoutResources` | PASS | 五类无效循环草稿均在发布时按预期原因拒绝，不产生正式版本或 Execution；删除后草稿、正式版本和运行资源均为空。 |

## 执行命令与结果

以下命令均使用本报告基本信息中的真实 PostgreSQL 环境变量及 cleanup=true：

1. `./gradlew :core:compileTestJava`：exit 0。
2. `./gradlew :core:test --tests 'org.cses.flow.core.services.executions.Uc09LoopOrchestrationTest.s1FixedLoopRunsEveryBodyStepThreeTimesBeforeFollowingStep'`：首次 exit 1；测试夹具按“任意 PAUSED TaskRun 恰好一个”选择时，同时查询到 Pause 和其 PAUSED Loop 祖先共 2 个。该失败属于测试选择错误，不是 UC 反例。
3. 同一 S1 命令单独复验：改为按 Pause 声明的 `continue` 输出从公开查询选择后 exit 0，1/1 PASS。
4. `./gradlew :core:test --tests 'org.cses.flow.core.services.executions.Uc09LoopOrchestrationTest.s2LoopUntilStopsAfterSecondCompletedIteration'`：exit 0，1/1 PASS。
5. `./gradlew :core:test --tests 'org.cses.flow.core.services.executions.Uc09LoopOrchestrationTest'`：exit 0，6/6 PASS。
6. 根工程 `./gradlew test`：执行完成，376 tests completed，5 failed，1 skipped；UC-09
   6/6 全部通过。五个失败均位于 UC-04 S8 或本 UC 之外，并在批量报告中单独归因。

## 失败与反例

- UC-09 S1～S6 没有生产反例。
- 首次 S1 失败由测试夹具误选 PAUSED Loop 祖先引起，已保持用户查询路径并按目标 Pause
  的公开恢复字段精确选择，随后稳定通过。
- 跨 UC 未解决反例：UC-04 CE-01。
- 全量回归另有三个与 UC-09/Generation 无关的稳定回归观察，详见同批次报告。

## 数量汇总

| 总场景 | PASS | FAIL | NOT_COVERED | BLOCKED |
| ---: | ---: | ---: | ---: | ---: |
| 6 | 6 | 0 | 0 | 0 |

## 测试结论

UC-09 全部六场景本次结论为 **PASS**。新增循环 Generation 语义以及原有循环成功、
失败、校验和清理路径均通过真实 PostgreSQL 验证。

## 活动资源与未解决事项

- S1～S3 到达 SUCCESS，S4/S5 到达预期 FAILED，S6 未创建 Execution；夹具关闭并
  cleanup=true 删除本轮随机租户数据，最终仍在运行的本轮 Execution=0、等待恢复的
  本轮 TaskRun=0。
- 未解决跨 UC 反例：UC-04 CE-01。
