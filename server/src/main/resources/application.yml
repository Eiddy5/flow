# Safe defaults shared by standalone Flow and host applications embedding the
# complete Flow server module. Application identity, ports and platform
# bootstrap configuration live in the flow-standalone environment.
