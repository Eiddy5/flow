micronaut:
  config-client:
    enabled: ${FLOW_DEMO_PLATFORM_MANAGED:false}

consul:
  client:
    registration:
      enabled: ${FLOW_DEMO_PLATFORM_MANAGED:false}
    config:
      enabled: ${FLOW_DEMO_PLATFORM_MANAGED:false}
    watch:
      service:
        enabled: ${FLOW_DEMO_PLATFORM_MANAGED:false}
